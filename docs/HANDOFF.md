# SecuPilot Handoff

## Source Of Truth
- Root path: `D:\产品设计\New folder`
- Rule: only this directory may be treated as runnable truth.
- Any zip, copied folder, or loose file outside this root is input-only material until it is explicitly imported here.

## Current Snapshot
- Snapshot ID: `S5-PRD-INTAKE-AUTOMATION-TRIGGER-2026-04-22-001`
- Stage: `s5-prd-intake-automation-trigger`
- Owner of code/doc changes: `VS Code / human-supervised workspace`
- Owner of product/governance decisions: `Human`
- Reviewer: `Claude Web or human-supervised external review when available; cc switch output is test evidence only`

## Current Scope
- Canonical application code now lives under `backend/`.
- Canonical data/build utilities now live under `scripts/`.
- Canonical generated datasets now live under `mock_data/`.
- Root-level `.py` files are compatibility wrappers so the current runnable snapshot is not broken.
- Sprint 3 planning documents now live under `docs/` and are part of the governed handoff set.
- Git workflow scaffolding now lives under `.githooks/`, `.gitignore`, `.gitattributes`, `.gitmessage.txt`, and `scripts/install_git_workflow.py`.
- Runtime productization now lives under `backend/app/main.py`, `backend/app/runtime_service.py`, and `run_runtime.py`.
- Case view contract and shaping layer now live under `docs/SP3_B1_Case_View_Contract.md` and `backend/app/agents/case_view.py`.
- Jarvis case embedding now lives under `backend/app/agents/jarvis_hunt_engine.py` and the `case_view.jarvis_plan` mapping.
- Degraded UX contract now lives under `docs/SP3_B4_Degraded_UX_Contract.md` and the `case_view.executive_summary` / `case_view.analysis_limits` mappings.
- Case contract tests now freeze summary, Jarvis, evidence refs, and degraded action semantics under `backend/tests/test_case_view.py`.
- S3-C-0 adapter contract skeleton now lives under `backend/app/tools/siem_adapter.py`, `docs/S3C0_ADAPTER_CONTRACT.md`, and the orchestrator's `TimeRangeSpec + AdapterResult` flow.
- S3-C follow-up now adds dedicated SIEM timeout handling before tool execution, plus explicit `partial / unavailable / gather timeout / scenario metadata timeout` regression coverage.
- S3-C-1 now adds the first production-facing SIEM adapter path, runtime configuration consumption, and transport-normalization tests under `docs/S3C1_SIEM_ADAPTER_BOUNDARY.md`.
- S3-C-1 follow-up now adds thin vendor field mapping profiles (`splunk_like` / `elastic_like`) and a governed `runtime_mode=production -> investigate_sync -> threat_case` smoke path.
- S3-C-1 hardening now maps non-JSON transport responses to `production_transport_bad_response:*` and exposes `adapter_type / adapter_configured` in runtime readiness.
- S3-C-2 now hardens vendor profiles by filling canonical `activity_name`, escaping `splunk_like` free text, replacing risky `elastic_like query_string` usage, and freezing fixture playback tests under `docs/S3C2_VENDOR_PROFILE_HARDENING.md`.
- S3-C-2 follow-up now proves `activity_name` flows end-to-end through the production smoke path and lowers generic `action` behind `rule.name` in canonical field selection.
- S3-C-3 now adds offline vendor replay fixtures, `ReplayTransport`, and two end-to-end replay tests under `docs/S3C3_VENDOR_REPLAY_SPEC.md` and `backend/tests/test_vendor_replay.py`.
- S3-D-1 now hardens the governed release gate by aligning fast/full gate documentation, requiring review-pack completeness, and expanding verification to review artifacts as first-class acceptance inputs.
- S3-D-2 now freezes wrapper inventory, canonical import guidance, and wrapper retirement criteria under `docs/PROJECT_STRUCTURE.md` and `docs/S3D2_WRAPPER_RETIREMENT_PLAN.md`.
- S3-D-2 follow-up now distinguishes standalone smoke scripts from compatibility test wrappers, moves canonical test commands ahead of wrapper shortcuts, and defines wrapper retirement counter start plus rollback behavior.
- S3-D-3 now freezes runtime operability semantics under `docs/S3D3_RUNTIME_OPERABILITY_CONTRACT.md` and exposes operator-facing `state_class / failure_category / operator_message` fields in runtime health and readiness.
- S3-D-3 follow-up now clarifies `DEGRADED` as a reserved future runtime state, makes `static_data_path` authoritative over the legacy `mock_data_path` alias, and adds log-backed coverage for both mock and production static-data startup failures.
- S3-D-4 now freezes snapshot transition discipline, Claude Web upload rules, and alignment rules so future stage changes are driven by generated review artifacts instead of chat-only instructions.
- S3-D-4 follow-up now adds the missing package step to the snapshot checklist, makes the generated review pack authoritative over handwritten upload lists, and forbids stale zip reuse during review-pack generation.
- SP3-D-13 now records the governance closeout decision under `docs/S3D13_GOVERNANCE_REVIEW_PASS.md`.
- Sprint 4 planning is now frozen under `docs/SPRINT4_PRD.md` and `docs/SPRINT4_JIRA_BACKLOG.md`.
- Sprint 4 planning follow-up now makes `SP4-A-3` the explicit unlock for `SP4-B-1`, requires `SP4-C-1` to freeze persistence backend selection, and clarifies that `SP4-D-1` plus `SP4-D-3` may start in parallel while downstream pilot smoke work waits for `S4-A + S4-B + S4-C`.
- S4-A-1 now freezes the static-data source contract under `docs/S4A1_STATIC_DATA_SOURCE_CONTRACT.md`, `backend/app/tools/static_data_sources.py`, and the new static-data config fields in `backend/app/config.py`.
- S4-A-2 now routes T1 / T4 / T5 bootstrap static data through `backend/app/tools/static_data_adapters.py`, replaces the four direct JSON bootstrap reads in `backend/app/agents/graph.py`, and freezes the adapter baseline under `docs/S4A2_STATIC_DATA_ADAPTER_BASELINE.md`.
- S4-A-3 now adds a canonical host identity resolver under `backend/app/tools/host_identity.py`, routes SIEM asset queries plus T3 / blast target selection through the same resolver, and freezes the behavior under `docs/S4A3_HOST_IDENTITY_RESOLVER.md`.
- S4-A-4 now adds TTL cache semantics for static-data adapters, freezes governed refresh behavior under `docs/S4A4_STATIC_DATA_CACHE_AND_TESTS.md`, and extends regression coverage for cache expiry, missing sources, and static-data readiness classification.
- S4-A-5 now records the governed source integration review closeout under `docs/S4A5_SOURCE_INTEGRATION_REVIEW_PASS.md`, accepts asset inventory as the authoritative host-identity seed for Sprint 4, and explicitly lists unsupported source fields plus deferred source-mode work before downstream pilot execution.
- S4-B-1 now freezes the production-facing EDR process-event contract under `docs/S4B1_EDR_ADAPTER_CONTRACT.md`, `backend/app/tools/edr_adapter.py`, and `backend/tests/test_edr_adapter_contract.py` without changing frozen T3 input or output semantics.
- S4-B-2 now routes T3 process-event ingestion through explicit EDR adapters, replaces the last direct `process_events` bootstrap read in `backend/app/agents/graph.py`, and freezes the baseline under `docs/S4B2_PRODUCTION_EDR_INGESTION_BASELINE.md`.
- S4-B-3 now adds offline EDR replay fixtures plus `EDRReplayTransport`, proves canonical normalization for `crowdstrike_like` and `elastic_defend_like`, and freezes replay validation under `docs/S4B3_EDR_REPLAY_FIXTURES.md`.
- S4-B-4 now proves T3 anomaly detection, persistence detection, IOC extraction, and `partial / degraded` semantics remain stable under production-shaped EDR ingestion, frozen under `docs/S4B4_T3_PRODUCTION_PARITY_TESTS.md`.
- S4-B-4 follow-up now removes machine-local mock-data assumptions from the parity tests and makes `backend/app/tools/siem_adapter.py` safe under standard-logging fallback when `structlog` is unavailable.
- S4-B-5 now closes the telemetry stream review under `docs/S4B5_TELEMETRY_REVIEW_PASS.md`, removes the remaining machine-local EDR test paths, and clarifies that `extra{}` may exist only as audit/debug baggage and must never influence T3 output or case contracts.
- S4-C-1 now freezes the durable case schema, lifecycle status model, action-request record, audit record, and pilot persistence backend choice under `docs/S4C1_PERSISTENT_CASE_SCHEMA_FREEZE.md` and `backend/app/tools/persistent_case.py`.
- S4-C-2 now adds a governed SQLite case store, `POST /api/v1/cases`, `GET /api/v1/cases/{case_id}`, immutable record helpers, and retrieval/persistence regression coverage under `docs/S4C2_CASE_STORE_AND_RETRIEVAL_API.md`, `backend/app/tools/case_store.py`, and `backend/tests/test_case_store.py`.
- S4-C-3 now freezes the action-request and approval contract under `docs/S4C3_ACTION_REQUEST_AND_APPROVAL_CONTRACT.md`, adds durable create/submit/approve/reject/cancel helpers plus runtime APIs, and folds the prior unstructured create-case exception path into governed `internal_error` handling.
- S4-C-4 now locks lifecycle regression coverage under `docs/S4C4_CASE_LIFECYCLE_REGRESSION_TESTS.md`, blocks closed-case reject/cancel updates, and proves deterministic create/retrieve/review/approve/close audit history through persisted round-trips.
- S4-C-4 follow-up now routes the lifecycle regression create step through `runtime_service.create_case_sync()` while keeping deterministic case semantics via a controlled investigation output, closing the last review gap before `SP4-C-5`.
- S4-C-5 now records the persisted case lifecycle product closeout under `docs/S4C5_PRODUCT_REVIEW_PASS.md`, accepting `S4-C` as the Sprint 4 baseline for pilot-facing analyst and manager usage.
- S4-D-1 now freezes the pilot environment and secret profile contract under `docs/S4D1_ENVIRONMENT_AND_SECRET_PROFILE_FREEZE.md`, exposes profile-level missing requirements through runtime readiness, and records that `SP4-D-2` must not start until `SP4-A-5` has an explicit governed closeout record.
- S4-D-1 follow-up now treats `siem_vendor=generic_http` as invalid for `pilot_local`, exposes a loopback-host warning for remote pilot access in readiness, removes `mock_data_path` from `pilot_local` optional fields, and clarifies the deterministic lifecycle regression create path in `backend/tests/test_case_lifecycle_regression.py`.
- S4-D-2 now freezes one governed pilot smoke path under `docs/S4D2_PILOT_SMOKE_PATH.md`, exposes `POST /api/v1/pilot-smoke`, and proves `pilot_local readiness -> production-shaped investigate -> persistent create_case -> get_case` through `backend/app/runtime_service.py`, `backend/app/main.py`, and dedicated runtime regression coverage.
- S4-D-2 review follow-up now keeps `pilot_smoke_sync()` on a single readiness snapshot for downstream error payloads, documents the outer `200 OK` vs inner `create_case=201` contract, and adds regression coverage for `time_range` propagation plus investigate/get-case failure stages.
- S4-D-3 now freezes the operator runbook and failure-triage baseline under `docs/S4D3_OPERATOR_RUNBOOKS_AND_FAILURE_TRIAGE.md`, links governed failure-step triage back from `docs/S4D2_PILOT_SMOKE_PATH.md`, and adds a redacted operator triage artifact checklist under `docs/RELEASE_PROCESS.md`.
- S4-D-4 now freezes the pilot validation gate baseline under `docs/S4D4_PILOT_VALIDATION_GATE.md`, exposes `py -3 scripts/git_preflight.py --mode pilot` as the deterministic gate entry, and extends `scripts/verify_release.py` to prove `S4D2 / S4D3 / S4D4` pilot-governed artifacts are present in the manifest, review pack, and release zip.
- S4-D-5 now records the readiness review record under `docs/S4D5_PILOT_READINESS_REVIEW.md`, the review closeout under `docs/S4D5_PILOT_READINESS_REVIEW_PASS.md`, and accepts the current D-stream baseline as the pilot-ready operator baseline for Sprint 4.
- Sprint 4 integrated closeout now records `docs/S4_SPRINT4_PILOT_BASELINE_REVIEW.md` as the integrated review record and `docs/S4_SPRINT4_PILOT_BASELINE_REVIEW_PASS.md` as the integrated PASS closeout; `S4-A / S4-B / S4-C / S4-D` jointly satisfy the Sprint 4 Delivery Definition, and the current governed baseline is accepted as the Sprint 4 integrated pilot baseline for controlled pilot use.
- Sprint 5 planning now records `docs/SPRINT5_DISCOVERY_BRIEF.md` as the Sprint 5 discovery record, `docs/SPRINT5_PRD.md` as the Sprint 5 PRD draft baseline, and `docs/SPRINT5_JIRA_BACKLOG.md` as the Sprint 5 backlog baseline. Sprint 5 starts from `S4-INTEGRATED-2026-04-13-001`; `S5-A` is the first product direction, `S5-E` is a lightweight parallel governance decision, `S5-C` is conditional, and `S5-B / S5-D` remain discovery-limited until external inputs exist.
- S5-A-1 now records `docs/S5A1_PILOT_PREPARATION_CHECKLIST.md` as the controlled pilot preparation checklist. It separates preparation, dry-run, redaction, sign-off, and external pilot execution boundaries, and confirms S4/S5 governed evidence without starting external pilot execution.
- S5-A-2 now records `docs/S5A2_DRY_RUN_EVIDENCE_TEMPLATE.md` as the dry-run evidence template baseline. It converts the S5-A-1 preparation checklist into fillable evidence capture for the pilot validation gate and `POST /api/v1/pilot-smoke` path while preserving the dry-run-only boundary.
- S5-A-3 now records `docs/S5A3_PILOT_RUN_LOG_REDACTION_BOUNDARY.md` as the pilot run log evidence redaction boundary baseline. It defines what dry-run, pilot-prep, and future sign-off evidence may retain without replacing S4-D-3 / `RELEASE_PROCESS` operator escalation triage redaction rules.
- S5-A-4 now records `docs/S5A4_PILOT_SIGN_OFF_CHECKLIST.md` as the pilot sign-off checklist baseline. It defines `PASS` / `HOLD` / `NEEDS_DECISION` judgment for controlled dry-run sign-off without creating a real customer/operator sign-off record or authorizing external pilot execution.
- S5-A-5 now records `docs/S5A5_DRY_RUN_EXTERNAL_PILOT_BOUNDARY_CLOSEOUT.md` as the dry-run versus external pilot boundary closeout. It closes the dry-run/external-pilot boundary for the S5-A preparation package, but it is not the S5-A stream review pass.
- S5-A Controlled Pilot Preparation now records `docs/S5A_REVIEW_PASS.md` as the stream-level review pass. S5-A is closed as a governed controlled pilot preparation baseline. This does not authorize external pilot execution. External pilot execution, S5-C triggering, S5-E governance, and S5-B/S5-D scope expansion remain gated on separate product/governance decisions.
- Post-S5-A decision checkpoint now records `docs/S5_EXTERNAL_PILOT_DECISION_CHECKPOINT.md` as the governed post-S5-A decision record. S5-A is closed as a governed controlled pilot preparation baseline. This checkpoint does not authorize external pilot execution. The recommended immediate next step is `S5-E-1` lightweight governance review while external pilot inputs are gathered, unless product/governance can immediately provide all required pilot scope, role, access, evidence retention, redaction, go/no-go, and rollback/hold authority inputs.
- S5-E-1 now records `docs/S5E1_AI_COLLAB_OPERATING_MODEL_REVIEW.md` as the AI collaboration operating model review closeout. S5-E-1 reviews `docs/AI_COLLAB_OPERATING_MODEL.md` and records `NEEDS_LIGHT_REVISION`. `docs/AI_COLLAB_OPERATING_MODEL.md` remains ungoverned and must not be treated as a governed contract. Future governance of `docs/AI_COLLAB_OPERATING_MODEL.md` requires a separate light-revision task, snapshot transition, manifest update, verification refresh, and review-pack alignment.
- S5-E-2 now records `docs/S5E2_AI_COLLAB_LIGHT_REVISION_CLOSEOUT.md` as the AI collaboration operating model light revision closeout. `docs/AI_COLLAB_OPERATING_MODEL.md` was lightly revised but remains ungoverned. `docs/S5E2_AI_COLLAB_LIGHT_REVISION_CLOSEOUT.md` is the governed closeout record. Future governance of `docs/AI_COLLAB_OPERATING_MODEL.md` requires a separate snapshot path and manifest entry.
- S5-E-3 now records `docs/S5E3_AI_COLLAB_GOVERNANCE_DECISION.md` as the AI collaboration operating model governance decision closeout. S5-E-3 records `APPROVE_GOVERNANCE` for `docs/AI_COLLAB_OPERATING_MODEL.md` as a future governed artifact. This does not itself govern `docs/AI_COLLAB_OPERATING_MODEL.md`; it must remain outside manifest key_files until a separate governance snapshot explicitly adds it.
- S5-E-4 now governs `docs/AI_COLLAB_OPERATING_MODEL.md` as the team collaboration operating model. The document governs collaboration workflow only; it does not override product PRDs, runtime contracts, `docs/RELEASE_PROCESS.md`, manifest, `verify_report`, review pack, release zip, or full gate requirements, and it does not introduce external tool dependencies.
- Sprint 5 midpoint decision now records `docs/S5_MIDPOINT_DECISION.md` as the governed planning decision record. S5-A Controlled Pilot Preparation is closed, S5-E AI Collaboration Operating Model is governed as collaboration workflow guidance, external pilot execution remains unauthorized, and the next product path requires product/governance confirmation before implementation begins. The midpoint recommendation is conditional: external pilot decision package only if all required pilot inputs are available; S5-C only with dry-run feedback or explicit product decision; S5-B/S5-D discovery only if external inputs are available.
- External pilot input intake now records `docs/S5_EXTERNAL_PILOT_INPUT_INTAKE.md` as the governed intake checklist. The intake defines the seven required input categories for any later external pilot decision package, does not authorize external pilot execution, and records that all seven categories currently require explicit product/governance confirmation before a decision package can proceed.
- External pilot input assessment now records `docs/S5_EXTERNAL_PILOT_INPUT_ASSESSMENT.md` as the initial governed assessment state for the seven external pilot input categories. All seven categories are currently `UNKNOWN` in governed repo evidence, the project cannot draft an external pilot decision package yet, and external pilot execution remains unauthorized.
- S5-C case workflow hardening plan now records `docs/S5C_CASE_WORKFLOW_HARDENING_PLAN.md` as the planning-only closeout. S5-C planning may proceed by explicit product decision while external pilot inputs remain `UNKNOWN`; this plan does not authorize implementation. S5-C must preserve S4-C durable case lifecycle, audit history, review/approval/close semantics, and non-destructive action request guarantees. `S5-B / S5-D` remain discovery-only unless source/telemetry inputs become available and a separate product/governance decision expands their scope.
- S5-C-1 case workflow journey contract now records `docs/S5C1_CASE_WORKFLOW_JOURNEY_CONTRACT.md` as the governed journey contract. It defines descriptive `pilot_local` operator/analyst/manager/reviewer workflow roles and journey stages, preserves S4-C durable case lifecycle, audit history, review/approval/close semantics, closed-case safety, and non-destructive action request semantics, and does not authorize runtime/API/test implementation, enterprise RBAC, ticketing integration, workflow engine, destructive response automation, external pilot execution, or real customer/operator sign-off.
- S5-C-2 close reason and lifecycle semantics now records `docs/S5C2_CLOSE_REASON_AND_LIFECYCLE_SEMANTICS.md` as the governed contract/planning artifact. It defines close reason taxonomy and lifecycle semantics, preserves S4-C durable case lifecycle, audit history, closed-case safety, and non-destructive action request semantics, and does not authorize runtime/API/test implementation, public close-case API implementation, new persisted lifecycle states, destructive response automation, external pilot execution, or real customer/operator sign-off.
- S5-C-3 action request approval / denial contract now records `docs/S5C3_ACTION_REQUEST_APPROVAL_DENIAL_CONTRACT.md` as the governed contract/planning artifact. Claude Code delta review found no `HIGH` or `MEDIUM` blockers and one `LOW` wording issue, which was addressed by avoiding runtime/system authorization wording. S5-C-3 preserves S4-C durable lifecycle, audit history, closed-case safety, and non-destructive action-request semantics; it does not authorize implementation, runtime/API/test changes, schema changes, new persisted lifecycle states, public close-case API implementation, destructive response automation, external pilot execution, or real customer/operator sign-off. `S5-C-4` owns the public close-case HTTP endpoint decision, and `S5-C-5` owns the case workflow review pass.
- S5-C-4 public close-case HTTP endpoint decision now records `docs/S5C4_PUBLIC_CLOSE_CASE_HTTP_ENDPOINT_DECISION.md` as the governed decision artifact. Claude Code delta review found no `HIGH`, `MEDIUM`, `LOW`, or `INFO` findings. The preliminary decision is `KEEP_DEFERRED`: S5-C-4 does not implement or authorize the public close-case HTTP endpoint. Any future endpoint implementation requires a later governed implementation ticket with exact files, behavior changes, acceptance criteria, test plan, method/path/request/response/error model, audit behavior, and pending action request behavior. `S5-C-5` owns the case workflow review pass.
- S5-C-5 case workflow review pass now records `docs/S5C5_CASE_WORKFLOW_REVIEW_PASS.md` as the governed stream-level review pass. Claude Code delta review found no `HIGH`, `MEDIUM`, `LOW`, or `INFO` findings. The S5-C stream verdict is `PASS`: the planning/contract stream is accepted as a governed baseline for future scoped implementation tickets. S5-C PASS does not authorize implementation, external pilot execution, public close-case endpoint implementation, real SIEM/EDR/source-system access, or real customer/operator sign-off. S5-C-4 selected `KEEP_DEFERRED` for the public close-case HTTP endpoint. Any future implementation ticket must define exact files, behavior changes, acceptance criteria, test plan, audit/evidence behavior, redaction/secret handling, pending action request behavior if relevant, hold/rollback criteria, and `reviewer_when_cc_implements` if Claude Code is primary implementor.
- S5-C implementation decision checkpoint now records `docs/S5C_IMPLEMENTATION_DECISION_CHECKPOINT.md` as the governed decision-routing artifact. Claude Code delta review found no `HIGH`, `MEDIUM`, `LOW`, or `INFO` findings. The selected preliminary route is `S5-C-IMPL-1_DOC_TEST_ALIGNMENT_ONLY`; this is only a next-route selection and does not authorize implementation. S5-C PASS remains a planning/contract baseline only. S5-C-4 selected `KEEP_DEFERRED` for the public close-case HTTP endpoint, and external pilot execution, real SIEM/EDR/source-system access, and real customer/operator sign-off remain unauthorized. Any future implementation ticket must define `primary_implementor`, `reviewer`, `reviewer_when_cc_implements` if Claude Code is primary implementor, exact files, exact behavior changes, acceptance criteria, test plan, audit/evidence behavior, redaction/secret handling check, pending action request behavior if relevant, hold/rollback criteria, and external review requirement.
- S5-C-IMPL-1 doc/test alignment now records `docs/S5C_IMPL1_DOC_TEST_ALIGNMENT.md` as the governed documentation/test-plan alignment artifact. S5-C-IMPL-1 is documentation/test-plan alignment only, with preliminary decision `READY_FOR_SCOPED_TEST_HARDENING_TICKET`. Claude Code delta review raised one conditional `MEDIUM` precondition finding, but exact verification resolved it: `releases/release_manifest.json` contains `docs\S5C_IMPLEMENTATION_DECISION_CHECKPOINT.md`, `docs/HANDOFF.md` records the checkpoint as governed, and `docs/S5C_IMPL1_DOC_TEST_ALIGNMENT.md` includes `docs/S5C_IMPLEMENTATION_DECISION_CHECKPOINT.md` in Alignment Inputs. No source content blocker remains. S5-C-IMPL-1 authorizes no code, test, runtime, API, schema, dependency, external pilot, real SIEM/EDR/source-system, or real customer/operator sign-off changes. The public close-case endpoint remains `KEEP_DEFERRED`. Any future scoped test-hardening ticket must define `primary_implementor`, `reviewer`, `reviewer_when_cc_implements` if Claude Code is primary implementor, exact files, exact behavior changes, exact tests to add or modify, acceptance criteria, test command, audit/evidence behavior if relevant, redaction/secret handling check, hold/rollback criteria, and external review requirement.
- S5-C-IMPL-2 action request test hardening now records `backend/tests/test_case_action_request_contract.py` as the only modified file for this test-only closeout. It strengthens existing action request approval, rejection, cancellation, audit, snapshot immutability, and closed-case safety assertions without changing production code, runtime, API, schema, dependency, public close-case endpoint, external pilot, real SIEM/EDR/source-system, or real customer/operator sign-off behavior. Targeted test passed with `py -3 -m unittest -q backend.tests.test_case_action_request_contract`. Claude Code delta review found no `HIGH` or `MEDIUM` findings; one `LOW` duplicate assertion cleanup was addressed, and the `INFO` observations were non-blocking.
- S5-C-IMPL-3 closed-case safety regression expansion now records `backend/tests/test_case_lifecycle_regression.py` as the only modified file for this test-only closeout. It strengthens deterministic closed-case retrieval, audit, action-request preservation, response immutability, and invalid lifecycle transition assertions without changing production code, runtime, API, schema, dependency, docs semantics, public close-case endpoint, external pilot, real SIEM/EDR/source-system, or real customer/operator sign-off behavior. Targeted test passed with `py -3 -m unittest -q backend.tests.test_case_lifecycle_regression`. Claude Code delta review found no `HIGH`, `MEDIUM`, or `LOW` findings; one `INFO` redundancy observation was non-blocking. The public close-case endpoint remains `KEEP_DEFERRED`, and the new helper-only invalid lifecycle transition guard rejects `closed -> archived` without adding `archived` as an accepted state.
- S5-C-IMPL-4 test hardening review pass now records `docs/S5C_IMPL4_TEST_HARDENING_REVIEW_PASS.md` as the governed review-pass artifact for the S5-C implementation-preparation / test-hardening mini-stream. Predecessor baseline was `S5-C-IMPL-2026-04-14-003`. Claude Code review verdict was `SAFE`: no `HIGH` or `MEDIUM` findings, `LOW-1` was fixed before closeout by correcting the IMPL-2 residual observation, and `INFO-1` about IMPL-2/IMPL-3 having test-file changes rather than separate docs was non-blocking. Verdict: `PASS` for the S5-C implementation-preparation / test-hardening mini-stream only. PASS does not authorize production implementation, additional test edits, runtime/API/schema changes, dependency changes, public close-case endpoint work, external pilot execution, real customer/operator sign-off, or real external access. The public close-case HTTP endpoint remains `KEEP_DEFERRED`.
- Sprint 5 stream review checkpoint now records `docs/S5_STREAM_REVIEW_CHECKPOINT.md` as the governed route-selection checkpoint. Predecessor baseline was `S5-C-IMPL-2026-04-14-004`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, or `LOW` findings, and `INFO-1` was corrected before closeout by explicitly tracing S5-A evidence to `docs/S5_EXTERNAL_PILOT_DECISION_CHECKPOINT.md`. Preliminary verdict: `RECOMMEND_AI_COLLAB_AMENDMENT_DECISION`. This checkpoint does not authorize implementation, runtime/API/schema/test/dependency changes, public close-case endpoint work, external pilot execution, real customer/operator sign-off, real external access, or direct `docs/AI_COLLAB_OPERATING_MODEL.md` modification. S5-C remains parked safely, S5-C-4 public close-case endpoint remains `KEEP_DEFERRED`, and `S5-B / S5-D` remain discovery-only unless source/telemetry inputs become available.
- AI Collaboration Operating Model amendment decision now records `docs/AI_COLLAB_OPERATING_MODEL_AMENDMENT_DECISION.md` as the governed docs-only decision artifact. Predecessor baseline was `S5-STREAM-REVIEW-2026-04-14-001`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, or `LOW` findings, and `INFO-1` was corrected before closeout by explicitly listing dependency changes in the Preliminary Decision non-meaning. Preliminary decision: `ACCEPT_AMENDMENT_PLAN`. This decision accepts the nine pending collaboration rules only as candidates for a later bounded amendment ticket; it does not modify `docs/AI_COLLAB_OPERATING_MODEL.md` and does not make the nine rules newly governed until the later amendment is completed. It does not authorize code/test/runtime/API/schema/dependency/release tooling changes, external pilot execution, real customer/operator sign-off, real external access, or S4-C/S5-C product semantic changes. The later amendment ticket must name exact `docs/AI_COLLAB_OPERATING_MODEL.md` sections and wording scope.
- AI_COLLAB bounded amendment now records `docs/AI_COLLAB_OPERATING_MODEL.md` as the governed amended collaboration operating model. Predecessor baseline was `AI-COLLAB-AMEND-DECISION-2026-04-14-001`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, or `LOW` findings, and `INFO-1` was corrected before closeout by adding the explicit human-directed emergency correction / human go-no-go carve-out to the single-writer rule. All nine accepted rules were codified. The amendment is docs-only and does not change product semantics, S4-C/S5-C semantics, runtime/API/schema/test behavior, release tooling, manifest format, dependencies, external pilot authorization, real customer/operator sign-off, or real external access. It does not retroactively invalidate previously governed baselines. Human go/no-go remains final, and external review remains trigger-based rather than a routine-ticket default blocker.
- Sprint 5 next route after AI_COLLAB amendment now records `docs/S5_NEXT_ROUTE_AFTER_AI_COLLAB_AMENDMENT.md` as the governed product-mainline route-selection checkpoint. Predecessor baseline was `AI-COLLAB-AMEND-2026-04-14-001`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, or `LOW` findings; `INFO-1` was tooling-only and required no content correction. Preliminary verdict: `RECOMMEND_OPEN_BOUNDED_S5_B_DISCOVERY_TICKET`. This checkpoint does not authorize implementation, runtime/API/schema/test/dependency changes, real source-system access, external pilot execution, real customer/operator sign-off, public close-case endpoint work, S5-C runtime work, or additional AI_COLLAB modification. S5-C remains parked, S5-C-4 public close-case endpoint remains `KEEP_DEFERRED`, the S5-A package remains `NOT_READY` due to `UNKNOWN` external pilot inputs, and `S5-B / S5-D` remain discovery-only unless governed inputs become available. The next recommended ticket must use the governed AI_COLLAB fields: `primary_implementor`, `reviewer`, `reviewer_when_cc_implements` if relevant, `requires_external_review` decision, exact files/scope, acceptance criteria, validation, and HOLD triggers.
- S5-B bounded source/input discovery now records `docs/S5B_BOUNDED_SOURCE_INPUT_DISCOVERY.md` as the governed discovery-only artifact. Predecessor baseline was `S5-NEXT-ROUTE-2026-04-15-001`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, or `LOW` findings; `INFO-1` about the Discovery Questions table owner column was non-blocking and required no content correction. Preliminary recommendation: `PROCEED_TO_S5_B_1_SOURCE_INPUT_GAP_MATRIX`. This document is discovery-only and does not authorize implementation, runtime/API/schema/test/dependency changes, real source-system access, live refresh, external authentication, CMDB sync, multi-tenant ownership, external pilot execution, real customer/operator sign-off, or S4-A identity authority changes. S4-A identity authority is inherited and must not be bypassed. The S5-A external pilot package remains `NOT_READY` / `UNKNOWN`, S5-C remains parked, and the public close-case endpoint remains `KEEP_DEFERRED`. Future `S5-B-3`, `S5-B-4`, and `S5-B-5` candidates require external review if they freeze identity/source semantics or close the stream.
- S5-B-1 source input gap matrix now records `docs/S5B1_SOURCE_INPUT_GAP_MATRIX.md` as the governed docs-only gap matrix. Predecessor baseline was `S5-B-DISCOVERY-2026-04-15-001`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, or `LOW` findings; `INFO-1` was fixed by removing redundant S4-A wording, and `INFO-2` was addressed with a 7/7 S5-A input relationship mapping including `Participating roles` as not directly applicable to S5-B source/input discovery except for future source ownership / external input request confirmation. Preliminary recommendation: `PROCEED_TO_S5_B_2_SYNTHETIC_SOURCE_FIXTURE_SHAPE`. This document is a docs-only gap matrix and does not authorize implementation, source adapter changes, runtime/API/schema/test/dependency changes, real source-system access, live refresh/sync, external authentication, CMDB sync, multi-tenant ownership, external pilot execution, real customer/operator sign-off, or S4-A identity authority changes. Status vocabulary remains `UNKNOWN` / `NEEDS_DECISION` / `DEFERRED` / `NOT_APPLICABLE` only; no status means `READY`, `APPROVED`, `IMPLEMENTABLE`, or `PILOT_READY`. S4-A identity authority is preserved, the S5-A external pilot package remains `NOT_READY` / `UNKNOWN`, S5-C remains parked, and the public close-case endpoint remains `KEEP_DEFERRED`. The next recommended route is `S5-B-2 Synthetic Source Fixture Shape`, still non-authorizing.
- S5-B-2 synthetic source fixture shape now records `docs/S5B2_SYNTHETIC_SOURCE_FIXTURE_SHAPE.md` as the governed docs/table-only fixture-shape discovery artifact. Predecessor baseline was `S5-B-DISCOVERY-2026-04-15-002`. Claude Code review verdict was `SAFE`: no `HIGH` or `MEDIUM` findings, and `LOW-1` was fixed before closeout by adding a `requires_external_review` re-evaluation reminder for S5-B-3. Preliminary recommendation: `PROCEED_TO_S5_B_3_SOURCE_IDENTIFIER_MAPPING_DECISION`. This document does not authorize implementation, fixture file creation, source adapter changes, source contract freeze, runtime/API/schema/test/dependency changes, real source-system access, live refresh/sync, external authentication, CMDB sync, multi-tenant ownership, external pilot execution, real customer/operator sign-off, or S4-A identity authority changes. `source_mode_candidate` values are discussion labels only: `api_candidate` and `hybrid_candidate` remain unavailable for implementation under S4-A unless a later governed decision changes them, and `bundle_like` does not redefine current bundle behavior. S4-A identity authority and resolver priority are preserved. The S5-A external pilot package remains `NOT_READY` / `UNKNOWN`, S5-C remains parked, and the public close-case endpoint remains `KEEP_DEFERRED`. S5-B-3 must re-evaluate `requires_external_review` and trigger it if identity/source semantics or S4-A authority are frozen or changed.
- S5-B-3 source identifier mapping decision now records `docs/S5B3_SOURCE_IDENTIFIER_MAPPING_DECISION.md` as the governed docs-only decision artifact. Predecessor baseline was `S5-B-DISCOVERY-2026-04-15-003`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, or `LOW` findings; `INFO-1` and `INFO-2` were fixed before closeout by adding the source-of-truth line and explicitly listing new contract freeze or modification to frozen contract as a standalone `requires_external_review` trigger. Preliminary decision: `READY_FOR_LATER_CONTRACT_DRAFT`. This only permits later non-implementing contract candidate drafting and does not freeze mapping, approve identity mapping, authorize implementation, or validate external evidence. This document does not change S4-A identity authority, resolver priority, ambiguity handling, trust precedence, source ownership semantics, source adapter behavior, runtime/API/schema/test/dependency behavior, external pilot status, or real sign-off. S4-A `AssetInventorySnapshot` authority and resolver priority `asset_id -> hostname -> fqdn -> ip_address -> aliases` are preserved. The S5-A external pilot package remains `NOT_READY` / `UNKNOWN`, S5-C remains parked, and the public close-case endpoint remains `KEEP_DEFERRED`. S5-B-4 / S5-B-4A must evaluate `requires_external_review` and set it `true` if they freeze a contract, freeze identity/source semantics, modify a frozen contract, change S4-A authority, resolver behavior, trust precedence, source ownership semantics, accept real external evidence, or close the stream.
- S5-B-4 source freshness / provenance contract candidate now records `docs/S5B4_SOURCE_FRESHNESS_PROVENANCE_CONTRACT.md` as the governed docs-only non-implementing contract candidate. Predecessor baseline was `S5-B-DISCOVERY-2026-04-15-004`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, `LOW`, or `INFO` findings. Preliminary decision: `READY_FOR_S5_B_5_DISCOVERY_REVIEW_PASS`. `CONTRACT_CANDIDATE` does not mean frozen contract, implementation-ready, production-ready, externally validated, or pilot-ready. This document does not authorize implementation, source adapter changes, runtime/API/schema/test/dependency changes, real source-system access, live refresh/sync/polling, CMDB sync, multi-tenant ownership, external pilot execution, real customer/operator sign-off, or S4-A identity authority changes. Freshness terms do not imply live refresh/sync/polling; provenance/confidence terms do not change trust precedence; `source_system_label` and `source_mode_candidate` do not create source authority. S4-A `AssetInventorySnapshot` authority and resolver priority `asset_id -> hostname -> fqdn -> ip_address -> aliases` are preserved. The S5-A external pilot package remains `NOT_READY` / `UNKNOWN`, S5-C remains parked, and the public close-case endpoint remains `KEEP_DEFERRED`. The next recommended route is S5-B-5 Discovery Review Pass, with `requires_external_review=true` because stream closeout triggers external review under AI_COLLAB.
- S5-B-5 source/input discovery review pass now records `docs/S5B5_SOURCE_INPUT_DISCOVERY_REVIEW_PASS.md` as the governed docs-only stream review pass. Predecessor baseline was `S5-B-DISCOVERY-2026-04-15-005`. Claude Code review verdict was `SAFE`: no `HIGH`, `MEDIUM`, or `LOW` findings; `INFO-1` was fixed before closeout by adding `no S5-C reopening` and `no public close-case endpoint work` to the preliminary verdict non-meaning list. Preliminary verdict: `PASS_AND_PARK_S5_B_DISCOVERY`. `requires_external_review=true` because stream/milestone closeout triggers external review under `docs/AI_COLLAB_OPERATING_MODEL.md`. S5-B discovery artifacts are accepted only as a governed discovery baseline. `PASS_AND_PARK` does not authorize implementation, source adapter contract freeze, fixture file creation/modification, runtime/API/schema/test/dependency changes, real source-system access, live refresh/sync/polling, external authentication, CMDB sync, multi-tenant ownership, external pilot execution, real customer/operator sign-off, S4-A identity authority changes, S5-C reopening, or public close-case endpoint work. S4-A `AssetInventorySnapshot` authority and resolver priority `asset_id -> hostname -> fqdn -> ip_address -> aliases` are preserved. The S5-A external pilot package remains `NOT_READY` / `UNKNOWN`. S5-C remains parked, and the S5-C-4 public close-case endpoint remains `KEEP_DEFERRED`. Possible future routes include S5-D bounded telemetry discovery, S5-B-4A mapping contract candidate, return to external input collection, or product decision.
- S5 route after S5-B discovery closeout now records `docs/S5_ROUTE_AFTER_S5B_DISCOVERY_CLOSEOUT.md` as the governed route-selection checkpoint. Predecessor baseline was `S5-B-DISCOVERY-2026-04-15-006`. Initial Claude Code review verdict was `CONDITIONAL`; targeted re-review verdict was `SAFE`. Initial `LOW-1`, `LOW-2`, `LOW-3`, `INFO-1`, `INFO-2`, and `INFO-3` findings were all fixed before closeout, and no new findings were raised after targeted re-review. Preliminary verdict: `RECOMMEND_OPEN_S5_D_BOUNDED_TELEMETRY_DISCOVERY`. This route checkpoint does not authorize implementation, telemetry adapter implementation, source adapter implementation, runtime/API/schema/test/dependency changes, real SIEM/EDR/source/telemetry access, queue/fan-out, performance/SLA commitments, alert routing, response automation, raw telemetry payload retention, external pilot execution, real customer/operator sign-off, S4-A identity authority changes, S5-C reopening, or public close-case endpoint work. S5-B remains `PASS_AND_PARK` discovery baseline only. The S5-A external pilot package remains `NOT_READY` / `UNKNOWN`. S5-C remains parked, and the S5-C-4 public close-case endpoint remains `KEEP_DEFERRED`. The next recommended route is S5-D bounded telemetry discovery, but the actual S5-D ticket still requires a separate governed draft and review.
- S5-D bounded telemetry discovery now records `docs/S5D_BOUNDED_TELEMETRY_DISCOVERY.md` as the governed docs-only S5-D bounded telemetry discovery artifact. Predecessor baseline was `S5-ROUTE-POST-S5B-2026-04-15-001`. Claude Code review was initially `CONDITIONAL`; `LOW-1` was fixed by adding redaction/secret/evidence retention boundary changes to the `requires_external_review` re-evaluation triggers, and `INFO-1` was addressed by adding an AI_COLLAB-complete re-evaluation trigger acceptance criterion. Targeted re-review verdict was `SAFE`. Preliminary recommendation: `PROCEED_TO_S5_D_1_TELEMETRY_GAP_MATRIX`. This document does not authorize implementation, telemetry adapter work, source adapter work, fixture file creation/modification, runtime/API/schema/test/dependency changes, real SIEM/EDR/source/telemetry access, credentials/tokens/API keys, raw telemetry payloads, real telemetry event retention without separate evidence-retention/redaction approval, telemetry normalization freeze, timestamp/freshness semantics freeze, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot READY claim, external pilot execution, real customer/operator sign-off, S4-A identity authority changes, S5-C reopening, or public close-case endpoint work. S5-A remains `NOT_READY` / `UNKNOWN`. S5-B remains `PASS_AND_PARK`. S5-C remains parked, and the S5-C-4 public close-case endpoint remains `KEEP_DEFERRED`.
- S5-D-1 telemetry gap matrix now records `docs/S5D1_TELEMETRY_GAP_MATRIX.md` as the governed docs-only S5-D-1 telemetry gap matrix artifact. Predecessor baseline was `S5-D-DISCOVERY-2026-04-15-001`. Claude Code review verdict was `SAFE`, with `HIGH`, `MEDIUM`, `LOW`, and `INFO` findings all `None`. Preliminary recommendation: `PROCEED_TO_S5_D_2_SYNTHETIC_TELEMETRY_EVENT_SHAPE`. This document does not authorize implementation, telemetry adapter work, source adapter work, fixture file creation/modification, runtime/API/schema/test/dependency changes, live SIEM/EDR/source/telemetry access, credentials/tokens/API keys, raw telemetry payloads, real telemetry event retention without separate evidence-retention/redaction approval, telemetry normalization freeze, timestamp/freshness semantics freeze, telemetry provenance/confidence changing trust precedence, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot READY claim, external pilot execution, real customer/operator sign-off, S4-A identity authority changes, S5-C reopening, or public close-case endpoint work. S5-A remains `NOT_READY` / `UNKNOWN`. S5-B remains `PASS_AND_PARK`. S5-C remains parked, and the S5-C-4 public close-case endpoint remains `KEEP_DEFERRED`.
- S5-D-2 synthetic telemetry event shape now records `docs/S5D2_SYNTHETIC_TELEMETRY_EVENT_SHAPE.md` as the governed docs-only S5-D-2 synthetic telemetry event shape artifact. Predecessor baseline was `S5-D-DISCOVERY-2026-04-15-002`. Claude Code review verdict was `SAFE`, with `HIGH`, `MEDIUM`, `LOW`, and `INFO` findings all `None`. `contracts/AI_COLLAB_CONTRACT.md` was confirmed present and unmodified, but it was not part of the active S5-D-2 edit scope. Preliminary recommendation: `PROCEED_TO_S5_D_3_TELEMETRY_PROVENANCE_AND_FRESHNESS_BOUNDARY`. This document does not authorize implementation, telemetry adapter work, source adapter work, fixture file creation/modification, runtime/API/schema/test/dependency changes, live SIEM/EDR/source/telemetry access, credentials/tokens/API keys, raw telemetry payloads, real telemetry event retention without separate evidence-retention/redaction approval, telemetry schema freeze, telemetry normalization freeze, timestamp/freshness semantics freeze, telemetry provenance/confidence changing trust precedence, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot READY claim, external pilot execution, real customer/operator sign-off, S4-A identity authority changes, S5-C reopening, public close-case endpoint work, or AI_COLLAB/contract file changes. S5-A remains `NOT_READY` / `UNKNOWN`. S5-B remains `PASS_AND_PARK`. S5-C remains parked, and the S5-C-4 public close-case endpoint remains `KEEP_DEFERRED`.
- S5-D-3 telemetry provenance and freshness boundary now records `docs/S5D3_TELEMETRY_PROVENANCE_AND_FRESHNESS_BOUNDARY.md` as the governed docs-only S5-D-3 telemetry provenance/freshness boundary artifact. Predecessor baseline was `S5-D-DISCOVERY-2026-04-15-003`. Claude Code review verdict was `SAFE`, with `HIGH`, `MEDIUM`, and `LOW` findings all `None`; `INFO-1` about `reviewer_when_cc_implements` wording was intentionally not fixed because it is stylistic and has zero governance risk. Full gate must be run and PASS before commit. Preliminary recommendation: `PROCEED_TO_S5_D_4_REDACTION_SECRET_AND_EVIDENCE_RETENTION_BOUNDARY`. This recommendation is docs-only and non-authorizing; it does not authorize redaction policy freeze, secrets handling implementation, evidence retention, real telemetry handling, fixture creation/modification, runtime/API/schema/test/dependency changes, external review bypass, S4-A host identity authority changes, S5-C reopening, or public close-case endpoint work. This document also does not authorize live SIEM/EDR/source/telemetry access, credentials/tokens/API keys, raw telemetry payloads, telemetry event retention, telemetry schema freeze, telemetry normalization freeze, timestamp/freshness semantics freeze, provenance/confidence trust precedence freeze, source authority decisions, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot readiness claims, or AI_COLLAB operating model / contract changes. S5-A remains `NOT_READY` / `UNKNOWN`. S5-B remains `PASS_AND_PARK`. S5-C remains parked, and the S5-C-4 public close-case endpoint remains `KEEP_DEFERRED`.
- S5-D-4 redaction, secret, and evidence retention boundary now records `docs/S5D4_REDACTION_SECRET_AND_EVIDENCE_RETENTION_BOUNDARY.md` as the governed docs-only S5-D-4 boundary artifact. Predecessor baseline was `S5-D-DISCOVERY-2026-04-15-004`. Claude Code review verdict was `SAFE`, with `HIGH`, `MEDIUM`, and `LOW` findings all `None`; `INFO-1` about the word `initially` in `requires_external_review: false initially` was intentionally not fixed because the wording is precise in this high-risk boundary context and carries zero governance risk. Full gate must be run and PASS before commit. Preliminary recommendation: `ROUTE_DECISION_REQUIRED_AFTER_S5_D_4`. This recommendation is docs-only and non-authorizing; it does not authorize redaction policy freeze, secrets handling implementation, evidence retention, retention/deletion/expiry policy, evidence-pack/storage/replay behavior, real telemetry handling, raw payload handling, fixture creation/modification, runtime/API/schema/test/dependency changes, live SIEM/EDR/source/telemetry access, credentials/tokens/API keys/auth headers/cookies collection, S4-A identity authority changes, S5-C reopening, public close-case endpoint work, external pilot readiness or execution, stream/milestone closeout, or external review bypass.
- S5-D route decision after S5-D-4 now records `docs/S5D_ROUTE_DECISION_AFTER_S5D4.md` as the governed docs-only route-selection checkpoint. Predecessor baseline was `S5-D-DISCOVERY-2026-04-15-005`. Claude Code review verdict was `SAFE`, with `HIGH`, `MEDIUM`, `LOW`, and `INFO` findings all `None`. Full gate must be run and PASS before commit. Preliminary verdict: `PRODUCT_GOVERNANCE_ROUTE_DECISION_REQUIRED`. This means a later human product/governance decision must choose the next route after S5-D-4. This closeout does not itself select S5-D stream review pass, does not select policy-candidate work, does not close the S5-D stream, and does not trigger or bypass external review by itself. If a later route selects stream/milestone closeout, `requires_external_review` must be true. If a later route freezes or changes redaction/secret/evidence-retention boundaries, `requires_external_review` must be true. If a later route changes S4-A identity authority, `requires_external_review` must be true and the route should HOLD unless explicitly governed. This closeout does not authorize implementation, runtime/API/schema/test/dependency changes, fixture creation/modification, live SIEM/EDR/source/telemetry access, connector credentials/tokens/API keys/auth headers/cookies or secret material, raw telemetry payloads, raw logs/screenshots/exports/event bodies/customer/operator evidence, real telemetry event retention, redaction policy freeze, secrets handling implementation, evidence retention approval, retention/deletion/expiry policy, storage/replay/evidence-pack behavior, telemetry schema freeze, telemetry normalization freeze, timestamp/freshness semantics freeze, provenance/confidence trust precedence freeze, source authority decisions, S4-A resolver priority changes, S5-C reopening, public close-case endpoint work, external pilot readiness claims, external pilot execution, real customer/operator sign-off, external review bypass, or AI_COLLAB operating model / contract changes.
- S5-D telemetry discovery review pass now records `docs/S5D5_TELEMETRY_DISCOVERY_REVIEW_PASS.md` as the governed docs-only S5-D stream review pass. Predecessor baseline was `S5-D-ROUTE-AFTER-S5D4-2026-04-15-001`. Claude Code review verdict was `SAFE`, with `HIGH`, `MEDIUM`, `LOW`, and `INFO` findings all `None`. Full gate must be run and PASS before commit. Final verdict: `PASS_AND_PARK_S5_D_DISCOVERY`. `requires_external_review=true` because stream/milestone closeout triggers external review under AI_COLLAB. External review does not authorize implementation and does not replace human go/no-go. S5-D discovery artifacts are accepted only as a governed discovery baseline. S5-D is parked until a later human product/governance decision opens a specific next route. This closeout does not authorize implementation, telemetry adapter work, source adapter work, runtime/API/schema/test/dependency changes, fixture file creation/modification, live SIEM/EDR/source/telemetry access, credentials/tokens/API keys/auth headers/cookies or secret material, raw telemetry payloads/logs/screenshots/exports/event bodies/customer/operator evidence, real telemetry event retention, redaction policy freeze, secrets handling implementation, evidence retention approval, retention/deletion/expiry policy, storage/replay/evidence-pack behavior, telemetry schema freeze, telemetry normalization freeze, timestamp/freshness semantics freeze, provenance/confidence trust precedence freeze, source authority decisions, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot readiness improvement, external pilot execution, real customer/operator sign-off, S4-A resolver priority changes, S5-C reopening, public close-case endpoint work, AI_COLLAB operating model or contract changes, external review bypass, or automatic next route. S5-A remains `NOT_READY` / `UNKNOWN`. S5-B remains `PASS_AND_PARK` discovery baseline only. S5-C remains parked. Public close-case endpoint remains `KEEP_DEFERRED`. S4-A host identity authority and resolver order remain unchanged: `asset_id -> hostname -> fqdn -> ip_address -> aliases`.
- Sprint 5 milestone review pass now records `docs/S5_SPRINT5_MILESTONE_REVIEW_PASS.md` as the governed docs-only Sprint 5 milestone review pass. Predecessor baseline was `S5-D-DISCOVERY-REVIEW-PASS-2026-04-15-001`. Claude Code initial review verdict was `CONDITIONAL`: initial `HIGH` none, `MEDIUM` none, `LOW-1` required verdict wording correction, and `INFO` none. `LOW-1` was fixed before closeout by changing the verdict to `SPRINT5_BASELINE_REVIEW_PASS_WITH_EXTERNAL_INPUTS_PENDING`. Targeted re-review verdict was `SAFE`, with targeted `HIGH`, `MEDIUM`, `LOW`, and `INFO` findings all `None`. Full gate must be run and PASS before commit. Final verdict: `SPRINT5_BASELINE_REVIEW_PASS_WITH_EXTERNAL_INPUTS_PENDING`. `requires_external_review=true` because Sprint 5 milestone-level review / parking pass triggers external review under AI_COLLAB. External review does not authorize implementation and does not replace human go/no-go. Sprint 5 discovery, planning, collaboration-governance, source/input discovery, telemetry discovery, and case workflow planning baselines are accepted only as governed baselines. External pilot inputs remain pending / `NOT_READY` / `UNKNOWN`. Future work requires a later human product/governance decision and scoped ticket. This closeout does not authorize external pilot execution, external pilot readiness, real customer/operator sign-off, real SIEM/EDR/source/telemetry access, credentials/tokens/API keys/auth headers/cookies or secret material, raw telemetry/source payloads/logs/screenshots/exports/event bodies/customer/operator evidence, evidence retention, redaction policy freeze, secrets handling implementation, runtime/API/schema/test/dependency changes, fixture file creation/modification, S5-C runtime implementation, public close-case endpoint work, S5-B source adapter contract freeze, S5-B identity/source mapping freeze, S5-D telemetry schema/normalization/freshness/provenance/redaction/retention freeze, S4-A resolver priority changes, AI_COLLAB operating model or contract changes, external review bypass, or automatic next route. S5-A external pilot inputs remain `NOT_READY` / `UNKNOWN`. S5-B remains `PASS_AND_PARK` source/input discovery baseline only. S5-D remains `PASS_AND_PARK` telemetry discovery baseline only. S5-C remains parked as planning/contract baseline unless a later scoped implementation ticket is explicitly opened. Public close-case endpoint remains `KEEP_DEFERRED`. S4-A host identity authority and resolver order remain unchanged: `asset_id -> hostname -> fqdn -> ip_address -> aliases`.
- Sprint 5 post-milestone route decision now records `docs/S5_POST_MILESTONE_ROUTE_DECISION.md` as the governed docs-only route decision artifact. Predecessor baseline was `S5-MILESTONE-REVIEW-PASS-2026-04-15-001`. Claude Code review verdict was `SAFE`, with `HIGH` none, `MEDIUM` none, `LOW` none, and `INFO-1` / `INFO-2` non-blocking and intentionally not fixed. Full gate must be run and PASS before commit. Final recommendation: `RECOMMEND_EXTERNAL_INPUT_TRACKER_AND_S5_C_SCOPED_IMPLEMENTATION_DECISION_PREP`. `requires_external_review=true`; external review does not authorize implementation and does not replace human go/no-go. This closeout recommends two bounded follow-up directions: External Input Tracker as a docs-only tracker/decision artifact, and S5-C Scoped Implementation Decision Prep as a docs-only scoped decision artifact. This closeout does not itself create or start either follow-up ticket. The External Input Tracker is not an external pilot decision package and must not declare any input `READY`. S5-C Scoped Implementation Decision Prep is not implementation authorization. Route B follow-up must re-evaluate `requires_external_review`, and the practical expectation is that external review will likely be required once exact S5-C clauses are named. Future follow-up tickets still require separate sanity check, human confirmation, draft, review, and closeout. External pilot inputs remain `NOT_READY` / `UNKNOWN`, and external pilot execution remains unauthorized. S5-B and S5-D remain `PASS_AND_PARK` unless a later explicit reopen decision is made. S5-C public close-case endpoint remains `KEEP_DEFERRED`. S4-A host identity authority and resolver order remain unchanged: `asset_id -> hostname -> fqdn -> ip_address -> aliases`. AI_COLLAB remains collaboration workflow guidance only and is not modified. This closeout does not authorize external pilot execution, external pilot readiness claims, real customer/operator sign-off, real SIEM/EDR/source/telemetry access, credentials/tokens/API keys/auth headers/cookies or secret material, raw telemetry/source payloads/logs/screenshots/exports/event bodies/customer/operator evidence, evidence retention, redaction policy freeze, secrets handling implementation, runtime/API/schema/test/dependency changes, fixture file creation/modification, public close-case endpoint work, S5-C runtime implementation, S5-B/S5-D reopen, S5-B/S5-D policy/contract freeze, S4-A resolver priority changes, AI_COLLAB operating model or contract changes, external review bypass, or automatic next route beyond the selected bounded follow-up directions.
- Sprint 5 post-milestone external input tracker now records `docs/S5_POST_MILESTONE_EXTERNAL_INPUT_TRACKER.md` as the governed docs-only external input tracker baseline. Predecessor baseline was `S5-POST-MILESTONE-ROUTE-2026-04-16-001`. Claude Code review verdict was `SAFE`, with `HIGH` none, `MEDIUM` none, and `LOW` none. `INFO-1` was intentionally not fixed because Section 10 HOLD conditions already cover `complete`, the tracker matrix does not use `complete` as a status, and risk is nil. Full gate must be run and PASS before commit. `requires_external_review=true`; external review does not authorize implementation and does not replace human go/no-go. All seven external pilot input categories remain non-ready / `NOT_READY` / `UNKNOWN` / `NEEDS_OWNER` / `NEEDS_DECISION` as applicable, and the tracker does not declare any input `READY`. The tracker is not an external pilot decision package and does not authorize external pilot execution, real customer/operator sign-off, real external-system access, credential/token/API key/auth header/cookie/secret collection, raw payload/log/screenshot/export/event body/customer/operator evidence collection or retention, evidence retention, redaction policy freeze, secrets handling implementation, runtime/API/schema/test/dependency changes, fixture file creation/modification, S5-C runtime implementation, public close-case endpoint work, S5-B reopen, S5-D reopen, S5-B/S5-D policy or contract freeze, S4-A resolver priority changes, AI_COLLAB changes, external review bypass, human go/no-go replacement, or automatic next route. Route B / S5-C Scoped Implementation Decision Prep remains a separate later ticket requiring separate sanity check, human confirmation, draft, review, and closeout. Route B must re-evaluate `requires_external_review`, and the practical expectation is that external review will likely be required once exact S5-C clauses are named. S5-B remains `PASS_AND_PARK`. S5-D remains `PASS_AND_PARK`. S5-C remains parked unless a later scoped implementation decision is opened. Public close-case endpoint remains `KEEP_DEFERRED`. S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`.
- S5-C scoped implementation decision prep now records `docs/S5C_SCOPED_IMPLEMENTATION_DECISION_PREP.md` as the governed docs-only S5-C scoped implementation decision prep baseline. Predecessor baseline was `S5-POST-MILESTONE-EXTERNAL-INPUT-TRACKER-2026-04-16-001`. Claude Code review verdict was `SAFE`, with `HIGH` none, `MEDIUM` none, `LOW` none, and `INFO` none. Full gate must be run and PASS before commit. `requires_external_review=true`; external review does not authorize implementation and does not replace human go/no-go. This artifact does not authorize implementation, runtime/API/schema/test/dependency changes, fixture file creation/modification, S5-C runtime work, public close-case endpoint work, weakening `KEEP_DEFERRED`, destructive response automation, RBAC/ticketing/workflow engine integration, new persisted lifecycle/action-request statuses, evidence retention/storage/replay/deletion/expiry/evidence-pack behavior, redaction policy freeze, secrets handling implementation, external pilot execution/readiness, real customer/operator sign-off, real external-system access, credentials/tokens/API keys/auth headers/cookies/secret material, raw evidence/logs/screenshots/exports/payloads/event bodies/customer/operator evidence, S5-B reopen, S5-D reopen, S4-A resolver changes, AI_COLLAB changes, external review bypass, human go/no-go replacement, or automatic implementation ticket. The S5-C-3 file-header mismatch remains documentation hygiene only; HANDOFF, manifest, and later governance are controlling evidence that S5-C-3 is a governed contract/planning artifact. Public close-case HTTP endpoint remains `KEEP_DEFERRED`. S5-B and S5-D remain `PASS_AND_PARK` unless a later explicit reopen decision is made. External Input Tracker remains governed and does not make any external input `READY`. Future S5-C Scoped Implementation Decision ticket is permitted only as a separate later ticket. That later ticket must define exact governed S5-C clauses, exact file paths, exact behavior changes, exact tests/test commands, acceptance criteria, audit history expectations, evidence non-retention boundary, redaction/secret handling check, pending action request behavior if relevant, closed-case safety behavior if relevant, rollback/HOLD criteria, public close-case endpoint status preserving `KEEP_DEFERRED` unless a later explicit endpoint ticket opens it, external pilot/sign-off/access prohibitions, `requires_external_review` decision/rationale, human go/no-go, and reviewer assignments. S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`.
- S5-C scoped implementation decision now records `docs/S5C_SCOPED_IMPLEMENTATION_DECISION.md` as the governed docs-only S5-C scoped implementation decision baseline. Predecessor baseline was `S5C-SCOPED-IMPLEMENTATION-DECISION-PREP-2026-04-16-001`. Claude Code review verdict was `SAFE`, with `HIGH` none, `MEDIUM` none, `LOW` none, and `INFO` none. Full gate must be run and PASS before commit. `requires_external_review=true`; external review does not authorize implementation and does not replace human go/no-go. Final / preliminary recommendation: `PRELIMINARY_RECOMMENDATION_OPEN_FIRST_ALLOWED_S5_C_IMPLEMENTATION_TICKET_FOR_CASE_LIFECYCLE_AND_ACTION_REQUEST_SEMANTICS`. This means it permits a later separate tightly scoped implementation ticket draft for a subset of allowed areas, preferably case lifecycle and action-request semantics, but it does not authorize implementation now and does not create an automatic implementation ticket. A later implementation ticket must still define exact governed S5-C clauses, exact implementation scope, exact file paths, exact behavior changes, exact tests/test commands, acceptance criteria, audit history expectations, evidence non-retention boundary, redaction/secret handling check, pending action request behavior if relevant, closed-case safety behavior if relevant, rollback/HOLD criteria, public close-case endpoint status preserving `KEEP_DEFERRED`, external pilot/sign-off/access prohibitions, `requires_external_review` decision/rationale, human go/no-go, and implementor/reviewer assignments. Allowed later implementation-ticket candidate areas include only the decision rows marked `ALLOW_LATER_IMPLEMENTATION_TICKET`, especially case lifecycle and action-request semantics, and only through a later separate ticket. Public close-case HTTP endpoint remains `HOLD_PUBLIC_ENDPOINT` / `KEEP_DEFERRED`. Pending action request behavior around close-case workflows remains tied to `HOLD_PUBLIC_ENDPOINT` unless a later explicit endpoint ticket opens it. Evidence/audit note handling and redaction/secret handling remain `HOLD_POLICY_OR_EXTERNAL_REVIEW`. Destructive response automation and RBAC/ticketing/workflow engine integration remain `HOLD_OUT_OF_SCOPE`. External pilot/customer evidence behavior remains `HOLD_EXTERNAL_INPUT`. External Input Tracker remains governed and does not make any external input `READY`. S5-B and S5-D remain `PASS_AND_PARK` unless a later explicit reopen decision is made. The S5-C-3 file-header mismatch remains documentation hygiene only; HANDOFF, manifest, and later governance are controlling evidence that S5-C-3 is a governed contract/planning artifact. This closeout does not authorize runtime/API/schema/test/dependency changes, fixture file creation/modification, S5-C runtime work by itself, public close-case endpoint work, weakening `KEEP_DEFERRED`, destructive response automation, RBAC/ticketing/workflow engine integration, new persisted lifecycle/action-request statuses, evidence retention/storage/replay/deletion/expiry/evidence-pack behavior, redaction policy freeze, secrets handling implementation, external pilot execution/readiness, real customer/operator sign-off, real external-system access, credentials/tokens/API keys/auth headers/cookies/secret material, raw logs/screenshots/exports/payloads/event bodies/customer/operator evidence, S5-B reopen, S5-D reopen, S4-A resolver changes, AI_COLLAB changes, external review bypass, human go/no-go replacement, or automatic implementation. S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`.

- S5-C-IMPL-5 implementation-ticket definition now records `docs/S5C_IMPL5_CASE_LIFECYCLE_ACTION_REQUEST_IMPLEMENTATION_TICKET.md` as the governed docs-only S5-C-IMPL-5 implementation-ticket definition baseline. Predecessor baseline was `S5C-SCOPED-IMPLEMENTATION-DECISION-2026-04-16-001`. Claude Code review verdict was `SAFE`, with `HIGH` none, `MEDIUM` none, and `LOW` none. `INFO-1` was intentionally not fixed because S5C1 and S5C2 are subsumed by `docs/S5C5_CASE_WORKFLOW_REVIEW_PASS.md`, which is listed, the governance chain is fully traceable, and risk is zero. Full gate must be run and PASS before commit. `requires_external_review=true`; external review does not authorize implementation and does not replace human go/no-go. The artifact defines only a future separate S5-C-IMPL-5 code/test ticket boundary and does not authorize implementation now. Preliminary recommendation: `PRELIMINARY_RECOMMENDATION_READY_FOR_SEPARATE_S5_C_IMPL5_CODE_AND_TEST_TICKET`. This means it permits a later separate tightly scoped S5-C-IMPL-5 code/test ticket draft for case lifecycle and action-request semantics. That later ticket must still define exact governed clauses, exact files, exact behavior changes, exact tests/test commands, acceptance criteria, audit history expectations, evidence non-retention boundary, redaction/secret handling check, rollback/HOLD criteria, `requires_external_review` decision/rationale, human go/no-go, and implementor/reviewer assignments. The later ticket may still HOLD if exact scope is not clean enough. Candidate future file scope is limited to `backend/app/tools/persistent_case.py`, `backend/app/tools/case_store.py`, `backend/app/runtime_service.py`, `backend/tests/test_case_action_request_contract.py`, `backend/tests/test_case_lifecycle_regression.py`, `backend/tests/test_case_store.py`, and `backend/tests/test_runtime_service.py`. Candidate governed clause scope is limited to durable case lifecycle, case close reason/lifecycle semantics, action request approval, action request denial/rejection, action request cancellation/withdrawal, audit history/decision traceability, and closed-case safety/immutability boundary. Public close-case HTTP endpoint remains `HOLD_PUBLIC_ENDPOINT` / `KEEP_DEFERRED`. Pending action request close-case endpoint behavior remains endpoint-dependent HOLD and does not open endpoint work. Evidence/audit note retention and redaction/secret handling remain `HOLD_POLICY_OR_EXTERNAL_REVIEW`. Destructive response automation, RBAC/ticketing/workflow engine integration, S5-B reopen, S5-D reopen, S4-A resolver behavior, and AI_COLLAB changes remain `HOLD_OUT_OF_SCOPE`. External pilot/customer evidence behavior remains `HOLD_EXTERNAL_INPUT`. External Input Tracker remains governed and does not make any external input `READY`. This closeout does not authorize code changes, test changes, runtime/API/schema/dependency changes, fixture creation/modification, S5-C runtime work by itself, public close-case endpoint work, weakening `KEEP_DEFERRED`, destructive response automation, RBAC/ticketing/workflow engine integration, new persisted lifecycle/action-request statuses, evidence retention/storage/replay/deletion/expiry/evidence-pack behavior, redaction policy freeze, secrets handling implementation, external pilot execution/readiness, real customer/operator sign-off, real external-system access, credentials/tokens/API keys/auth headers/cookies/secret material, raw logs/screenshots/exports/payloads/event bodies/customer/operator evidence, S5-B reopen, S5-D reopen, S4-A resolver changes, AI_COLLAB changes, external review bypass, human go/no-go replacement, or automatic implementation. S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`.

- S5-C-IMPL-5 code/test implementation ticket now records `docs/S5C_IMPL5_CASE_LIFECYCLE_ACTION_REQUEST_CODE_TEST_TICKET.md` as the governed docs-only code/test implementation ticket baseline. It does not authorize implementation; any later implementation requires external review as required, explicit human GO, scoped files, review, full gate, and closeout. Public close-case endpoint remains `KEEP_DEFERRED`; S5-B/S5-D remain `PASS_AND_PARK`; ORDIV-L1A remains parked; external pilot inputs remain `NOT_READY` / `UNKNOWN`; external pilot execution remains unauthorized.
- S5-C-IMPL-5 implementation closeout now records `docs/S5C_IMPL5_CASE_LIFECYCLE_ACTION_REQUEST_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout for the prior implementation. It records the reviewed implementation baseline and full gate PASS for that stage only. It does not authorize additional implementation, public endpoint work, external pilot readiness/execution, S5-B/S5-D reopen, ORDIV report/CSV/L1B work, S4-A resolver changes, AI_COLLAB changes, or further staging/commit/push without explicit human authorization.
- Sprint 5 post-S5-C-IMPL-5 route decision now records `docs/S5_POST_S5C_IMPL5_ROUTE_DECISION.md` as the governed docs-only route decision baseline. Its route selection is non-authorizing and requires separate human product/governance decision before any next scoped ticket. It preserves ORDIV-L1A parked state, S5-B/S5-D `PASS_AND_PARK`, public close-case endpoint `KEEP_DEFERRED`, external pilot inputs `NOT_READY` / `UNKNOWN`, and external pilot execution unauthorized.
- Sprint 5 post-S5-C-IMPL-5 mainline route-selection checkpoint now records `docs/S5_POST_S5C_IMPL5_MAINLINE_ROUTE_SELECTION.md` as the governed docs-only mainline route-selection baseline. The practical selected outcome is `PARK_AND_WAIT_FOR_PRODUCT_INPUT`; it does not authorize implementation, external pilot readiness, public endpoint work, real-data/report/CSV/L1B work, dependency changes, evidence retention, redaction policy freeze, secrets handling, staging, commit, or push.
- Governance context model stage now records `docs/GOVERNANCE_CONTEXT_MODEL.md`, `docs/PRODUCT_STATE.md`, `docs/GOVERNANCE_DECISION_LOG.md`, and `docs/ROADMAP_AND_PARKED_ITEMS.md` as governed context/memory documents. These rolling product maps summarize governed truth but do not authorize implementation, override source governed docs, reopen parked routes, claim readiness, or replace scoped tickets, review, human GO, full gate, and closeout.
- Autonomous vacation operating model stage now records `docs/AUTONOMOUS_VACATION_OPERATING_MODEL.md`, `docs/AUTONOMOUS_AUTHORIZATION_POLICY.md`, `docs/DELEGATED_APPROVER_CHARTER.md`, `docs/AUTONOMOUS_DELIVERY_PIPELINE.md`, `docs/L3_CUSTOMER_TRIAL_LAUNCH_CRITICAL_PATH.md`, and `docs/AUTONOMOUS_HOLD_QUEUE.md` as docs-only autonomous/L3 acceleration governance artifacts. It defines an authorization framework only and does not activate Red authority, customer launch, external pilot execution, production deployment, credentials, real-data handling, public endpoint activation, S5-B/S5-D reopen, ORDIV work, S4-A resolver changes, or AI_COLLAB changes.
- Autonomous policy activation-prep stage records delegated approver details for `jarvis, technical lead`, the authorization window `2026-04-18 00:00 Asia/Shanghai` to `2026-05-06 23:59 Asia/Shanghai`, target L3 planning deadline, allowed Red-1/Red-2 preparation boundaries, Red-3 prohibitions, and the network slow retry rule. The policy remains pending external review and approver identity/accountability confirmation in that stage and does not authorize ACTIVE status, Red execution, launch, deployment, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, or AI_COLLAB change.
- Autonomous policy external review and approver confirmation stage now records `docs/AUTONOMOUS_POLICY_EXTERNAL_REVIEW_AND_APPROVER_CONFIRMATION.md` as the docs-only review/confirmation artifact. External governance/security review returned `PASS_WITH_CONDITIONS`; MEDIUM-1 startup guardrail is incorporated; human confirms `jarvis, technical lead` is an accountable human approver for the authorization window and not an AI/system alias/automation account/non-human proxy. AHQ-015 and AHQ-016 are closed, AHQ-018 remains HOLD, and policy state advances only to `ACTIVATION_READY_PENDING_FINAL_HUMAN_GO`. This stage does not mark the policy `ACTIVE` or authorize Red execution, launch, deployment, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, staging, commit, or push.
- Autonomous policy final activation stage now records `docs/AUTONOMOUS_POLICY_FINAL_ACTIVATION.md` as the docs-only final activation artifact. Human product/governance supplied `FINAL_HUMAN_GO`, policy state advances to `ACTIVE` only during the authorization window, `delegation_expires` remains `2026-05-06 23:59 Asia/Shanghai`, and AHQ-018 is `CLOSED_BY_FINAL_HUMAN_GO`. AHQ-003 through AHQ-014 remain HOLD, AHQ-017 remains `HOLD_IF_AMBIGUOUS`, and Red-3 remains never AI-self-authorized or delegable by normal Red-1/Red-2 approval. This stage does not authorize launch execution, external pilot execution, production deployment, credential handling, real-data handling, public endpoint activation, S5-B/S5-D reopen, ORDIV reopen/report/CSV/L1B work, Red-3 action, legal/commercial commitment, public GA, customer/operator sign-off, evidence deletion, S4-A resolver change, AI_COLLAB change, staging, commit, or push.
- Autonomous operation startup stage now records `docs/AUTONOMOUS_OPERATION_STARTUP.md` as the docs-only autonomous operating loop startup artifact. Default cadence is every 1 hour after the anti-overengineering template refresh. Green/Yellow docs-only stages may be drafted and closeout-gated when lane and authorization are clear, but staging, commit, and push remain governed by exact closeout rules. Every run must read `docs/DELEGATED_APPROVER_CHARTER.md`, verify `delegation_expires`, load the eight core governance docs, confirm manifest baseline and git cleanliness, select one allowed item, classify lane, and proceed only when authorization is clear. This stage does not authorize launch execution, external pilot execution, production deployment, credential handling, real-data handling, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, unsafe full gate, release packaging, staging, commit, or push.
- Autonomous toolchain integration stage now records `docs/AUTONOMOUS_TOOLCHAIN_INTEGRATION.md`, `docs/AUTONOMOUS_TOOL_CAPABILITY_MATRIX.md`, and `docs/AUTONOMOUS_TOOL_RUNBOOK.md` as docs-only toolchain governance artifacts. It defines the four-tool loop across Codex automation, VS Code workspace, Claude Code through the user-configured `cc switch` API tool, and Claude Web running in the user's AdsPower browser/profile. Codex, VS Code CLI, and Codex CLI are locally visible; Claude Code access is through `cc switch`, while `claude.exe` is not usable and must not be treated as the automation path. AdsPower Local API is reachable but requires API-key authentication; no key is used or recorded by this stage. Claude Code non-interactive review behavior, AdsPower/browser control, and Claude Web automation are not verified. AHQ-020 through AHQ-022 block full four-tool automation claims. This stage does not execute the full toolchain, launch browsers or AdsPower profiles, log into Claude Web, handle credentials/sessions/API keys, run full gate, package, stage, commit, push, launch, deploy, execute external pilot, reopen S5-B/S5-D or ORDIV, perform Red-3 action, change S4-A resolver order, or change AI_COLLAB.
- AdsPower Claude Web automation verification stage now records `docs/ADSPOWER_CLAUDE_WEB_AUTOMATION_VERIFICATION.md` and `docs/ADSPOWER_CLAUDE_WEB_RUNBOOK.md` as docs-only verification artifacts. AdsPower Local API accepted user-level environment-variable API-key provisioning without printing or recording the key, one already-active AdsPower profile and one already-open Claude Web page were found, and a harmless prompt round trip returned the expected non-secret token. AHQ-021 advances only to `VERIFIED_FOR_REVIEW_PROMPT_TEST_ONLY`; AHQ-022 remains `PARTIAL_VERIFIED_ACTIVE_PROFILE_ONLY`; AHQ-023 is `CLOSED_BY_USER_ENV_AUTH_OK`; AHQ-020 remains `HOLD_FOR_TOOL_VERIFICATION`. This stage does not authorize API-key disclosure, AdsPower profile launch/switch, login automation, cookie/session/token/auth-header inspection, high-risk review substitution, Red execution, launch, deployment, external pilot execution, real-data handling, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, full gate/package, staging, commit, or push.
- Autonomous ops loop refresh and Day-1 closeout stage now records `docs/AUTONOMOUS_OPS_LOOP_REFRESH_AND_DAY1_CLOSEOUT.md` as the docs-only ops loop refresh artifact. The duplicate autonomous ops loop card is removed, one active loop remains, the later anti-overengineering refresh updates active cadence to every 1 hour, per-run read set expands to include toolchain and AdsPower Claude Web verification/runbook docs, and AHQ-019 is conditionally superseded only for standing Green docs-only closeout after this stage itself closes with review PASS, full gate PASS, release verification PASS, closeout commit, and push. This stage does not authorize Yellow implementation, Red execution, full four-tool automation, AdsPower profile launch/switch, Claude Web login, cookie/session/token/auth-header inspection, launch, deployment, external pilot execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, S4-A resolver change, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, AI_COLLAB change, or unsafe staging/commit/push.
- CC Switch Claude Code review automation verification stage now records `docs/CC_SWITCH_CLAUDE_CODE_REVIEW_AUTOMATION_VERIFICATION.md` and `docs/CC_SWITCH_CLAUDE_CODE_RUNBOOK.md` as docs-only verification/runbook artifacts. No local `cc` command or alias was discoverable, `where.exe cc` found no match, and `claude.exe` visibility is not accepted as the governed `cc switch` API path. No harmless non-interactive review-only request was submitted, no parseable review result was returned, and AHQ-020 remains `HOLD_FOR_TOOL_VERIFICATION`. AHQ-021 remains `VERIFIED_FOR_REVIEW_PROMPT_TEST_ONLY`; AHQ-022 remains `PARTIAL_VERIFIED_ACTIVE_PROFILE_ONLY`. This stage does not authorize edits, implementation, code/test/dependency/fixture/runtime/API/schema/release-script/contract/AI_COLLAB changes, staging, commit, push, Red execution, launch, deploy, external pilot execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, AdsPower profile launch/switch, Claude Web login, cookie/session/token/auth-header inspection, or full four-tool automation.
- VS Code role and toolchain orchestration stage now records `docs/VSCODE_ROLE_AND_TOOLCHAIN_ORCHESTRATION.md` as the docs-only role clarification baseline. VS Code is the local workspace/editing execution surface only; Codex remains orchestration, governance loading, lane classification, review routing, authorized gate/package/release verification, manifest update, and closeout owner under policy limits. This stage does not authorize implementation, code/test changes, browser login/session access, AdsPower profile launch/switch, Red execution, launch, deployment, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, AI_COLLAB changes, full gate/package, staging, commit, or push.
- AdsPower profile launch verification stage now records `docs/ADSPOWER_PROFILE_LAUNCH_VERIFICATION.md` and `docs/ADSPOWER_PROFILE_LAUNCH_RUNBOOK.md` as the configured-profile launch/attach baseline for Claude Web review-prompt readiness. It verifies only the configured profile launch/attach path and does not authorize profile creation, profile switching, login automation, cookie/session/token/auth-header inspection, browser storage/profile-file inspection, reading Claude conversation history, Yellow implementation, Red execution, launch, deployment, external pilot execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, AI_COLLAB changes, full gate/package, staging, commit, or push.
- CC switch command-path provisioning stage now records `docs/CC_SWITCH_COMMAND_PATH_PROVISIONING.md` and `docs/CC_SWITCH_REVIEW_ONLY_INVOCATION_RUNBOOK.md` as the bounded Claude Code review-only verdict-line path. AHQ-020 advances to `VERIFIED_REVIEW_ONLY_VERDICT_LINE_WITH_LIMITS`, enabling only a limited four-tool Green/docs-only review loop. This stage does not authorize Claude Code file edits, file-read review beyond supplied prompt material unless separately governed, command execution, tests, staging, commit, push, strict JSON-schema reliance, Yellow implementation, Red execution, launch, deployment, external pilot execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, AI_COLLAB changes, full autonomous implementation, or replacing required human/delegated/Claude Web/external review.
- Next product development route-selection stage now records `docs/NEXT_PRODUCT_DEVELOPMENT_ROUTE_SELECTION.md` as the governed docs-only route-selection artifact. It selects `OPEN_S5C_NEXT_SCOPED_IMPLEMENTATION_TICKET_PREP` as the next autonomously advanceable product-development task. The selected route is Green docs-only ticket prep for a possible later S5-C scoped implementation ticket; it does not authorize implementation, code/test/dependency/fixture/runtime/API/schema/release-script/contract changes, AI_COLLAB changes, Yellow implementation, Red execution, launch, deploy, external pilot readiness/execution, credentials, real data, evidence retention, redaction policy freeze, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, Red-3 action, AdsPower profile creation/switching, Claude Web login automation, Claude Code file edits/command execution/tests, staging, commit, or push.
- S5-C next scoped implementation ticket-prep stage now records `docs/S5C_NEXT_SCOPED_IMPLEMENTATION_TICKET_PREP.md` as the governed docs-only ticket-prep artifact. It selects `OPEN_S5C_IMPL6_CLOSE_REASON_INTERNAL_SEMANTICS_TICKET` as a future candidate only: internal S5-C close reason taxonomy validation and synthetic tests. It does not authorize implementation, code/test/dependency/fixture/runtime/API/schema/release-script/contract changes, AI_COLLAB changes, Yellow implementation, Red execution, launch, deploy, external pilot readiness/execution, credentials, real data, evidence retention, redaction policy freeze, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, Red-3 action, AdsPower profile creation/switching, Claude Web login automation, Claude Code file edits/command execution/tests, staging, commit, or push.
- S5-C-IMPL-6 close reason internal semantics ticket stage now records `docs/S5C_IMPL6_CLOSE_REASON_INTERNAL_SEMANTICS_TICKET.md` as the governed docs-only scoped implementation ticket. It defines a later Yellow implementation scope for internal S5-C close reason taxonomy validation in `backend/app/tools/persistent_case.py` plus synthetic tests in `backend/tests/test_case_lifecycle_regression.py`, `backend/tests/test_case_store.py`, and optionally `backend/tests/test_case_action_request_contract.py` only if the later implementation ticket or explicit human GO records that unresolved action-request closure interactions are touched. The allowed file list is a boundary, not implementation authorization. Importing, wiring, or exposing the helper through `backend/app/runtime_service.py`, `backend/app/main.py`, or any runtime/API path is out of scope and requires a separate governed route. For this S5-C-IMPL-6 manifest draft, `releases\release_manifest.json` path fields follow the existing Windows source-of-truth manifest convention for this repository and do not authorize changing release tooling semantics. It preserves public close-case endpoint `KEEP_DEFERRED`, S5-B/S5-D parked, ORDIV-L1A parked, external pilot unauthorized, S4-A resolver order, and AI_COLLAB unchanged. It does not authorize implementation, code/test/dependency/fixture/runtime/API/schema/release-script/contract changes, Yellow implementation, Red execution, launch, deploy, external pilot readiness/execution, credentials, real data, evidence retention, redaction policy freeze, public endpoint work, parked-stream reopen, Red-3 action, AdsPower profile creation/switching, Claude Web login automation, Claude Code file edits/command execution/tests, staging, commit, or push.
- S5-C-IMPL-6 implementation closeout stage now records `docs/S5C_IMPL6_CLOSE_REASON_INTERNAL_SEMANTICS_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout artifact. The bounded Yellow implementation adds internal close reason taxonomy validation, `close_persistent_case()`, and close reason audit `details` in `backend/app/tools/persistent_case.py`, with synthetic tests in `backend/tests/test_case_lifecycle_regression.py` and `backend/tests/test_case_store.py`. `backend/tests/test_case_action_request_contract.py` was not edited because unresolved action-request closure interactions were not touched, but it passed as a regression test. Claude Code review-only returned `PASS`; targeted tests, full structured unittest, and full closeout gate/package/release verification passed. This stage does not touch `backend/app/runtime_service.py`, `backend/app/main.py`, public endpoint/API/schema/runtime behavior, fixtures, dependencies, release scripts, contracts, AI_COLLAB, real data, credentials, evidence retention, S5-B/S5-D, ORDIV, Red-3, staging, commit, or push.
- Next product development route-selection stage after S5-C-IMPL-6 now records `docs/NEXT_PRODUCT_DEVELOPMENT_ROUTE_SELECTION.md` as the governed docs-only route-selection artifact. It selects `OPEN_S5C_IMPL7_CASE_REVIEW_SURFACE_TICKET_PREP` as the next autonomously advanceable product-development task. The selected route is Green docs-only ticket prep for a possible later S5-C case review surface/read-model hardening ticket using existing governed data. The `IMPL7` label is a planning/ticket sequence label only and is not implementation authorization; a separate Yellow implementation review gate and explicit GO are required before any code/test changes. Baseline release sha256 remains `6568e236b2dc098ec4f52b8965db9f7090b8049d050db4d5edbdddc3bf7ad379` until this stage creates and verifies its own release artifact. Remaining non-automated areas are recorded as HOLD boundaries rather than blockers to governed Green/Yellow development. This stage does not authorize implementation, code/test/dependency/fixture/runtime/API/schema/release-script/contract changes, Yellow implementation, Red execution, launch, deploy, external pilot readiness/execution, credentials, real data, evidence retention, redaction policy freeze, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, Red-3 action, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, Claude Code file edits/command execution/tests, staging, commit, or push.
- S5-C-IMPL-7 case review surface ticket-prep stage now records `docs/S5C_IMPL7_CASE_REVIEW_SURFACE_TICKET_PREP.md` as the governed docs-only ticket-prep artifact. It prepares a possible later Yellow implementation ticket for internal case review surface/read-model hardening using existing `build_case_view()` data only. Candidate future files are `backend\app\agents\case_view.py` and `backend\tests\test_case_view.py`; `backend\tests\test_runtime_service.py` remains optional only if the later ticket explicitly protects existing runtime payload assertions. `backend\app\runtime_service.py`, `backend\app\main.py`, public endpoint/API/schema files, fixtures, dependencies, release scripts, contracts, AI_COLLAB, real data, credentials, and runtime behavior remain excluded. This stage does not authorize implementation, code/test changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, Red-3 action, staging, commit, or push.
- S5-C-IMPL-7 case review surface implementation ticket now records `docs/S5C_IMPL7_CASE_REVIEW_SURFACE_IMPLEMENTATION_TICKET.md` as the governed scoped implementation ticket. It freezes a later Yellow implementation limited to `backend\app\agents\case_view.py` and `backend\tests\test_case_view.py`, adding bounded internal review guidance under `analysis_limits["review_guidance"]` while preserving existing top-level case view panels and keeping `execution_authorized: False`. `backend\tests\test_runtime_service.py`, `backend\app\runtime_service.py`, `backend\app\main.py`, public endpoint/API/schema files, `backend\app\tools\persistent_case.py`, fixtures, dependencies, release scripts, contracts, AI_COLLAB, real data, credentials, and runtime behavior remain excluded. Human product/governance supplied Yellow implementation GO in the route prompt, constrained by the ticket and effective only after ticket closeout PASS. This stage does not authorize launch, deployment, external pilot readiness/execution, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, Red-3 action, staging, commit, or push outside governed closeout.
- S5-C-IMPL-7 case review surface implementation closeout now records `docs/S5C_IMPL7_CASE_REVIEW_SURFACE_IMPLEMENTATION_CLOSEOUT.md` as the governed Yellow implementation closeout artifact. The implementation adds bounded internal review guidance under `analysis_limits["review_guidance"]` in `backend\app\agents\case_view.py` and synthetic tests in `backend\tests\test_case_view.py`, preserving existing top-level panels, `CASE_VIEW_SCHEMA_VERSION`, runtime/API/schema behavior, public endpoint deferral, persistence, fixtures, dependencies, release scripts, contracts, real-data, credentials, and AI_COLLAB. Claude Code review-only final verdict is `PASS`, targeted case-view tests passed, and full gate/package/release verification passed. This stage does not authorize additional implementation, excluded files, launch, deployment, external pilot readiness/execution, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, Red-3 action, staging, commit, or push without separate explicit authority.

- Next product development route selection after S5-C-IMPL-7 now records `docs/NEXT_PRODUCT_DEVELOPMENT_ROUTE_SELECTION.md` as the governed Green docs-only route-selection artifact. The selected next route is `OPEN_POST_S5C_IMPL7_ROUTE_DECISION_STAGE`, which should decide whether to open S5-C stream review/refresh, prepare a precise S5-C-IMPL8 ticket, refresh external input tracking, or park/wait. A new conversation window is not required for this stage; if a later new window is opened, it must reload the manifest, HANDOFF, rolling maps, delegated approver charter, and core governance docs before action. This stage does not authorize implementation, code/test/dependency/fixture/runtime/API/schema/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, evidence retention, redaction policy freeze, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, Red-3 action, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, Claude Code file edits/command execution/tests, staging, commit, or push.

- Post S5-C-IMPL-7 route decision now records `docs/S5_POST_S5C_IMPL7_ROUTE_DECISION.md` as the governed Green docs-only route-decision artifact. The selected next route is `OPEN_S5C_STREAM_REVIEW_REFRESH_STAGE`, which should review S5-C planning plus S5-C-IMPL-5, S5-C-IMPL-6, and S5-C-IMPL-7 closeouts before naming any S5-C-IMPL8 gap, autonomous Yellow backlog preauthorization package, external input tracker refresh, or stream park/complete outcome. Vacation-mode posture is explicit: Green docs-only stages may proceed and close under AHQ-019 when review, full gate, release verification, manifest PASS, exact staged scope, and no-HOLD conditions pass; Yellow implementation still requires exact scoped ticket/GO or later governed preauthorization; Red, launch, deployment, external pilot execution, real data, credentials, public endpoint activation, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, and AI_COLLAB changes remain unavailable to unattended automation. This stage does not authorize implementation, code/test/dependency/fixture/runtime/API/schema/release-script/contract changes, Yellow implementation, Red execution, AdsPower profile creation/switching, Claude Web login automation, Claude Code file edits/command execution/tests, staging, commit, or push.

- S5-C stream review refresh now records `docs/S5C_STREAM_REVIEW_REFRESH.md` and `docs/S5C_AUTONOMOUS_YELLOW_BACKLOG_PREAUTHORIZATION.md` as governed docs-only artifacts. The stream decision is `S5_C_STREAM_REFRESH_PASS_WITH_YELLOW_BACKLOG_PREAUTH`; S5-C remains coherent after IMPL5/6/7 and may continue through four exact Yellow backlog items after this stage closes PASS: internal workflow summary helper, audit event vocabulary guard, pending action-request boundary helpers, and case-view review-guidance regression hardening. `S5C-YB-04` is test-only; any need to edit `backend\app\agents\case_view.py` must HOLD for a later separate scoped route. Each future Yellow item must run one at a time, match item-specific files and tests, pass Claude Code review-only, targeted tests, full gate, release verification, manifest PASS, exact staged scope, and no-HOLD checks before closeout commit/push. This stage does not authorize code/test implementation during the docs-only refresh, unlisted implementation, runtime/API/schema behavior, public endpoint work, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.

- S5-C-IMPL-8 internal workflow summary closeout now records `docs/S5C_IMPL8_INTERNAL_WORKFLOW_SUMMARY_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout artifact for preauthorized Yellow backlog item `S5C-YB-01`. The implementation adds read-only `persistent_case_workflow_summary(record)` in `backend\app\tools\persistent_case.py` with synthetic lifecycle and store round-trip tests in `backend\tests\test_case_lifecycle_regression.py` and `backend\tests\test_case_store.py`. The helper is derived only from `PersistentCaseRecord`, keeps `execution_authorized: False`, includes `close_reason` only from existing lifecycle audit `details`, and does not mutate records or add persisted/API/runtime/schema fields. Claude Code review-only final verdict is `PASS`, and targeted tests passed before full closeout gate. This stage does not authorize unlisted implementation, runtime/API/schema behavior, public endpoint work, dependency or fixture changes, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.

- S5-C-IMPL-9 audit event vocabulary guard closeout now records `docs\S5C_IMPL9_AUDIT_EVENT_VOCABULARY_GUARD_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout artifact for preauthorized Yellow backlog item `S5C-YB-02`. The implementation adds `GOVERNED_AUDIT_EVENT_TYPES`, `governed_audit_event_types()`, and `_require_audit_event_type()` in `backend\app\tools\persistent_case.py`, validates audit event types during audit-entry deserialization and persistent-case serialization validation, and adds synthetic contract/store tests in `backend\tests\test_case_action_request_contract.py` and `backend\tests\test_case_store.py`. Claude Code review-only final verdict is `PASS`, and targeted tests passed before full closeout gate. This stage does not authorize new audit event types, migrations, backfills, unlisted implementation, runtime/API/schema behavior, public endpoint work, dependency or fixture changes, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.

- S5-C-IMPL-10 pending action-request boundary helpers closeout now records `docs\S5C_IMPL10_PENDING_ACTION_REQUEST_BOUNDARY_HELPERS_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout artifact for preauthorized Yellow backlog item `S5C-YB-03`. The implementation adds read-only `pending_action_request_ids(record)` and `has_pending_action_requests(record)` in `backend\app\tools\persistent_case.py`, keeps pending defined exactly as `pending_approval`, aligns workflow summary pending count to the helper, and adds synthetic contract/lifecycle tests in `backend\tests\test_case_action_request_contract.py` and `backend\tests\test_case_lifecycle_regression.py`. Claude Code review-only final verdict is `PASS`, and targeted tests passed before full closeout gate. This stage does not authorize endpoint behavior, runtime/API/schema behavior, public endpoint work, action execution, RBAC, ticketing, workflow-engine, external-system or destructive-response semantics, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.
- S5-C-IMPL-11 case-view review-guidance regression hardening closeout now records `docs\S5C_IMPL11_CASE_VIEW_REVIEW_GUIDANCE_REGRESSION_HARDENING_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout artifact for preauthorized Yellow backlog item `S5C-YB-04`. The implementation is test-only in `backend\tests\test_case_view.py`, adding synthetic regression coverage for no-chain complete cases, low-confidence cases, degraded cases with suggested action, partial evidence-gap cases, top-level panel preservation, and `analysis_limits["review_guidance"]` location preservation. Claude Code review-only final verdict is `PASS_WITH_FINDINGS` with no blocking finding, and targeted case-view tests passed before full closeout gate. The next recommended autonomous route is `OPEN_AUTONOMOUS_YELLOW_BACKLOG_TEMPLATE_ANTI_OVERENGINEERING_REFRESH_STAGE`. This stage does not authorize production code changes, runtime/API/schema behavior, public endpoint work, persistent case changes, dependency or fixture changes, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.
- Autonomous Yellow backlog template anti-overengineering refresh now records `docs\AUTONOMOUS_YELLOW_BACKLOG_TEMPLATE_ANTI_OVERENGINEERING_REFRESH.md` and `docs\AUTONOMOUS_YELLOW_BACKLOG_PREAUTHORIZATION_TEMPLATE.md` as governed Green docs-only artifacts. It aligns the active autonomous loop cadence to every 1 hour, records safe review fallback from local Claude Code process-spawn failure to the verified AdsPower Claude Web review-prompt path, and requires future Yellow backlog preauthorization packages to carry exact-file, exact-test, anti-overengineering, max-change-budget, review-checklist, and HOLD rules. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, evidence retention, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.
- Current phase summary refresh now records `docs\CURRENT_PHASE_SUMMARY_REFRESH.md` and `docs\SECUPILOT_PHASE_SUMMARY_20260421.md` as governed Green docs-only artifacts. The summary supersedes the stale untracked root `SecuPilot_阶段性总结_20260409.md` for orientation, records completed autonomous governance/toolchain work, closed S5-C Yellow backlog items, current autonomous capabilities, remaining HOLDs, automation risks and controls, and recommends `OPEN_NEXT_PRODUCT_DEVELOPMENT_ROUTE_SELECTION_STAGE` as the next route. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, evidence retention, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.
- Next product development route selection now records `docs\NEXT_PRODUCT_DEVELOPMENT_ROUTE_SELECTION.md` as the governed Green docs-only route-selection artifact. It selects `OPEN_NEXT_YELLOW_BACKLOG_PREAUTHORIZATION_STAGE` as the next bounded product-development route, using `docs\AUTONOMOUS_YELLOW_BACKLOG_PREAUTHORIZATION_TEMPLATE.md` to prepare exact future Yellow items or park if no exact item is ready. SWE agent is recorded as `NOT_INSTALLED_OR_NOT_VERIFIED`; it may become a future bounded implementation accelerator only after separate `OPEN_SWE_AGENT_CAPABILITY_VERIFICATION_STAGE`. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, SWE agent installation/execution/integration, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, evidence retention, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.
- Next Yellow backlog preauthorization now records `docs\NEXT_YELLOW_BACKLOG_PREAUTHORIZATION_STAGE.md` and `docs\NEXT_YELLOW_BACKLOG_PREAUTHORIZATION.md` as governed Green docs-only artifacts. The package prepares three exact future S5-C Yellow items: `S5C-YB-05` reopen lifecycle audit regression in `backend\tests\test_case_lifecycle_regression.py`, `S5C-YB-06` action-request terminal guard regression in `backend\tests\test_case_action_request_contract.py` with `backend\app\tools\persistent_case.py` allowed only for a bounded helper-enforcement bug, and `S5C-YB-07` workflow summary immutability regression in `backend\tests\test_case_lifecycle_regression.py`. The next default route after closeout PASS is `OPEN_S5C_PREAUTHORIZED_YELLOW_BACKLOG_ITEM_05`. The package carries anti-overengineering rules, exact files/tests/HOLD conditions, and keeps SWE agent `NOT_INSTALLED_OR_NOT_VERIFIED`. This stage does not authorize implementation during the docs-only package stage, unlisted Yellow items, unlisted files, broad refactors, SWE agent installation/execution/integration, runtime/API/schema behavior, public endpoint work, dependency or fixture changes, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push outside exact item closeout rules.
- SWE agent capability verification now records `docs\SWE_AGENT_CAPABILITY_VERIFICATION.md` and `docs\SWE_AGENT_RUNBOOK.md` as governed Green docs-only artifacts. Local discovery found no SWE agent command, pip package, or Python module, so SWE agent remains `HOLD_FOR_TOOL_INSTALL_AND_VERIFICATION` under AHQ-024. The future unblock route is `OPEN_SWE_AGENT_INSTALL_AND_CAPABILITY_VERIFICATION_STAGE`, which must prove repo-root confinement, exact file-scope confinement, harmless dry run, no secret/real-data access, no dependency install without approval, no staging/commit/push, before/after git status, and external review before SWE agent can be used as a bounded implementation accelerator. This stage does not authorize SWE agent installation, execution, Yellow backlog participation, implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, review replacement, route selection, manifest/gate/release ownership, staging, commit, push, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or cookie/session/token/auth-header inspection.
- SWE agent install and capability verification now records `docs\SWE_AGENT_INSTALL_AND_CAPABILITY_VERIFICATION.md` and updates `docs\SWE_AGENT_RUNBOOK.md` as governed Green docs-only artifacts. Official tool selection is `mini-swe-agent`; version `2.2.8` is present in user Python site-packages, `mini-extra` help and package import work, but the main `mini` / `mini-swe-agent` entrypoint fails in the current Codex non-interactive Windows shell with `NoConsoleScreenBufferError`. No harmless no-write agent dry run, repo-root confinement, exact file-scope confinement, mutation control, or Yellow backlog compatibility was proven. AHQ-024 is now `PARTIAL_VERIFIED_INSTALL_HELP_ONLY_EXECUTION_HOLD`, and the future route is `OPEN_MINI_SWE_AGENT_NONINTERACTIVE_DRY_RUN_VERIFICATION_STAGE`. This stage does not authorize SWE agent execution, Yellow backlog participation, implementation assistance, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, review replacement, route selection, manifest/gate/release ownership, staging, commit, push, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or cookie/session/token/auth-header inspection.
- Mini SWE agent noninteractive dry-run verification now records `docs\MINI_SWE_AGENT_NONINTERACTIVE_DRY_RUN_VERIFICATION.md` as a governed Green docs-only artifact and updates `docs\SWE_AGENT_RUNBOOK.md`. WSL2 is present only as `docker-desktop`, not a governed user distro; the probed WSL shell lacks `bash`, has empty `PATH`, and exposes no usable Python or mini-swe-agent; `winpty` is unavailable; `cmd.exe /c` still fails with `NoConsoleScreenBufferError`; no harmless no-write main-agent dry run was completed. AHQ-024 is now `HOLD_WSL_PTY_DRY_RUN_NOT_AVAILABLE`, and the future route is `OPEN_MINI_SWE_AGENT_PTY_RUNNER_PROVISIONING_STAGE`. This stage does not authorize SWE agent execution, Yellow backlog participation, implementation assistance, WSL distro installation, PTY runner installation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, review replacement, route selection, manifest/gate/release ownership, staging, commit, push, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or cookie/session/token/auth-header inspection.
- Mini SWE agent PTY runner provisioning now records `docs\MINI_SWE_AGENT_PTY_RUNNER_PROVISIONING.md` as a governed Green docs-only artifact and updates `docs\SWE_AGENT_RUNBOOK.md`. WSL2 is installed and online distro listing can see `Ubuntu-24.04`, but only `docker-desktop` is installed locally; `winpty` is unavailable; `winget.exe`, `wt.exe`, and `conhost.exe` visibility is evidence only and does not authorize system changes. The selected future path is human-controlled `Ubuntu-24.04` WSL2 provisioning, or equivalent governed PTY runner provisioning, followed by `OPEN_MINI_SWE_AGENT_WSL2_NO_WRITE_DRY_RUN_VERIFICATION_STAGE`. AHQ-024 is now `HOLD_PENDING_HUMAN_WSL2_USER_DISTRO_PROVISIONING`. Because a new PRD is expected soon, new Yellow implementation should pause after this tools stage unless product/governance explicitly continues from existing YB-05. This stage does not authorize SWE agent execution, Yellow backlog participation, implementation assistance, WSL distro installation by Codex, PTY runner installation by Codex, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, review replacement, route selection, manifest/gate/release ownership, staging, commit, push, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, AdsPower profile creation/switching, Claude Web login automation, or cookie/session/token/auth-header inspection.
- S5-C-IMPL-12 reopen lifecycle audit closeout now records `docs\S5C_IMPL12_REOPEN_LIFECYCLE_AUDIT_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout artifact for preauthorized Yellow backlog item `S5C-YB-05`. The implementation is test-only in `backend\tests\test_case_lifecycle_regression.py`, adding synthetic close/reopen lifecycle audit coverage and proving the reopened record's final `case_reopened` audit plus the previous close audit `details["close_reason"]` preservation. Claude Code review-only returned `PASS_WITH_FINDINGS`, the LOW finding was fixed, focused re-review returned `PASS`, and targeted lifecycle regression tests passed before full closeout gate. The next route after closeout is `OPEN_MINI_SWE_AGENT_WSL2_NO_WRITE_DRY_RUN_VERIFICATION_STAGE`; `S5C-YB-06` remains available later but is not selected immediately. This stage does not authorize additional implementation, files outside `backend\tests\test_case_lifecycle_regression.py`, production code changes, runtime/API/schema behavior, public endpoint work, new lifecycle status, new audit event type, new close reason, dependency or fixture changes, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3, S4-A resolver changes, AI_COLLAB changes, SWE agent execution, AdsPower profile creation/switching, Claude Web login automation, or Claude Code file edits/command execution/tests/staging/commit/push.

## Working Rules
1. Claude and Codex must both read `releases/release_manifest.json` before reviewing or changing anything.
2. Claude must not treat any file outside `D:\产品设计\New folder` as latest code.
3. Codex must make all runnable code changes inside `D:\产品设计\New folder`.
4. Any external zip must first be unpacked under `incoming/<name>_<date>/`.
5. External material becomes valid code only after it is copied into the source-of-truth root and the manifest is updated.
6. A release is valid only if it is created from `scripts/package_release.py`.
7. A release is accepted only if `scripts/verify_release.py` passes.
8. Git hooks and commit template must be installed with `py -3 scripts/install_git_workflow.py`.

## Claude Web Workflow
Because Claude Web cannot directly browse your local filesystem like Codex:

1. Run `py -3 scripts/build_claude_review_pack.py`.
2. Upload the generated review pack or its copied files from `releases/claude_review_pack/<snapshot>/`.
3. Always include:
   - `docs/HANDOFF.md`
   - `contracts/AI_COLLAB_CONTRACT.md`
   - `releases/release_manifest.json`
   - `docs/SPRINT3_PRD.md`
   - `docs/SPRINT3_JIRA_BACKLOG.md`
   - `docs/SP3_B1_Case_View_Contract.md`
   - `docs/SP3_B4_Degraded_UX_Contract.md`
   - `docs/S3C0_ADAPTER_CONTRACT.md`
   - `docs/S3C1_SIEM_ADAPTER_BOUNDARY.md`
   - `docs/S3C2_VENDOR_PROFILE_HARDENING.md`
   - `docs/S3C3_VENDOR_REPLAY_SPEC.md`
   - `docs/S3D_ENGINEERING_HARDENING_PRD.md`
   - `docs/S3D2_WRAPPER_RETIREMENT_PLAN.md`
   - `docs/S3D3_RUNTIME_OPERABILITY_CONTRACT.md`
   - `docs/S3D4_SNAPSHOT_TRANSITION_CHECKLIST.md`
   - `docs/S3D13_GOVERNANCE_REVIEW_PASS.md`
   - `docs/S3D_JIRA_BACKLOG.md`
   - `docs/SPRINT4_PRD.md`
   - `docs/SPRINT4_JIRA_BACKLOG.md`
   - `docs/S4A1_STATIC_DATA_SOURCE_CONTRACT.md`
   - `docs/S4A2_STATIC_DATA_ADAPTER_BASELINE.md`
   - `docs/S4A3_HOST_IDENTITY_RESOLVER.md`
   - `docs/S4A4_STATIC_DATA_CACHE_AND_TESTS.md`
   - `docs/S4B1_EDR_ADAPTER_CONTRACT.md`
   - `docs/S4B2_PRODUCTION_EDR_INGESTION_BASELINE.md`
   - `docs/S4B3_EDR_REPLAY_FIXTURES.md`
   - `docs/S4B4_T3_PRODUCTION_PARITY_TESTS.md`
   - `docs/S4B5_TELEMETRY_REVIEW_PASS.md`
   - `docs/S4C1_PERSISTENT_CASE_SCHEMA_FREEZE.md`
   - `docs/S4C2_CASE_STORE_AND_RETRIEVAL_API.md`
   - `docs/S4C3_ACTION_REQUEST_AND_APPROVAL_CONTRACT.md`
   - `docs/S4C4_CASE_LIFECYCLE_REGRESSION_TESTS.md`
   - `docs/S4C5_PRODUCT_REVIEW_PASS.md`
   - `docs/S4D1_ENVIRONMENT_AND_SECRET_PROFILE_FREEZE.md`
   - `docs/S4A5_SOURCE_INTEGRATION_REVIEW_PASS.md`
   - `docs/S4D2_PILOT_SMOKE_PATH.md`
   - `docs/S4D3_OPERATOR_RUNBOOKS_AND_FAILURE_TRIAGE.md`
   - `docs/S4D4_PILOT_VALIDATION_GATE.md`
   - `docs/S4D5_PILOT_READINESS_REVIEW.md`
   - `docs/S4D5_PILOT_READINESS_REVIEW_PASS.md`
   - `docs/S4_SPRINT4_PILOT_BASELINE_REVIEW.md`
   - `docs/S4_SPRINT4_PILOT_BASELINE_REVIEW_PASS.md`
   - `docs/SPRINT5_DISCOVERY_BRIEF.md`
   - `docs/SPRINT5_PRD.md`
   - `docs/SPRINT5_JIRA_BACKLOG.md`
   - `docs/S5A1_PILOT_PREPARATION_CHECKLIST.md`
   - `docs/S5A2_DRY_RUN_EVIDENCE_TEMPLATE.md`
   - `docs/S5A3_PILOT_RUN_LOG_REDACTION_BOUNDARY.md`
   - `docs/S5A4_PILOT_SIGN_OFF_CHECKLIST.md`
   - `docs/S5A5_DRY_RUN_EXTERNAL_PILOT_BOUNDARY_CLOSEOUT.md`
   - `docs/S5A_REVIEW_PASS.md`
   - `docs/S5_EXTERNAL_PILOT_DECISION_CHECKPOINT.md`
   - `docs/S5E1_AI_COLLAB_OPERATING_MODEL_REVIEW.md`
   - `docs/S5E2_AI_COLLAB_LIGHT_REVISION_CLOSEOUT.md`
   - `docs/S5E3_AI_COLLAB_GOVERNANCE_DECISION.md`
   - `docs/AI_COLLAB_OPERATING_MODEL.md`
   - `docs/S5_MIDPOINT_DECISION.md`
   - `docs/S5_EXTERNAL_PILOT_INPUT_INTAKE.md`
   - `docs/S5_EXTERNAL_PILOT_INPUT_ASSESSMENT.md`
   - `docs/S5C_CASE_WORKFLOW_HARDENING_PLAN.md`
   - `docs/S5C1_CASE_WORKFLOW_JOURNEY_CONTRACT.md`
   - `docs/S5C2_CLOSE_REASON_AND_LIFECYCLE_SEMANTICS.md`
   - `docs/S5C3_ACTION_REQUEST_APPROVAL_DENIAL_CONTRACT.md`
   - `docs/S5C4_PUBLIC_CLOSE_CASE_HTTP_ENDPOINT_DECISION.md`
   - `docs/S5C5_CASE_WORKFLOW_REVIEW_PASS.md`
   - `docs/S5C_IMPLEMENTATION_DECISION_CHECKPOINT.md`
   - `docs/S5C_IMPL1_DOC_TEST_ALIGNMENT.md`
   - `docs/S5C_IMPL4_TEST_HARDENING_REVIEW_PASS.md`
   - `docs/S5_STREAM_REVIEW_CHECKPOINT.md`
   - `docs/AI_COLLAB_OPERATING_MODEL_AMENDMENT_DECISION.md`
   - `docs/S5_NEXT_ROUTE_AFTER_AI_COLLAB_AMENDMENT.md`
   - `docs/S5B_BOUNDED_SOURCE_INPUT_DISCOVERY.md`
   - `docs/S5B1_SOURCE_INPUT_GAP_MATRIX.md`
   - `docs/S5B2_SYNTHETIC_SOURCE_FIXTURE_SHAPE.md`
   - `docs/S5B3_SOURCE_IDENTIFIER_MAPPING_DECISION.md`
   - `docs/S5B4_SOURCE_FRESHNESS_PROVENANCE_CONTRACT.md`
   - `docs/S5B5_SOURCE_INPUT_DISCOVERY_REVIEW_PASS.md`
   - `docs/S5_ROUTE_AFTER_S5B_DISCOVERY_CLOSEOUT.md`
   - the exact code files under review
4. In the prompt, state the snapshot ID and tell Claude not to use any other zip or folder as truth.
5. Ask Claude to review or design, not to become the source of code truth.
6. If Claude proposes code changes, apply them back into `D:\产品设计\New folder` and update the manifest.

## Baseline Test Commands

### Canonical Authoritative Gate
- `py -3 -m unittest -q backend.tests.test_t3_hunt backend.tests.test_secupilot_drafts backend.tests.test_runtime_service backend.tests.test_case_view backend.tests.test_siem_adapter_contract backend.tests.test_vendor_replay backend.tests.test_static_data_contracts backend.tests.test_static_data_adapters backend.tests.test_host_identity_resolver backend.tests.test_edr_adapter_contract backend.tests.test_edr_replay backend.tests.test_t3_production_parity backend.tests.test_persistent_case_contract backend.tests.test_case_action_request_contract backend.tests.test_case_store backend.tests.test_case_lifecycle_regression`
- `py -3 -m unittest -q backend.tests.test_environment_profile`
- `py -3 -m unittest -q backend.tests.test_persistent_case_contract`
- `py -3 -m unittest -q backend.tests.test_case_action_request_contract`
- `py -3 -m unittest -q backend.tests.test_case_store`
- `py -3 -m unittest -q backend.tests.test_case_lifecycle_regression`
- `py -3 -m unittest -q backend.tests.test_vendor_replay`

### Legacy / Compat Shortcuts
- `py -3 test_t3_hunt.py`
- `py -3 test_secupilot_drafts.py`
- `py -3 selfcheck_t1_t5.py`

The canonical suite above is the authoritative gate. Wrapper commands remain only for compatibility until retirement criteria are met.

## Release Outputs
- Manifest: `releases/release_manifest.json`
- Packages: `releases/*.zip`
- Verification report: `releases/verify_report.json`

## Next Expected Use
- Build a Claude review pack from the current snapshot.
- Review current snapshot with Claude using the manifest and the generated review pack.
- Treat `docs/S4A5_SOURCE_INTEGRATION_REVIEW_PASS.md` plus `docs/S4D2_PILOT_SMOKE_PATH.md` as the governed baseline for any `SP4-D-2` review.
- Treat `docs/S4D3_OPERATOR_RUNBOOKS_AND_FAILURE_TRIAGE.md` plus `docs/RELEASE_PROCESS.md` as the governed operator baseline before extending validation-gate work.
- Treat `docs/S4D1_ENVIRONMENT_AND_SECRET_PROFILE_FREEZE.md` plus `docs/S4D2_PILOT_SMOKE_PATH.md` plus `docs/S4D3_OPERATOR_RUNBOOKS_AND_FAILURE_TRIAGE.md` plus `docs/S4D4_PILOT_VALIDATION_GATE.md` plus `docs/S4D5_PILOT_READINESS_REVIEW.md` plus `docs/S4D5_PILOT_READINESS_REVIEW_PASS.md` as the governed pilot-readiness baseline before any pilot execution or sign-off activity.
- Treat `S4-INTEGRATED-2026-04-13-001` as the governed Sprint 4 integrated pilot baseline before Sprint 5 planning, external pilot preparation, or pilot sign-off activity.
- Treat `S5-PLAN-2026-04-13-001` as the governed Sprint 5 planning baseline before starting `S5-A` or `S5-E` work.
- Treat `docs/S5A1_PILOT_PREPARATION_CHECKLIST.md` as the governed upstream checklist before `S5-A-2` dry-run evidence template work.
- Use `docs/S5_EXTERNAL_PILOT_DECISION_CHECKPOINT.md` to choose the next path: `S5-E-1` lightweight governance review as the default immediate next step while pilot inputs are gathered; external pilot decision package only if all required pilot decision inputs are available; `S5-C` only with dry-run feedback or explicit product decision; `S5-B / S5-D` discovery only if external inputs are available.
- Use `docs/AI_COLLAB_OPERATING_MODEL.md` as governed collaboration workflow guidance for future multi-tool work.
- Product/runtime decisions remain governed by their own PRDs, contracts, tests, and release process.
- Use `docs/S5_MIDPOINT_DECISION.md` to choose the next mainline. No implementation stream should start from chat-only context without a structured ticket and governed baseline check.
- Use `docs/S5_EXTERNAL_PILOT_INPUT_INTAKE.md` to collect or confirm the seven external pilot inputs. If all seven are provided or explicitly accepted, the next step may be an external pilot decision package; if not, continue collecting inputs or choose `S5-C` / `S5-B` / `S5-D` according to governed midpoint criteria.
- Use `docs/S5_EXTERNAL_PILOT_INPUT_ASSESSMENT.md` to collect or explicitly accept the seven external pilot inputs. If all seven become `PROVIDED` or explicitly accepted by product/governance, an external pilot decision package may be drafted; if inputs remain `UNKNOWN` and product explicitly chooses case workflow hardening, proceed to S5-C planning. `S5-B / S5-D` remain discovery-only unless source/telemetry inputs are available.
- Use `docs/S5C_CASE_WORKFLOW_HARDENING_PLAN.md` to draft `S5-C-1 Case Workflow Journey Contract`. Do not implement code, runtime, API, or test changes until a later governed ticket explicitly authorizes them.
- Use `docs/S5C1_CASE_WORKFLOW_JOURNEY_CONTRACT.md` as the governed journey contract before drafting `S5-C-2 Close Reason And Lifecycle Semantics`. No implementation may start from S5-C-1 alone.
- Use `docs/S5C2_CLOSE_REASON_AND_LIFECYCLE_SEMANTICS.md` before drafting `S5-C-3 Action Request Approval / Denial Contract`. No implementation may start from S5-C-2 alone.
- Use `docs/S5C3_ACTION_REQUEST_APPROVAL_DENIAL_CONTRACT.md` as the governed action-request semantics baseline for later S5-C work. No implementation may start from S5-C-3 alone.
- Use `docs/S5C4_PUBLIC_CLOSE_CASE_HTTP_ENDPOINT_DECISION.md` as the governed endpoint decision baseline. S5-C-4 selected `KEEP_DEFERRED`; it does not implement or authorize the public close-case HTTP endpoint.
- Use `docs/S5C5_CASE_WORKFLOW_REVIEW_PASS.md` as the governed S5-C planning/contract stream review pass. S5-C is accepted as a baseline for future scoped implementation tickets only; no implementation, external pilot execution, public close-case endpoint implementation, real SIEM/EDR/source-system access, or real customer/operator sign-off may start from S5-C PASS alone.
- Use `docs/S5C_IMPLEMENTATION_DECISION_CHECKPOINT.md` as the governed S5-C implementation decision route. The selected preliminary route is `S5-C-IMPL-1_DOC_TEST_ALIGNMENT_ONLY`, but it is only a route selection; no implementation may start until a later governed implementation ticket defines exact files, behavior changes, acceptance criteria, test plan, audit/evidence behavior, redaction/secret handling, pending action request behavior if relevant, hold/rollback criteria, external review requirement, and `reviewer_when_cc_implements` if Claude Code is primary implementor.
- Use `docs/S5C_IMPL1_DOC_TEST_ALIGNMENT.md` as the governed doc/test alignment baseline before drafting any scoped S5-C test-hardening ticket. S5-C-IMPL-1 does not authorize code, test, runtime, API, schema, dependency, public close-case endpoint, external pilot, real external-system access, or real sign-off changes.
- Use `backend/tests/test_case_action_request_contract.py` as the governed S5-C action request semantics hardening baseline. S5-C-IMPL-2 remains test-only and does not authorize production code, runtime, API, schema, dependency, public close-case endpoint, external pilot, real external-system access, or real sign-off changes.
- Use `docs/S5C_IMPL4_TEST_HARDENING_REVIEW_PASS.md` as the governed S5-C implementation-preparation / test-hardening mini-stream review pass. Its `PASS` accepts the IMPL-1/2/3 mini-stream baseline only and does not authorize production implementation, additional test edits, runtime/API/schema changes, dependency changes, public close-case endpoint work, external pilot execution, real sign-off, or real external access.
- Use `docs/S5_STREAM_REVIEW_CHECKPOINT.md` as the governed Sprint 5 route-selection checkpoint. The recommended next route is a separate docs-only `AI_COLLAB` amendment decision; the checkpoint itself does not authorize implementation, runtime/API/schema/test/dependency changes, public endpoint work, external pilot execution, real sign-off, real external access, or direct `docs/AI_COLLAB_OPERATING_MODEL.md` edits.
- Use `docs/AI_COLLAB_OPERATING_MODEL_AMENDMENT_DECISION.md` as the governed decision record before any bounded amendment to `docs/AI_COLLAB_OPERATING_MODEL.md`. The decision accepts the nine rules only as candidates; the later amendment ticket must name exact AI_COLLAB sections and wording scope and must not change product/runtime/test/dependency or release-tooling behavior.
- Use the amended `docs/AI_COLLAB_OPERATING_MODEL.md` as governed collaboration workflow guidance for ticket ownership, single-writer lock, Claude Code implementation/review separation, trigger-based external review, integration verifier checklist role, and human final go/no-go. It does not override product/runtime/release contracts or previously governed baselines.
- Use `docs/S5_NEXT_ROUTE_AFTER_AI_COLLAB_AMENDMENT.md` as the governed product-mainline route checkpoint after AI_COLLAB amendment. The preliminary verdict recommends a bounded `S5-B` source/input discovery ticket only; it does not authorize implementation, real source-system access, external pilot execution, S5-C runtime work, public endpoint work, or additional AI_COLLAB changes.
- Use `docs/S5B_BOUNDED_SOURCE_INPUT_DISCOVERY.md` as the governed S5-B source/input discovery baseline before drafting `S5-B-1 Source Input Gap Matrix`. S5-B remains discovery-only, inherits S4-A identity authority, and does not authorize implementation, real source access, live refresh, external authentication, CMDB sync, multi-tenant ownership, external pilot execution, real sign-off, or identity-authority changes.
- Use `docs/S5B1_SOURCE_INPUT_GAP_MATRIX.md` as the governed S5-B source/input gap matrix before drafting `S5-B-2 Synthetic Source Fixture Shape`. S5-B-1 classifies gaps only with `UNKNOWN` / `NEEDS_DECISION` / `DEFERRED` / `NOT_APPLICABLE`; it does not mark any source input as `READY`, `APPROVED`, `IMPLEMENTABLE`, or `PILOT_READY`.
- Use `docs/S5B2_SYNTHETIC_SOURCE_FIXTURE_SHAPE.md` as the governed docs/table-only synthetic fixture shape before drafting `S5-B-3 Source Identifier Mapping Decision`. S5-B-2 does not create fixture files, source adapter changes, source contract freeze, real source access, implementation readiness, external pilot readiness, or S4-A identity authority changes. S5-B-3 must re-evaluate `requires_external_review` and trigger it if identity/source semantics or S4-A authority are frozen or changed.
- Use `docs/S5B3_SOURCE_IDENTIFIER_MAPPING_DECISION.md` as the governed source identifier mapping decision before drafting S5-B-4 or S5-B-4A. Its `READY_FOR_LATER_CONTRACT_DRAFT` decision permits only later non-implementing contract candidate drafting; it does not freeze mapping, approve identity mapping, authorize implementation, validate external evidence, or change S4-A `AssetInventorySnapshot` authority and resolver priority.
- Use `docs/S5B4_SOURCE_FRESHNESS_PROVENANCE_CONTRACT.md` as the governed source freshness/provenance contract candidate before drafting S5-B-5 Discovery Review Pass. Its `READY_FOR_S5_B_5_DISCOVERY_REVIEW_PASS` decision permits stream review only; it does not freeze freshness/provenance semantics, authorize implementation, enable source adapter changes, accept real external evidence, imply live refresh/sync/polling, or change S4-A identity authority. S5-B-5 requires `requires_external_review=true` because stream closeout triggers external review under AI_COLLAB.
- Use `docs/S5B5_SOURCE_INPUT_DISCOVERY_REVIEW_PASS.md` as the governed S5-B source/input discovery stream review pass. Its `PASS_AND_PARK_S5_B_DISCOVERY` verdict parks S5-B as a discovery baseline only and does not authorize implementation, source adapter contract freeze, fixture file changes, runtime/API/schema/test/dependency changes, real source access, live refresh/sync/polling, external pilot readiness, real sign-off, S4-A authority changes, S5-C reopening, or public close-case endpoint work. Future routes require a separate scoped ticket and may include S5-D bounded telemetry discovery, S5-B-4A mapping contract candidate, return to external input collection, or product decision.
- Use `docs/S5_ROUTE_AFTER_S5B_DISCOVERY_CLOSEOUT.md` as the governed route checkpoint after S5-B discovery closeout. The preliminary verdict recommends a separate S5-D bounded telemetry discovery ticket only; it does not authorize implementation, telemetry adapter work, real SIEM/EDR/source/telemetry access, queue/fan-out, performance/SLA commitments, alert routing, response automation, raw telemetry payload retention, external pilot execution, S5-C reopening, or public close-case endpoint work.
- Use `docs/S5D_BOUNDED_TELEMETRY_DISCOVERY.md` as the governed S5-D bounded telemetry discovery baseline before drafting `S5-D-1 Telemetry Gap Matrix`. Its `PROCEED_TO_S5_D_1_TELEMETRY_GAP_MATRIX` recommendation permits only a later docs-only gap matrix artifact and does not authorize implementation, telemetry adapter work, source adapter work, fixture file creation/modification, runtime/API/schema/test/dependency changes, real SIEM/EDR/source/telemetry access, credentials/tokens/API keys, raw telemetry payloads, telemetry event retention without separate evidence-retention/redaction approval, telemetry normalization freeze, timestamp/freshness semantics freeze, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot readiness, S5-C reopening, or public close-case endpoint work.
- Use `docs/S5D1_TELEMETRY_GAP_MATRIX.md` as the governed S5-D-1 telemetry gap matrix baseline before drafting `S5-D-2 Synthetic Telemetry Event Shape`. Its `PROCEED_TO_S5_D_2_SYNTHETIC_TELEMETRY_EVENT_SHAPE` recommendation permits only a later docs-only synthetic telemetry event shape artifact and does not authorize implementation, telemetry adapter work, source adapter work, fixture file creation/modification, runtime/API/schema/test/dependency changes, live SIEM/EDR/source/telemetry access, credentials/tokens/API keys, raw telemetry payloads, real telemetry event retention without separate evidence-retention/redaction approval, telemetry normalization freeze, timestamp/freshness semantics freeze, telemetry provenance/confidence changing trust precedence, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot readiness, S5-C reopening, or public close-case endpoint work.
- Use `docs/S5D2_SYNTHETIC_TELEMETRY_EVENT_SHAPE.md` as the governed S5-D-2 synthetic telemetry event shape baseline before drafting `S5-D-3 Telemetry Provenance And Freshness Boundary`. Its `PROCEED_TO_S5_D_3_TELEMETRY_PROVENANCE_AND_FRESHNESS_BOUNDARY` recommendation permits only a later docs-only provenance/freshness boundary artifact and does not authorize implementation, telemetry adapter work, source adapter work, fixture file creation/modification, runtime/API/schema/test/dependency changes, live SIEM/EDR/source/telemetry access, credentials/tokens/API keys, raw telemetry payloads, real telemetry event retention without separate evidence-retention/redaction approval, telemetry schema freeze, telemetry normalization freeze, timestamp/freshness semantics freeze, telemetry provenance/confidence changing trust precedence, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot readiness, S5-C reopening, public close-case endpoint work, or AI_COLLAB/contract file changes.
- Use `docs/S5D3_TELEMETRY_PROVENANCE_AND_FRESHNESS_BOUNDARY.md` as the governed S5-D-3 telemetry provenance/freshness boundary baseline before drafting `S5-D-4 Redaction Secret And Evidence Retention Boundary`. Its `PROCEED_TO_S5_D_4_REDACTION_SECRET_AND_EVIDENCE_RETENTION_BOUNDARY` recommendation permits only a later docs-only redaction/secret/evidence-retention boundary artifact and does not authorize redaction policy freeze, secrets handling implementation, evidence retention, real telemetry handling, fixture creation/modification, runtime/API/schema/test/dependency changes, external review bypass, S4-A/S5-C boundary changes, live SIEM/EDR/source/telemetry access, credentials/tokens/API keys, raw telemetry payloads, telemetry schema freeze, telemetry normalization freeze, timestamp/freshness semantics freeze, provenance/confidence trust precedence freeze, source authority decisions, queue/fan-out, performance/SLA commitments, alert routing, response automation, external pilot readiness claims, public close-case endpoint work, or AI_COLLAB operating model / contract changes.
- Use `docs/S5D4_REDACTION_SECRET_AND_EVIDENCE_RETENTION_BOUNDARY.md` as the governed S5-D-4 redaction/secret/evidence-retention boundary baseline before choosing the next Sprint 5 product/governance route. Its `ROUTE_DECISION_REQUIRED_AFTER_S5_D_4` recommendation requires a separate route decision and does not authorize redaction policy freeze, secrets handling implementation, evidence retention, retention/deletion/expiry policy, evidence-pack/storage/replay behavior, real telemetry handling, raw payload handling, fixture creation/modification, runtime/API/schema/test/dependency changes, live SIEM/EDR/source/telemetry access, credentials/tokens/API keys/auth headers/cookies collection, S4-A identity authority changes, S5-C reopening, public close-case endpoint work, external pilot readiness or execution, stream/milestone closeout, or external review bypass.
- Use `docs/S5D_ROUTE_DECISION_AFTER_S5D4.md` as the governed route-selection checkpoint after S5-D-4 before opening any next S5-D, external-input, S5-C, or planning route. Its `PRODUCT_GOVERNANCE_ROUTE_DECISION_REQUIRED` verdict requires a later human product/governance decision and does not itself select stream review, select policy-candidate work, close S5-D, trigger/bypass external review, authorize implementation, authorize runtime/API/schema/test/dependency or fixture changes, authorize live telemetry/source access, authorize credentials/raw evidence/evidence retention, freeze redaction/secret/evidence-retention boundaries, change S4-A authority, reopen S5-C, open the public close-case endpoint, or claim external pilot readiness.
- Use `docs/S5D5_TELEMETRY_DISCOVERY_REVIEW_PASS.md` as the governed S5-D stream review pass. Its `PASS_AND_PARK_S5_D_DISCOVERY` verdict parks S5-D as a discovery baseline only and does not authorize implementation, telemetry adapter work, source adapter work, runtime/API/schema/test/dependency changes, fixture creation/modification, live SIEM/EDR/source/telemetry access, credentials/raw evidence/evidence retention, policy freeze, external pilot readiness, S4-A authority changes, S5-C reopening, public close-case endpoint work, external review bypass, or an automatic next route. Future S5-D work requires a separate human product/governance decision and scoped ticket.
- Use `docs/S5_SPRINT5_MILESTONE_REVIEW_PASS.md` as the governed Sprint 5 milestone review pass. Its `SPRINT5_BASELINE_REVIEW_PASS_WITH_EXTERNAL_INPUTS_PENDING` verdict parks Sprint 5 discovery, planning, collaboration-governance, source/input discovery, telemetry discovery, and case workflow planning baselines while preserving that external pilot inputs remain pending / `NOT_READY` / `UNKNOWN`. Future work requires a separate human product/governance decision and scoped ticket; this pass does not authorize external pilot execution, implementation, runtime/API/schema/test/dependency changes, fixtures, real external access, real sign-off, evidence retention, policy/contract freeze, S4-A authority changes, S5-C reopening, public close-case endpoint work, external review bypass, or an automatic next route.
- Use `docs/S5_POST_MILESTONE_ROUTE_DECISION.md` as the governed Sprint 5 post-milestone route decision. Its `RECOMMEND_EXTERNAL_INPUT_TRACKER_AND_S5_C_SCOPED_IMPLEMENTATION_DECISION_PREP` recommendation permits only later bounded docs-only follow-up tickets: an External Input Tracker and S5-C Scoped Implementation Decision Prep. It does not create or start either ticket, does not authorize implementation, does not make any external input `READY`, does not create an external pilot decision package, does not authorize S5-C runtime implementation, keeps the public close-case endpoint `KEEP_DEFERRED`, keeps S5-B and S5-D `PASS_AND_PARK` unless later explicitly reopened, preserves S4-A resolver order, and requires future follow-up tickets to pass separate sanity check, human confirmation, draft, review, and closeout.
- Use `docs/S5_POST_MILESTONE_EXTERNAL_INPUT_TRACKER.md` as the governed external input tracker baseline. It does not make any external input ready and does not open Route B.
- Use `docs/S5C_SCOPED_IMPLEMENTATION_DECISION_PREP.md` as the governed decision-prep baseline before drafting any later S5-C Scoped Implementation Decision. It does not authorize implementation.
- Use `docs/S5C_SCOPED_IMPLEMENTATION_DECISION.md` as the governed S5-C scoped implementation decision baseline before drafting any later implementation ticket. It permits only a later separate tightly scoped implementation ticket draft and does not authorize implementation now.
- Use `docs/S5C_IMPL5_CASE_LIFECYCLE_ACTION_REQUEST_IMPLEMENTATION_TICKET.md` as the governed implementation-ticket definition baseline before drafting any later S5-C-IMPL-5 code/test ticket. It does not authorize implementation now.
- Offline Real Data Integration scoped decision now records `docs/OFFLINE_REAL_DATA_INTEGRATION_SCOPED_DECISION.md` as the governed docs-only ORDIV scoped decision baseline. Baseline input was `S5C-IMPL5-TICKET-DEF-2026-04-16-001` at commit `99ba632bac2be9413a2b9674d1dacf8e9d984c30`. Actual primary implementor was `Codex local (human-authorized in current thread)`, with `Claude Code review-only` as reviewer; this records the execution surface honestly rather than defaulting to VS Code. Claude Code found no `HIGH` or `MEDIUM` blockers. `LOW-1` was fixed by requiring any repo-entry artifact to wait for a later governed ticket, for example `ORDIV-L1A-01`, to be drafted, reviewed, and accepted according to the scoped decision. `LOW-2` was fixed by adding per-row `Must not imply` and `HOLD trigger` safeguards to the candidate field-mapping table and clarifying that candidate target fields do not authorize new schema, persisted fields, adapter-contract fields, or frozen contract changes. `requires_external_review=true` because ORDIV touches offline real security data handling, output-side redaction, evidence-retention questions, future file-adapter behavior, S5-B source authority adjacency, S5-D telemetry normalization adjacency, dependency approval risk, and pilot-readiness adjacency. The recommended route is `RECOMMEND_SCOPED_DECISION` with later first candidate `RECOMMEND_NARROW_L1A_FIRST`. This permits only a later separate governed ORDIV-L1A-01 ticket draft for SIEM alert `.xlsx` ingestion and 14-field mapping validation; it does not authorize implementation now. Raw real `.xlsx`, syslog, device logs, payloads, SQL, IPs, hosts, usernames, IOC values, screenshots, customer/operator evidence, credentials, tokens, API keys, auth headers, cookies, or secret material must remain outside repo and outside AI review material unless output-side redacted as separately authorized. Evidence retention, redaction policy freeze, dependency changes, adapter code, tests, fixtures, local real-data execution, online Metron/Kafka/Solr/Elastic/SIEM/EDR/source/telemetry access, public close-case endpoint work, S5-B reopen, S5-D reopen, S4-A resolver changes, AI_COLLAB changes, external pilot execution/readiness, real sign-off, external review bypass, human go/no-go replacement, and automatic implementation remain unauthorized. S5-B and S5-D remain `PASS_AND_PARK`, public close-case endpoint remains `KEEP_DEFERRED`, external pilot inputs remain `NOT_READY` / `UNKNOWN`, and S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`.
- ORDIV-L1A-01 SIEM alert XLSX mapping ticket definition now records `docs/ORDIV_L1A_01_SIEM_ALERT_XLSX_MAPPING_TICKET.md` as the governed docs-only ORDIV-L1A-01 ticket-definition baseline. Predecessor baseline was `ORDIV-SCOPED-DECISION-2026-04-16-001` at commit `38d61ec`. VS Code / human-supervised workspace created and closes the docs-only draft; Codex planned only; Claude Code reviewed only. Claude Code review found no `HIGH`, `MEDIUM`, or `LOW` blockers, with `INFO` only. The INFO observations are non-blocking: `event_id` generation remains intentionally deferred to a later implementation ticket, the local validation threshold observation does not imply pilot readiness, and gap-reason examples do not freeze cross-cutting API surface. Claude Web escalation is not required now because Claude Code found no `HIGH`/`MEDIUM`/`LOW` blockers and no new contract freeze or frozen contract modification. This is a docs-only ticket-definition closeout, and future implementation remains unauthorized. A later implementation ticket must still be separately drafted, reviewed, externally reviewed as required, and human-approved before any code/test/dependency/runtime/fixture work. Future file scope remains limited to candidate new files `backend/app/tools/siem_alert_file_adapter.py` and `backend/tests/test_siem_alert_file_adapter.py`; this closeout does not authorize creating them. The target shape remains existing SIEM-compatible alert dictionaries only and does not authorize new schema, persisted fields, API fields, runtime contracts, or `SIEMAdapterProtocol` changes. No real `.xlsx`, syslog, device logs, payloads, SQL, IPs, hostnames, usernames, account IDs, IOC values, screenshots, customer/operator evidence, credentials, tokens, API keys, auth headers, cookies, or secret material may enter repo, fixtures, prompts, commits, reports, or review material unredacted. S5-B and S5-D remain `PASS_AND_PARK`. External pilot inputs remain `NOT_READY` / `UNKNOWN`. External pilot execution remains unauthorized. Public close-case endpoint remains `KEEP_DEFERRED`. S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`. AI_COLLAB remains unchanged.
- ORDIV-L1A OpenPyXL dependency decision now records `docs/ORDIV_L1A_OPENPYXL_DEPENDENCY_DECISION.md` as the governed docs-only dependency decision baseline. Predecessor baseline was `ORDIV-L1A01-TICKET-DEF-2026-04-16-001` at commit `6252259d0b9370d95e0ba6623a22dff7e4a100d8`. VS Code / human-supervised workspace performed draft and closeout. Codex role was planning and prompt orchestration. Claude Code role was review-only. Claude Code found no `HIGH`, `MEDIUM`, `LOW`, or `INFO` findings after amendment, and Claude Web is not required now because this decision selects local-only / park behavior and does not add tracked dependencies, modify frozen contracts, change dependency governance, create dependency manifests, change release process, freeze parser contracts, touch real data, or perform stream/milestone closeout. `openpyxl_available=False` remains recorded, so ORDIV-L1A implementation remains HOLD. Selected recommendation is `APPROVE_LOCAL_ENV_OPENPYXL_ONLY`, with fallback `PARK_L1A_UNTIL_OPENPYXL_AVAILABLE`. Tracked dependency addition cannot be activated by chat instruction alone and requires a separate governed tracked-dependency decision route with `requires_external_review=true` and Claude Web or a human-designated qualified external review path before any tracked dependency file work begins. This closeout does not authorize implementation, code, tests, fixtures, dependency file edits, lockfiles, `openpyxl` installation, real-data validation, evidence retention, redaction policy freeze, frozen contract changes, S5-B/S5-D reopen, S4-A resolver changes, public close-case endpoint work, AI_COLLAB changes, external pilot readiness/execution, or push.
- ORDIV-L1A-01 SIEM alert XLSX adapter implementation ticket now records `docs/ORDIV_L1A_01_SIEM_ALERT_XLSX_ADAPTER_IMPLEMENTATION_TICKET.md` as the governed docs-only adapter implementation ticket baseline. Predecessor baseline was `ORDIV-L1A-OPENPYXL-DEPENDENCY-DECISION-2026-04-16-001` at commit `be00f3fa0d22b3addbb2595d40b4db2847c4d961`. VS Code / human-supervised workspace created and closes the docs-only draft. Codex role was planning and prompt orchestration. Claude Code role was review-only. Claude Code found no `HIGH`, `MEDIUM`, `LOW`, or `INFO` findings, and Claude Web is not required now. `requires_external_review=true` applies to any later implementation attempt. Local-only OpenPyXL availability is confirmed: `openpyxl_available=True`, `openpyxl_origin=C:\Users\Administrator\AppData\Roaming\Python\Python314\site-packages\openpyxl\__init__.py`. No tracked dependency was added, and dependency files remain unchanged. Future implementation scope is exactly two NEW files: `backend/app/tools/siem_alert_file_adapter.py` and `backend/tests/test_siem_alert_file_adapter.py`. This closeout still does not authorize implementation now; any code/test implementation requires a later explicit human go/no-go after this closeout. This closeout preserves all boundaries: no `SIEMAdapterProtocol` changes, no runtime/config integration, no fixtures, no real data, no evidence retention, no redaction policy freeze, no S5-B/S5-D reopen, no S4-A resolver change, no public close-case endpoint work, no AI_COLLAB change, and no external pilot readiness/execution.
- ORDIV-L1A SIEM alert XLSX adapter implementation now records the two-file implementation baseline in exactly `backend/app/tools/siem_alert_file_adapter.py` and `backend/tests/test_siem_alert_file_adapter.py`. Predecessor baseline was `ORDIV-L1A01-ADAPTER-IMPL-TICKET-2026-04-16-001` at commit `26d92b466078196640f32196a31b1ddc9ce4c80e`. Human GO was explicitly given before implementation. Claude Web external review gate returned `GO_WITH_CONDITIONS_TO_HUMAN_GO_NO_GO`, and the preserved conditions are satisfied: `gap_reason` uses closed enum values plus safe row index only, and `event_id` is row-index-derived with uniqueness scoped only to a single adapter method call result set. Claude Code implementation review returned `GO` with no `HIGH`, `MEDIUM`, or `LOW` findings; INFO observations about empty `.tmp_testdata` parent directory hygiene and duplicate governed header in a pre-header title row were non-blocking. Claude Web re-escalation is not required. Local-only `openpyxl` was used, no tracked dependency was added, and dependency files remain unchanged. Tests are synthetic-only, generate temporary workbooks, and commit no workbook fixtures. Targeted tests passed: `py -3 -m unittest -q backend.tests.test_siem_alert_file_adapter` and `py -3 -m unittest -q backend.tests.test_siem_adapter_contract`. This implementation does not authorize real-data validation, evidence retention, redaction policy freeze, external pilot readiness/execution, `SIEMAdapterProtocol` changes, runtime/config integration, S5-B/S5-D reopen, S4-A resolver changes, public close-case endpoint work, or AI_COLLAB changes.
- ORDIV-L1A real-data validation scoped decision now records `docs/ORDIV_L1A_REAL_DATA_VALIDATION_SCOPED_DECISION.md` as the governed docs-only real-data validation scoped decision baseline. Predecessor baseline was `ORDIV-L1A01-ADAPTER-IMPL-2026-04-16-001` at commit `2665857702a7c9191a68e041102e6f66c84c80b5`. VS Code / human-supervised workspace created and closes the draft. Codex role was planning and prompt orchestration. Claude Code reviewed only and found no `HIGH`, `MEDIUM`, or `LOW` findings. INFO items were addressed or non-blocking, including inherited-state re-affirmation, logs/commits path scope alignment, and metric candidate caveat. Claude Web is not required before this docs-only closeout. Claude Web is required before any later real-data validation attempt. Preferred later candidate route is `OPEN_LOCAL_REDACTED_VALIDATION_RUN` only after Claude Web external review and explicit human go/no-go, with fallback `PARK_REAL_DATA_VALIDATION`; `START_REAL_DATA_VALIDATION_NOW` remains `NOT_RECOMMENDED` / `HOLD`. This closeout does not authorize opening, listing, reading, parsing, copying, inspecting, summarizing, uploading, retaining, or validating any real workbook. This closeout does not authorize validation reports, evidence retention, redaction policy freeze, real-data handling, external pilot readiness/execution, code/test/dependency changes, runtime/config integration, `SIEMAdapterProtocol` changes, S5-B/S5-D reopen, S4-A resolver changes, public close-case endpoint work, or AI_COLLAB changes. S5-B and S5-D remain `PASS_AND_PARK`. S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`. External pilot inputs remain `NOT_READY` / `UNKNOWN`. External pilot execution remains unauthorized. Public close-case endpoint remains `KEEP_DEFERRED`. AI_COLLAB remains unchanged.
- Sprint 5 post-ORDIV-L1A route decision now records `docs/S5_POST_ORDIV_L1A_ROUTE_DECISION.md` as the governed docs-only Sprint 5 post-ORDIV-L1A route decision baseline. Predecessor baseline was `ORDIV-L1A-REAL-DATA-VALIDATION-SCOPED-DECISION-2026-04-16-001` at commit `a589216bf7dd8689dd955847b5239a0cee2bb65a`. VS Code / human-supervised workspace created and closes the draft. Codex role was planning and prompt orchestration. Claude Code reviewed only and returned `GO` with no `HIGH`, `MEDIUM`, or `LOW` findings. `INFO-1` was addressed by explicitly adding raw filenames/full local paths to the non-authorization list. Claude Web is not required before this docs-only route decision closeout. ORDIV-L1A remains parked at `PARK_LOCAL_VALIDATION_NO_REPORT`. The route recommendation is `RETURN_TO_SPRINT5_MAINLINE_ROUTE_DECISION`, with fallback `PARK_ORDIV_L1A_NO_REPORT_AND_WAIT`. Deferred routes are `OPEN_VALIDATION_REPORT_GOVERNANCE_ROUTE`, `OPEN_CSV_INPUT_FORMAT_ROUTE_DECISION`, and `OPEN_L1B_SYSLOG_ROUTE_DECISION`. `CONTINUE_AD_HOC_REAL_DATA_WORK` remains `NOT_RECOMMENDED` / `HOLD`. This closeout does not authorize report creation, metric recording, further real-data validation, workbook access/listing/opening/parsing/copying/inspection/summarization/upload/retention, CSV processing, L1B/syslog parsing, code/test/dependency/runtime/config/`SIEMAdapterProtocol` changes, evidence retention, redaction policy freeze, external pilot readiness/execution, real customer/operator sign-off, S5-B/S5-D reopen, S4-A resolver changes, public close-case endpoint work, or AI_COLLAB changes. S5-B and S5-D remain `PASS_AND_PARK`. S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`. External pilot inputs remain `NOT_READY` / `UNKNOWN`. External pilot execution remains unauthorized. Public close-case endpoint remains `KEEP_DEFERRED`. AI_COLLAB remains unchanged. Claude Web remains required before any future route involving report governance with real-data output, tracked validation report artifact, CSV input contract freeze, L1B real telemetry/log handling, external pilot readiness claim, frozen contract modification, evidence retention, redaction policy freeze, dependency governance change, or stream/milestone closeout.
- Gate stabilization lineage note: test-only repair commit `0f34bbf3caa7501ef9ec8ae6d0085850b9b53bcd` immediately precedes this route decision closeout and stabilized the EDR replay investigate-window tests in `backend/tests/test_edr_replay.py` only. That repair did not change ORDIV-L1A route substance, did not access real data, and did not modify code, dependencies, fixtures, S5-B/S5-D/S4-A docs, AI_COLLAB files, frozen contracts, or release scripts.
- S5-C-IMPL-5 code/test implementation ticket now records `docs/S5C_IMPL5_CASE_LIFECYCLE_ACTION_REQUEST_CODE_TEST_TICKET.md` as the governed docs-only S5-C-IMPL-5 code/test implementation ticket baseline. Predecessor baseline was `S5-POST-ORDIV-L1A-ROUTE-DECISION-2026-04-16-001` at commit `51543ea9834f54216def4732245d8bba62955c7e`. Route is `OPEN_S5_C_IMPL5_CODE_AND_TEST_IMPLEMENTATION_TICKET`. This is docs-only and non-authorizing. Claude Code formal review returned `GO` with no `HIGH`, `MEDIUM`, or `LOW` findings; `INFO-1` was resolved by adding explicit section 9 prohibitions for CSV processing and L1B syslog/log parsing, and focused re-review returned `GO` with no new findings or regressions. `requires_external_review=true` for later implementation; Claude Web is not required for this docs-only closeout, but Claude Web is required before any later implementation human go/no-go. Human go/no-go remains required before implementation. Candidate future implementation files are `backend/app/tools/persistent_case.py`, `backend/app/tools/case_store.py`, and `backend/app/runtime_service.py`. Candidate future test files are `backend/tests/test_case_action_request_contract.py`, `backend/tests/test_case_lifecycle_regression.py`, `backend/tests/test_case_store.py`, and `backend/tests/test_runtime_service.py`. Public close-case endpoint remains `KEEP_DEFERRED`. S5-B and S5-D remain `PASS_AND_PARK`. S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`. External pilot inputs remain `NOT_READY` / `UNKNOWN`, and external pilot execution remains unauthorized. ORDIV-L1A remains `PARK_LOCAL_VALIDATION_NO_REPORT`. AI_COLLAB remains unchanged. This closeout does not authorize implementation, code/test changes, dependencies, fixtures, real external access, evidence retention, redaction policy freeze, pilot readiness, CSV processing, L1B/syslog parsing, report/metrics work, external review bypass, human go/no-go replacement, or automatic implementation.
- S5-C-IMPL-5 implementation closeout now records `docs/S5C_IMPL5_CASE_LIFECYCLE_ACTION_REQUEST_IMPLEMENTATION_CLOSEOUT.md` as the governed docs-only implementation closeout for implementation commit `42b0dd9`. Predecessor governed baseline was `c8b1e881cc13f91330e4b75d3b98fff454c21fa0`; previous snapshot/stage were `S5C-IMPL5-CODE-TEST-TICKET-2026-04-17-001` / `s5c-impl5-code-test-ticket`. Claude Web external review gate returned `CONDITIONAL PASS` with no blockers; human GO was granted; VS Code / human-supervised implementation completed; Claude Code review-only returned `PASS` with no findings. Targeted tests passed with `Ran 40 tests, OK`, and full gate passed with 142 tests OK plus release verification PASS. Public close-case endpoint remains `KEEP_DEFERRED`; S5-B and S5-D remain `PASS_AND_PARK`; ORDIV-L1A remains `PARK_LOCAL_VALIDATION_NO_REPORT`; external pilot inputs remain `NOT_READY` / `UNKNOWN`; external pilot execution remains unauthorized; S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`; AI_COLLAB remains unchanged. No new public API endpoints, runtime API/schema contracts, dependency changes, fixtures, evidence retention, evidence-pack behavior, real SIEM/EDR/source/telemetry access, CSV processing, or L1B syslog/log parsing are authorized. Role-boundary exception: Codex performed final staging/commit/push for `42b0dd9` without explicit closeout reassignment; do not rewrite pushed history. Going forward, Codex must not stage, commit, or push unless the human explicitly says: `Codex is authorized to stage/commit/push this closeout.` VS Code / human-supervised workspace owns closeout staging, commit, and push by default.
- Sprint 5 post-S5-C-IMPL-5 route decision now records `docs/S5_POST_S5C_IMPL5_ROUTE_DECISION.md` as the governed docs-only route decision baseline after S5-C-IMPL-5 implementation closeout. Predecessor baseline was `S5C-IMPL5-IMPLEMENTATION-CLOSEOUT-2026-04-17-001` at commit `d1164dca8a30ba8a9918cd2c20f74927c98384c5`. Claude Code review-only returned `PASS` with no findings. The selected route is `OPEN_POST_S5C_IMPL5_MAINLINE_ROUTE_SELECTION`, with safe fallback `PARK_AND_WAIT_FOR_PRODUCT_INPUT`. Claude Web is not required for this routine docs-only route decision because it selects only non-authorizing route selection / wait state. ORDIV-L1A remains `PARK_LOCAL_VALIDATION_NO_REPORT`; no ORDIV report, metrics, further real-data validation, workbook access, CSV processing, or L1B syslog/log parsing is authorized. Public close-case endpoint remains `KEEP_DEFERRED`; S5-B and S5-D remain `PASS_AND_PARK`; external pilot inputs remain `NOT_READY` / `UNKNOWN`; external pilot execution remains unauthorized; S4-A resolver order remains `asset_id -> hostname -> fqdn -> ip_address -> aliases`; AI_COLLAB remains unchanged.
- Sprint 5 post-S5-C-IMPL-5 mainline route-selection checkpoint now records `docs/S5_POST_S5C_IMPL5_MAINLINE_ROUTE_SELECTION.md` as the closed governed docs-only mainline route-selection baseline. Predecessor baseline is `S5-POST-S5C-IMPL5-ROUTE-DECISION-2026-04-17-001` at commit `5c2f382f1013774655ea548c893911784a6a3847`. Claude Code review-only returned `PASS` with no `HIGH`, `MEDIUM`, or `LOW` findings and did not edit files. The prior route decision selected `OPEN_POST_S5C_IMPL5_MAINLINE_ROUTE_SELECTION`. Because no explicit new human product/governance input is supplied, the practical selected outcome is `PARK_AND_WAIT_FOR_PRODUCT_INPUT`. This baseline does not authorize implementation, does not claim any external pilot input is `READY`, and does not open S5-B, S5-D, ORDIV report/CSV/L1B, public endpoint, external pilot, stream/milestone closeout, or AI_COLLAB work.
- Governance context model stage now records `docs/GOVERNANCE_CONTEXT_MODEL.md`, `docs/PRODUCT_STATE.md`, `docs/GOVERNANCE_DECISION_LOG.md`, and `docs/ROADMAP_AND_PARKED_ITEMS.md` as docs-only governance memory aids. The stage institutionalizes three context layers: per-stage minimal input, rolling product maps, and periodic synthesis/review. These documents summarize governed truth but do not authorize implementation, change product behavior, reopen parked routes, claim external pilot readiness, or replace source governed docs. Inherited boundaries remain unchanged: ORDIV-L1A `PARK_LOCAL_VALIDATION_NO_REPORT`, S5-B/S5-D `PASS_AND_PARK`, public close-case endpoint `KEEP_DEFERRED`, external pilot inputs `NOT_READY` / `UNKNOWN`, external pilot execution unauthorized, S4-A resolver order `asset_id -> hostname -> fqdn -> ip_address -> aliases`, and AI_COLLAB unchanged.
- Autonomous vacation operating model stage now records six docs-only autonomous/L3 launch acceleration artifacts: `docs/AUTONOMOUS_VACATION_OPERATING_MODEL.md`, `docs/AUTONOMOUS_AUTHORIZATION_POLICY.md`, `docs/DELEGATED_APPROVER_CHARTER.md`, `docs/AUTONOMOUS_DELIVERY_PIPELINE.md`, `docs/L3_CUSTOMER_TRIAL_LAUNCH_CRITICAL_PATH.md`, and `docs/AUTONOMOUS_HOLD_QUEUE.md`. The stage defines an authorization framework only for safe autonomous progress toward L3 Customer Trial Launch acceleration, where L3 means controlled customer-trial launch / private launch candidate, not public GA. Delegated approver and authorization window remain `TBD_DELEGATED_APPROVER` / `TBD_AUTHORIZATION_WINDOW` until explicitly supplied. This stage does not authorize implementation, launch execution, external pilot execution, real-data handling, credential handling, public endpoint activation, production deployment, S5-B/S5-D reopen, ORDIV report/CSV/L1B work, S4-A resolver change, or AI_COLLAB change. Inherited boundaries remain unchanged.
- Autonomous policy activation-prep stage now records delegated approver details for `jarvis, technical lead`, authorization window `2026-04-18 00:00 Asia/Shanghai` to `2026-05-06 23:59 Asia/Shanghai`, target L3 customer trial launch deadline no later than `2026-05-06 23:59 Asia/Shanghai`, approval SLA targets, allowed Red-1/Red-2 preparation boundaries, Red-3 prohibitions, and the network slow retry rule. If `jarvis` is an accountable human technical lead, `jarvis` may serve as delegated approver during the window; if `jarvis` is an AI/system alias, Red-lane approval authority remains `NOT_ACTIVE` / `HOLD`. Activation remains conditional pending required external review and approver identity/accountability confirmation. This stage is docs-only and does not authorize launch execution, external pilot execution, production deployment, real-data handling, credential handling, public endpoint activation, S5-B/S5-D reopen, ORDIV report/CSV/L1B work, S4-A resolver change, or AI_COLLAB change.
- Autonomous policy external review and approver confirmation stage now records `docs/AUTONOMOUS_POLICY_EXTERNAL_REVIEW_AND_APPROVER_CONFIRMATION.md` as the docs-only review/confirmation artifact. External governance/security review returned `PASS_WITH_CONDITIONS`; MEDIUM-1 is incorporated by requiring every autonomous session to re-read `docs/DELEGATED_APPROVER_CHARTER.md` and verify `delegation_expires` has not passed. Human confirms `jarvis, technical lead` is an accountable human approver for the authorization window and is not an AI agent, system alias, automation account, or non-human approval proxy. Policy state advances only to `ACTIVATION_READY_PENDING_FINAL_HUMAN_GO`; it is not ACTIVE. AHQ-015 is closed by the external review with incorporated guardrail, and AHQ-016 is closed by jarvis accountable-human confirmation. A separate final human activation GO remains required. This stage does not authorize launch execution, external pilot execution, production deployment, credential handling, real-data handling, public endpoint work, S5-B/S5-D reopen, ORDIV reopen, Red-3 action, staging, commit, or push.
- Autonomous policy final activation stage now records `docs/AUTONOMOUS_POLICY_FINAL_ACTIVATION.md` as the docs-only final activation artifact. Human product/governance supplied `FINAL_HUMAN_GO`, policy status is `ACTIVE` only during the authorization window `2026-04-18 00:00 Asia/Shanghai` to `2026-05-06 23:59 Asia/Shanghai`, and `delegation_expires` is `2026-05-06 23:59 Asia/Shanghai`. AHQ-018 is `CLOSED_BY_FINAL_HUMAN_GO`; AHQ-003 through AHQ-014 remain HOLD; AHQ-017 remains `HOLD_IF_AMBIGUOUS`. Future autonomous sessions must load the core governance docs, re-read `docs/DELEGATED_APPROVER_CHARTER.md`, and verify `delegation_expires` before autonomous action. This stage does not authorize launch execution, external pilot execution, production deployment, credential handling by AI, real-data handling, public endpoint activation, S5-B/S5-D reopen, ORDIV reopen/report/CSV/L1B work, Red-3 action, legal/commercial commitment, public GA, customer/operator sign-off, evidence deletion, schema/API breaking changes without separate human-level governed approval, S4-A resolver change, AI_COLLAB change, staging, commit, or push.
- Autonomous operation startup stage now records `docs/AUTONOMOUS_OPERATION_STARTUP.md` as the governed docs-only startup baseline for the autonomous operating loop. Default cadence is every 1 hour after the anti-overengineering template refresh, with one allowed item selected per run, lane classification required, and per-run reporting of baseline, selected task, files, checks, HOLDs, and requested human/jarvis action. Green/Yellow docs-only stages may be drafted and closeout-gated when authorized, but staging, commit, and push must follow exact closeout rules. This stage does not authorize launch execution, external pilot execution, production deployment, credential handling by AI, real-data handling, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, unsafe full gate, release packaging, staging, commit, or push.
- Autonomous toolchain integration stage now records `docs/AUTONOMOUS_TOOLCHAIN_INTEGRATION.md`, `docs/AUTONOMOUS_TOOL_CAPABILITY_MATRIX.md`, and `docs/AUTONOMOUS_TOOL_RUNBOOK.md` as the governed docs-only toolchain baseline. Current maturity is L1: local CLI detection for Codex/VS Code/Codex CLI, user-confirmed Claude Code access through the `cc switch` API tool, user-confirmed Claude Web surface in AdsPower, AdsPower Local API reachability with API-key requirement, and manual review prompt transfer. `claude.exe` is not usable and must not be treated as the Claude Code automation path. L2 non-interactive Claude Code review through `cc switch`, L3 AdsPower browser/external review path, L4 fully scheduled closeout, and L5 delegated Red preparation are not claimed. This stage adds AHQ-020, AHQ-021, and AHQ-022 for unverified Claude Code non-interactive review through `cc switch`, AdsPower/Claude Web automation, and AdsPower browser/profile/session control. It does not authorize AdsPower/browser launch, Claude Web login, credentials, sessions, API-key handling, full gate, release packaging, staging, commit, push, launch, deployment, external pilot execution, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, S4-A resolver change, or AI_COLLAB change.
- AdsPower Claude Web automation verification stage now records `docs/ADSPOWER_CLAUDE_WEB_AUTOMATION_VERIFICATION.md` and `docs/ADSPOWER_CLAUDE_WEB_RUNBOOK.md` as the governed AdsPower/Claude Web active-profile review-prompt test baseline. It verifies Local API auth through non-secret user-level environment variable lookup, already-active profile discovery, Claude Web CDP target discovery, input focus, and harmless prompt response detection. It does not verify profile launch, profile switching, login automation, cookie/session/token/auth-header inspection, high-risk review substitution, Red execution, launch, deployment, external pilot execution, real-data handling, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, S4-A resolver change, AI_COLLAB change, full gate/package, staging, commit, or push.
- Autonomous ops loop refresh and Day-1 closeout stage now records `docs/AUTONOMOUS_OPS_LOOP_REFRESH_AND_DAY1_CLOSEOUT.md` as the governed ops-loop refresh baseline. One autonomous ops loop remains active after duplicate removal, and the later anti-overengineering refresh updates active cadence to every 1 hour. Standing Green docs-only closeout may proceed without separate per-closeout human confirmation only after this stage closes with review PASS, full gate PASS, release verification PASS, closeout commit, and push, and only when exact Green docs-only scope, review PASS where required, full gate/package/release PASS, manifest PASS, no HOLD, exact staged files, and inbox reporting conditions are satisfied. It does not authorize Yellow implementation, Red execution, full four-tool automation, AdsPower profile launch/switch, Claude Web login, cookie/session/token/auth-header inspection, `cc switch` non-interactive Claude Code review, launch, deployment, external pilot execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, S4-A resolver change, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, AI_COLLAB change, or unsafe staging/commit/push.
- CC Switch Claude Code review automation verification stage now records `docs/CC_SWITCH_CLAUDE_CODE_REVIEW_AUTOMATION_VERIFICATION.md` and `docs/CC_SWITCH_CLAUDE_CODE_RUNBOOK.md` as the governed cc switch verification baseline. The current workspace cannot verify non-interactive Claude Code review-only prompt transfer because no local `cc` command/API path is discoverable. `claude.exe` visibility is not a substitute. AHQ-020 remains `HOLD_FOR_TOOL_VERIFICATION`; final review of this stage must use Claude Web through the verified AdsPower active-profile path or human-supervised external review. Next recommended route is `PARK_CC_SWITCH_REVIEW_AUTOMATION_VERIFICATION_WAIT_FOR_TOOL_PATH_INPUT`.
- VS Code role and toolchain orchestration stage now records `docs/VSCODE_ROLE_AND_TOOLCHAIN_ORCHESTRATION.md` as the governed docs-only role clarification baseline. VS Code is the local workspace/editing execution surface for exact allowed files only. Codex remains the orchestrator for governance loading, lane classification, review routing, gate/package/release verification when authorized, manifest updates, and governed closeout under policy limits. Claude Web in AdsPower remains the safe review-prompt transfer path, and Claude Code through `cc switch` remains HOLD until a callable non-interactive review-only path is governed and verified. This stage does not authorize code/test implementation, Yellow implementation, Red execution, launch, deployment, external pilot execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, browser login/session access, AdsPower profile launch/switch, full four-tool automation, staging, commit, or push.
- AdsPower profile launch verification stage now records `docs/ADSPOWER_PROFILE_LAUNCH_VERIFICATION.md` and `docs/ADSPOWER_PROFILE_LAUNCH_RUNBOOK.md` as the governed configured-profile launch/attach baseline for Claude Web review-prompt readiness. AdsPower Local API `browser/start` succeeded for the configured profile identifier through local non-secret references, CDP HTTP/WebSocket reachability was confirmed, a `claude.ai` target was found, editable input was present, and no credential prompt was present. No prompt was submitted in this stage, and no API-key value, profile identifier value, conversation text, cookies, sessions, tokens, auth headers, browser storage, or profile files were read or recorded. AHQ-022 may advance to `VERIFIED_PROFILE_LAUNCH_TO_REVIEW_PROMPT_READY_WITH_LIMITS`; AHQ-020 remains HOLD, AHQ-021 remains review-prompt-only, AHQ-003 through AHQ-014 remain HOLD, AHQ-017 remains `HOLD_IF_AMBIGUOUS`, and full four-tool automation remains unclaimed. This stage does not authorize profile creation, profile switching, login automation, Yellow implementation, Red execution, launch, deployment, external pilot execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, AI_COLLAB changes, full gate/package, staging, commit, or push.
- CC switch command-path provisioning stage now records `docs/CC_SWITCH_COMMAND_PATH_PROVISIONING.md` and `docs/CC_SWITCH_REVIEW_ONLY_INVOCATION_RUNBOOK.md` as the governed callable Claude Code review-only path. The verified path is stdin prompt transfer to cc-switch routed npm `claude.cmd` with `--bare`, JSON wrapper output, disabled tools, no session persistence, plan permission mode, budget cap, first-line verdict parsing, no web requests, and unchanged git status. Local `cc` remains unavailable, `claude.exe` remains rejected, non-bare invocation returned stale project context, command-argument multi-line prompts were truncated, and `--json-schema` timed out and is not verified. AHQ-020 may advance to `VERIFIED_REVIEW_ONLY_VERDICT_LINE_WITH_LIMITS`, enabling a limited four-tool Green/docs-only review loop only. This stage does not authorize Claude Code file edits, file-read review beyond supplied prompt material unless separately governed, command execution, tests, staging, commit, push, strict JSON-schema reliance, Yellow implementation, Red execution, launch, deployment, external pilot execution, credentials, real data, public endpoint work, S5-B/S5-D reopen, ORDIV work, AI_COLLAB changes, full autonomous implementation, or replacing required human/delegated/Claude Web/external review.
- Use `docs/NEXT_PRODUCT_DEVELOPMENT_ROUTE_SELECTION.md` as the governed next product-development route-selection artifact. Its selected route is `OPEN_S5C_NEXT_SCOPED_IMPLEMENTATION_TICKET_PREP`, a Green docs-only ticket-prep route for a possible later S5-C scoped implementation ticket. It may inspect governed docs and current code/test reality only as baseline context in the follow-up stage, and it must HOLD if exact future files, behavior, tests, and acceptance criteria cannot be safely named. It does not authorize implementation, code/test/dependency/fixture/runtime/API/schema/release-script/contract changes, AI_COLLAB changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, credentials, real data, evidence retention, redaction policy freeze, public endpoint work, S5-B/S5-D reopen, ORDIV work, S4-A resolver change, Red-3 action, AdsPower profile creation/switching, Claude Web login automation, Claude Code file edits/command execution/tests, staging, commit, or push.
- Use `docs/ORDIV_L1A_01_SIEM_ALERT_XLSX_MAPPING_TICKET.md` as the governed ORDIV-L1A-01 docs-only ticket-definition baseline before drafting any later SIEM alert `.xlsx` adapter implementation ticket. It does not authorize implementation.
- Use `docs/ORDIV_L1A_OPENPYXL_DEPENDENCY_DECISION.md` as the governed ORDIV-L1A OpenPyXL dependency decision baseline before drafting any later ORDIV-L1A implementation ticket. It selects local environment `openpyxl` provisioning only, with parking as fallback, and does not authorize implementation.
- Use `docs/ORDIV_L1A_01_SIEM_ALERT_XLSX_ADAPTER_IMPLEMENTATION_TICKET.md` as the governed ORDIV-L1A-01 adapter implementation ticket baseline before any later adapter code/test implementation. It does not authorize implementation now; a later implementation attempt requires explicit human go/no-go and `requires_external_review=true`.
- Use `backend/app/tools/siem_alert_file_adapter.py` and `backend/tests/test_siem_alert_file_adapter.py` as the governed ORDIV-L1A SIEM alert XLSX adapter implementation baseline. This baseline remains local/synthetic only and does not authorize real-data validation, runtime/config integration, external pilot readiness/execution, or evidence retention.
- Use `docs/ORDIV_L1A_REAL_DATA_VALIDATION_SCOPED_DECISION.md` as the governed ORDIV-L1A real-data validation scoped decision baseline before opening any later local real-data validation route. It does not authorize validation now; Claude Web external review and explicit human go/no-go remain required before any later real-data validation attempt.
- Use `docs/S5_POST_ORDIV_L1A_ROUTE_DECISION.md` as the governed Sprint 5 post-ORDIV-L1A route decision baseline. It parks ORDIV-L1A at `PARK_LOCAL_VALIDATION_NO_REPORT` and returns to Sprint 5 mainline route selection; it does not authorize report creation, metrics, further real-data validation, CSV processing, L1B/syslog work, or implementation changes.
- Use `docs/S5C_IMPL5_CASE_LIFECYCLE_ACTION_REQUEST_CODE_TEST_TICKET.md` as the governed S5-C-IMPL-5 code/test ticket baseline before any later implementation attempt. It does not authorize implementation; Claude Web external review and explicit human go/no-go remain required before any code/test work.
- Use `docs/S5C_IMPL5_CASE_LIFECYCLE_ACTION_REQUEST_IMPLEMENTATION_CLOSEOUT.md` as the governed docs-only S5-C-IMPL-5 implementation closeout. It records implementation commit `42b0dd9`, Claude Web `CONDITIONAL PASS` with no blockers, human GO, VS Code / human-supervised implementation completion, Claude Code review-only `PASS` with no findings, targeted tests `Ran 40 tests, OK`, and full gate `PASS` with 142 tests OK plus release verification PASS. The release manifest aligns the active snapshot/stage to `S5C-IMPL5-IMPLEMENTATION-CLOSEOUT-2026-04-17-001` / `s5c-impl5-implementation-closeout`. This closeout does not authorize additional implementation, code/test/dependency/fixture changes, public endpoint work, external pilot readiness/execution, S5-B/S5-D reopen, ORDIV-L1A report/metrics/CSV/L1B work, S4-A resolver changes, AI_COLLAB changes, further manifest/release-process changes outside governed review and human instruction, staging, commit, or push. Codex performed final staging/commit/push for `42b0dd9` without explicit closeout reassignment; do not rewrite pushed history. Going forward, Codex must not stage, commit, or push unless the human explicitly says: `Codex is authorized to stage/commit/push this closeout.` VS Code / human-supervised workspace owns closeout staging, commit, and push by default.
- Use `docs/S5_POST_S5C_IMPL5_ROUTE_DECISION.md` as the governed Sprint 5 route-selection checkpoint after S5-C-IMPL-5 implementation closeout. Its `OPEN_POST_S5C_IMPL5_MAINLINE_ROUTE_SELECTION` decision is non-authorizing and requires separate human product/governance decision before any next scoped ticket. Claude Web remains required before any later route involving contract freeze or frozen contract change, public endpoint work, real-data validation/report governance, CSV input contract freeze, L1B real telemetry/log handling, evidence retention/redaction policy freeze, dependency governance, external pilot readiness/execution/sign-off, stream/milestone closeout, or S5-B/S5-D reopen with source/telemetry semantics or real evidence.
- Use `docs/S5_POST_S5C_IMPL5_MAINLINE_ROUTE_SELECTION.md` as the governed docs-only mainline route-selection baseline. Its route decision is `PARK_AND_WAIT_FOR_PRODUCT_INPUT` because no new human product/governance input is supplied. It preserves ORDIV-L1A `PARK_LOCAL_VALIDATION_NO_REPORT`, S5-B and S5-D `PASS_AND_PARK`, external pilot inputs `NOT_READY` / `UNKNOWN`, external pilot execution unauthorized, public close-case endpoint `KEEP_DEFERRED`, S4-A resolver order `asset_id -> hostname -> fqdn -> ip_address -> aliases`, and AI_COLLAB unchanged. It does not authorize implementation, external pilot readiness, public endpoint work, real-data/report/CSV/L1B work, dependency changes, fixtures, evidence retention, redaction policy freeze, secrets handling, staging, commit, or push.
- Use `docs/PRODUCT_STATE.md`, `docs/GOVERNANCE_DECISION_LOG.md`, and `docs/ROADMAP_AND_PARKED_ITEMS.md` as governed memory aids for future new-window prompts. Future prompts may cite them for product memory, but must still carry the latest formal baseline, inherited boundaries, allowed files, and stage task. `docs/GOVERNANCE_CONTEXT_MODEL.md` controls the context discipline: chat windows are execution context, rolling maps are summaries only, and implementation still requires a separate scoped ticket, review, human GO, full gate, and closeout.
- Use `docs/CC_SWITCH_COMMAND_PATH_PROVISIONING.md`, `docs/CC_SWITCH_REVIEW_ONLY_INVOCATION_RUNBOOK.md`, `docs/CC_SWITCH_CLAUDE_CODE_REVIEW_AUTOMATION_VERIFICATION.md`, `docs/CC_SWITCH_CLAUDE_CODE_RUNBOOK.md`, `docs/AUTONOMOUS_OPS_LOOP_REFRESH_AND_DAY1_CLOSEOUT.md`, `docs/ADSPOWER_CLAUDE_WEB_AUTOMATION_VERIFICATION.md`, `docs/ADSPOWER_CLAUDE_WEB_RUNBOOK.md`, `docs/ADSPOWER_PROFILE_LAUNCH_VERIFICATION.md`, `docs/ADSPOWER_PROFILE_LAUNCH_RUNBOOK.md`, `docs/AUTONOMOUS_TOOLCHAIN_INTEGRATION.md`, `docs/AUTONOMOUS_TOOL_CAPABILITY_MATRIX.md`, `docs/AUTONOMOUS_TOOL_RUNBOOK.md`, `docs/AUTONOMOUS_POLICY_FINAL_ACTIVATION.md`, `docs/AUTONOMOUS_AUTHORIZATION_POLICY.md`, `docs/DELEGATED_APPROVER_CHARTER.md`, `docs/AUTONOMOUS_DELIVERY_PIPELINE.md`, `docs/L3_CUSTOMER_TRIAL_LAUNCH_CRITICAL_PATH.md`, and `docs/AUTONOMOUS_HOLD_QUEUE.md` as the current autonomous ops-loop, toolchain, AdsPower/Claude Web, and Claude Code review-only verification baseline. Before any autonomous action, the startup prompt must load the core governance docs, and every autonomous session must re-read `docs/DELEGATED_APPROVER_CHARTER.md` and verify `delegation_expires`. AdsPower/Claude Web automation is verified only for safe review-prompt transfer through the configured profile launch/attach path to Claude Web review-prompt readiness. Claude Code review-only automation is verified only for stdin prompt transfer to cc-switch routed `claude.cmd` with `--bare`, JSON wrapper output, disabled tools, no session persistence, plan permission mode, budget cap, first-line verdict parsing, no web requests, and unchanged git status. API keys and profile identifiers must not be pasted into chat, committed to repo, printed in logs, added to prompts, or recorded. Profile creation/switching, login automation, cookie/session/token/auth-header inspection, browser storage/profile-file inspection, high-risk review substitution, Claude Code file edits, file-read review beyond supplied prompt material unless separately governed, command execution, tests, Yellow implementation, Red execution, launch, deployment, external pilot execution, real-data handling, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, full autonomous implementation, full gate/package before review, staging, commit, and push outside the standing Green docs-only rule remain unauthorized without separate governed authority.
- Mini SWE Agent WSL2 no-write dry-run verification now records `docs/MINI_SWE_AGENT_WSL2_NO_WRITE_DRY_RUN_VERIFICATION.md` as the governed toolchain evidence. `SecuPilotUbuntu2404` WSL2 `Ubuntu-24.04` exists, normal user `secupilot` ran `mini-swe-agent` 2.2.8 from `/home/secupilot/.venvs/mini-swe-agent`, and a deterministic no-write main-agent dry run completed from the repo root mapping with one harmless `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`, parseable trajectory output, zero cost, and unchanged git status. AHQ-024 is `VERIFIED_NO_WRITE_DRY_RUN_WITH_LIMITS`. SWE agent may be considered only as a future bounded implementation accelerator after a later exact Yellow item explicitly names it with exact files, tests, max-change budget, model credential non-secret path if needed, independent review, gate, and HOLD rules. This stage does not authorize SWE agent execution against product work, current/past Yellow item integration, model-backed implementation, code/test changes, dependency changes in the repo, review replacement, route selection, manifest/gate/release ownership, staging, commit, push, launch, deployment, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, or AI_COLLAB changes.
- SWE Agent Yellow backlog template integration now records `docs/SWE_AGENT_YELLOW_BACKLOG_TEMPLATE_INTEGRATION.md` as the governed Green docs-only template integration stage. Future Yellow backlog templates may include an optional SWE agent block, but the default per item is `SWE agent use: not authorized for this item`. The only positive role is `bounded implementation accelerator`, and actual use still requires a later exact Yellow item to explicitly name SWE agent with exact repo root, files, behavior, tests, max-change budget, command path/version, model credential non-secret path if needed, output location, independent review, gate, closeout, and HOLD rules. Current and closed Yellow items, including the existing YB-06/YB-07 package, are not retroactively SWE-enabled. This stage does not authorize SWE agent execution against product work, model-backed implementation, review replacement, route selection, manifest/gate/release ownership, staging, commit, push, launch, deployment, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, or AI_COLLAB changes.
- S5-C-IMPL-13 action-request terminal guards closeout now records `docs\S5C_IMPL13_ACTION_REQUEST_TERMINAL_GUARDS_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout artifact for preauthorized Yellow backlog item `S5C-YB-06`. The implementation adds synthetic terminal guard regression tests in `backend\tests\test_case_action_request_contract.py` and makes one bounded production fix in `backend\app\tools\persistent_case.py` so `approve_action_request()` validates action-request transition before case lifecycle transition. `FROZEN_ACTION_REQUEST_TRANSITIONS` and the action-request vocabulary remain unchanged. Claude Code review-only returned `PASS_WITH_FINDINGS`; focused fixes closed the prior MEDIUM and LOW findings; targeted action-request contract tests passed with 10 tests OK. The SWE-enabled YB-07 ticket draft was temporarily stashed before this closeout to prevent release-package contamination and should be restored after this closeout commits/pushes. This stage does not authorize unlisted implementation, runtime/API/schema behavior, public endpoint work, action execution, RBAC, ticketing, workflow-engine, external-system/destructive-response semantics, dependency or fixture changes, release-script or contract changes, AI_COLLAB changes, SWE agent execution, Red execution, launch, deployment, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3 action, or S4-A resolver changes.
- S5-C YB-07 SWE workflow summary immutability ticket now records `docs\S5C_YB07_SWE_WORKFLOW_SUMMARY_IMMUTABILITY_TICKET.md` as the first exact SWE-enabled Yellow item ticket after YB-06 closeout. Future item `S5C-YB-07-SWE` is Yellow test-only, limited to `backend\tests\test_case_lifecycle_regression.py`, and may use SWE agent only as a no-write `bounded implementation accelerator` for patch suggestion / implementation planning. YB-06 is already closed and is not retroactively SWE-enabled. This stage does not authorize implementation during the docs-only ticket stage, production code changes, runtime/API/schema behavior, public endpoint work, direct SWE repo writes, model-backed SWE execution without a later governed non-secret credential path, review replacement, route selection by SWE agent, manifest/gate/release ownership by SWE agent, staging, commit, push, launch, deployment, real data, credentials, S5-B/S5-D reopen, ORDIV work, Red-3 action, or AI_COLLAB changes.
- S5-C-IMPL-14 workflow summary immutability closeout now records `docs\S5C_IMPL14_WORKFLOW_SUMMARY_IMMUTABILITY_IMPLEMENTATION_CLOSEOUT.md` as the governed closeout artifact for exact SWE-enabled Yellow item `S5C-YB-07-SWE`. The implementation is test-only in `backend\tests\test_case_lifecycle_regression.py`, adding one synthetic regression proving a mutated workflow summary and nested `action_request_counts` do not affect a future `persistent_case_workflow_summary()` result and that `execution_authorized` remains `False`. SWE agent was authorized only as a bounded implementation accelerator but was not used for product patch generation or repo writes. Claude Code review-only returned `PASS_WITH_FINDINGS`; the LOW finding was fixed with identity assertions; focused re-review returned `PASS`; targeted lifecycle regression tests passed with 11 tests OK. This stage does not authorize production code changes, runtime/API/schema behavior, public endpoint work, dependency or fixture changes, release-script or contract changes, AI_COLLAB changes, direct SWE repo writes, model-backed SWE execution without later governed non-secret credential path, review replacement, route selection by SWE agent, manifest/gate/release ownership by SWE agent, staging, commit, push, launch, deployment, real data, credentials, S5-B/S5-D reopen, ORDIV work, or Red-3 action.
- Wait for PRD and route selection readiness now records `docs\WAIT_FOR_PRD_AND_ROUTE_SELECTION_READINESS.md` as a governed Green docs-only waiting-stage artifact. Current decision is `WAIT_FOR_LATEST_PRD_OR_EXPLICIT_PRODUCT_DIRECTION`: new product implementation should pause until the latest PRD or explicit product direction arrives. While waiting, autonomous work may only do Green docs-only status summaries, PRD availability monitoring if supplied, route-selection readiness notes, and rolling-map hygiene. When PRD or explicit product direction arrives, the next route should be `OPEN_NEXT_PRODUCT_DEVELOPMENT_ROUTE_SELECTION_STAGE`. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, SWE agent product execution, or AI_COLLAB changes.
- SWE autonomous delivery acceleration baseline now records `docs\SWE_AUTONOMOUS_DELIVERY_ACCELERATION_PLAYBOOK.md`, `docs\AUTONOMOUS_DELIVERY_PROMPT_PACK.md`, and `docs\SWE_AUTONOMOUS_DELIVERY_ACCELERATION_BASELINE_CLOSEOUT.md` as governed Green docs-only acceleration references. The playbook fixes role boundaries: Codex is orchestration / implementation owner / prompt router / verification owner, VS Code is the local workbench, SWE is only a bounded implementation accelerator when a later exact Yellow item names it, Claude Code is focused code review, Claude Web is product / architecture / governance review, and Human/Jarvis owns product direction and authorization. The prompt pack gives reusable bounded prompts for PRD intake, SWE planning, Claude Code review, Claude Web review, Human/Jarvis GO-no-go, and routing records. The anti-generalization contract forbids model-driven helpers, modules, registries, frameworks, service layers, future-proofing, cleanup refactors, hidden requirements, or scope expansion unless explicitly named by the ticket. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, SWE agent product execution without a later exact Yellow item, or AI_COLLAB changes.
- Ticket readiness checklist now records `docs\TICKET_READINESS_CHECKLIST.md` and `docs\TICKET_READINESS_CHECKLIST_CLOSEOUT.md` as governed Green docs-only admission-gate artifacts. After PRD intake and route selection, candidate work must pass this checklist before becoming an exact implementation ticket or using SWE. The checklist requires product source, manifest baseline, route, lane, exact behavior, exact non-goals, exact allowed files, exact tests, review path, full gate, release verification, commit/push authority, HOLD conditions, anti-generalization checks, Red/HOLD trigger checks, and SWE eligibility. SWE remains disabled unless a later exact Yellow item explicitly names it as `bounded implementation accelerator` and the checklist returns `READY_FOR_SWE_ACCELERATION`. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, SWE agent product execution without a later exact Yellow item, or AI_COLLAB changes.
- Autonomous delivery dry-run rehearsal now records `docs\AUTONOMOUS_DELIVERY_DRY_RUN_REHEARSAL.md` and `docs\AUTONOMOUS_DELIVERY_DRY_RUN_REHEARSAL_CLOSEOUT.md` as governed Green docs-only rehearsal artifacts. The rehearsal proves that no latest PRD or explicit product direction keeps the product posture at `WAIT_FOR_LATEST_PRD_OR_EXPLICIT_PRODUCT_DIRECTION`, ticket readiness returns `HOLD_NO_PRODUCT_SOURCE`, SWE remains `SWE_AGENT_USE_NOT_AUTHORIZED`, Claude Code is not applicable without a diff, and prompt copying remains orchestration rather than approval. User-reported Claude Web usage limit is recorded as queued/unavailable until `2026-04-23 01:00 Asia/Shanghai`; it is not architecture/governance PASS, and any later route requiring Claude Web must obtain an actual verdict or remain `NEEDS_CLAUDE_WEB_REVIEW` / `HOLD_PENDING_CLAUDE_WEB_RESET`. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, SWE product execution, Claude Web review claims without an actual verdict, or AI_COLLAB changes.
- Live PRD intake runbook and metrics now record `docs\LIVE_PRD_INTAKE_RUNBOOK.md`, `docs\LIVE_PRD_INTAKE_METRICS_RECORD.md`, and `docs\LIVE_PRD_INTAKE_RUNBOOK_METRICS_CLOSEOUT.md` as governed Green docs-only first-response artifacts for the next real PRD or explicit product direction. A live PRD intake must register source and timestamp, load the current PASS baseline, screen for unsafe data/secrets/session material, summarize goals and non-goals, list changed assumptions and affected areas, draft candidate routes and lane classification, record Claude Web availability, run ticket-readiness precheck, track SWE eligibility, and populate the metrics record before route selection proceeds. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, SWE product execution, Claude Web review claims without an actual verdict, or AI_COLLAB changes.
- PRD intake automation trigger now records `docs\PRD_INTAKE_AUTOMATION_TRIGGER_RULES.md`, `docs\PRD_INTAKE_RUN_TRIGGER.md`, and `docs\PRD_INTAKE_AUTOMATION_TRIGGER_CLOSEOUT.md` as governed Green docs-only trigger artifacts. While the project waits for the latest PRD or explicit product direction, each autonomous ops-loop run should first apply trigger rules; only a positive governed product-source trigger may execute the run trigger, start live intake, and populate metrics. No-trigger runs must keep waiting and must not edit the live metrics record. The trigger key is `source_id + source_timestamp + baseline_snapshot` to prevent duplicate intake runs. This stage does not authorize implementation, code/test/runtime/API/schema/dependency/fixture/release-script/contract changes, Yellow implementation, Red execution, launch, deployment, external pilot readiness/execution, real data, credentials, public endpoint work, S5-B/S5-D reopen, ORDIV work, Red-3 action, SWE product execution, metrics edits without positive trigger and safety screen, Claude Web review claims without an actual verdict, or AI_COLLAB changes.
- S6 G0 pre-start confirmation now records `docs\S6_G0_PRE_START_CONFIRMATION_2026_04_25.md` as the governed automation-owned receipt and scheduling record for G0-01, G0-02, G0-05, G0-06, G0-07, G0-08, and G0-09. Decision is `G0_PRE_START_CONFIRMATION_PASS_WITH_NON_BLOCKING_RATIFICATION_CHECKPOINTS`. Human/Jarvis is not the implementor of these G0 checks; the autonomous delivery flow owns the evidence, while Human/Jarvis retains go/no-go, HOLD, scope-ratification, launch/deploy/pilot/real-data, full backlog import, and stage/commit/push authority. P2 implementation remains gated by `P2_V0_3_LIGHTWEIGHT_RATIFICATION_BEFORE_FIRST_P2_AP_IMPLEMENTATION`; P3 implementation remains gated by `P3_FULL_RATIFICATION_BEFORE_FIRST_P3_MV_IMPLEMENTATION`. Current P1, Storybook, mock fixture, route/handoff, and other bounded mock-only automation may continue under exact ticket checklists.
- Sprint 0 E0-01 launch now records `docs\S6_E0_01_RESOLVED_SURFACE_CONTEXT_TICKET_LAUNCH_CHECKLIST_2026_04_25.md` as the governed exact implementation checklist for `E0-01 — ResolvedSurfaceContext + ContextValidator + SH-08`. Human/Jarvis authorized Codex as primary implementor on execution surface `codex`, with VS Code/local repo as workspace surface, SWE disabled, Claude Code focused review via `claude-cmd`, and Claude Web architecture/governance review required because E0-01 defines root rendering authority and fail-closed validation behavior. Scope is limited to ResolvedSurfaceContext, ContextValidator, SH-08/Security Halt, provider skeleton, and unit/component tests. No page implementation, Storybook cross-surface stories, Playwright E2E, backend/runtime/API/schema, real data, secrets, deploy, external pilot, or stage/commit/push is authorized. `OPEN_S6_SB_C_STORYBOOK_STATIC_CORE_SURFACE_STORIES` is deferred until E0-01 closeout passes or is explicitly accepted by Jarvis. Current source-of-truth stays at GoNoGo v0.2.1, Backlog Tracker v0.4, Visual Kickoff v0.3, and G0 confirmation; the new Sprint 0 checklist v0.2 and E0-01 PR Brief v0.2 are accepted as execution-gate inputs only, not as a rollback to Backlog Tracker v0.2.
- E0-01 implementation is closed as `IMPLEMENTED_GATE_PASS_CLAUDE_WEB_PASS` in commit `aaaa199 Implement Sprint 0 E0-01 surface context`. Implemented files are limited to the new frontend surface context foundation and tests under `frontend\src\secupilot\surface\...`; no existing page, Storybook, Playwright, backend/runtime/API/schema, real-data, secrets, deploy, or external pilot scope was touched. Gates passed: frontend tests 25 OK, frontend build PASS, backend guard 42 OK, `git diff --check` PASS. Claude Code focused review initially returned `NO BLOCKING FINDINGS` with one P2 closeout-before-finish finding; the raw technical payload check was expanded to all surfaces, and Claude Code follow-up returned `NO BLOCKING FINDINGS`. Claude Web architecture/governance review was obtained through governed AdsPower review-prompt transfer and returned `E0-01_DECISION: PASS` with non-blocking informational notes on intentionally fail-closed synthetic-domain detection, future possible hardening of visible `SecurityHaltCode` reason text, and intentional non-P2 `action_mode === null` semantics. This review is evidence only and does not authorize launch, deploy, real data, or external pilot.
- Sprint 0 E0-02 launch checklist now records `docs\S6_E0_02_CORE_SURFACE_MOCK_FIXTURE_ADAPTER_LAUNCH_CHECKLIST_2026_04_25.md` as the governed exact checklist for `E0-02 — Mock Fixture Adapter phase states`. Status is `READY_FOR_JARVIS_IMPLEMENTATION_GO`, not implementation started. Scope is limited to mapping repo-local fixture Phase 0-6 records into `ResolvedSurfaceContext`, validating each phase through `validateResolvedSurfaceContext`, optional minimal app wiring to replace existing app-local context derivation with adapter output while preserving behavior, and focused tests. SWE remains disabled; Claude Code focused review is required after any implementation diff; Claude Web is conditional only if E0-01 authority semantics, validator strictness, product semantics, Storybook/Playwright/backend/API/schema, route handoff, cross-surface propagation, or Go/No-Go mandatory trigger categories are touched. No Jira cloud mutation has been performed; only Jira delta CSV/notes are prepared for Jarvis review.
- Sprint 0 E0-02 implementation is complete and gated as `IMPLEMENTED_GATE_PASS_CLAUDE_CODE_PASS_WITH_NON_BLOCKING_P3_NOTES`, pending Jarvis closeout authorization for stage/commit/push. Implemented files are limited to `frontend\src\secupilot\surface\fixtures\coreSurfaceFixtureAdapter.ts`, `frontend\src\secupilot\surface\fixtures\__tests__\coreSurfaceFixtureAdapter.test.ts`, `frontend\src\App.tsx`, `frontend\src\App.test.tsx`, plus the E0-02 checklist/route/handoff records. The adapter maps fixture Phase 0-6 into `ResolvedSurfaceContext`, normalizes fixture surfaces to governed `P2_APPROVAL` / `P3_MANAGER`, validates every phase through `validateResolvedSurfaceContext`, excludes URL/localStorage/sessionStorage authority, and keeps P3 privileged raw payload out of context and DOM. Gates passed: frontend tests 35 OK, frontend build PASS, backend guard 42 OK, `git diff --check` PASS with line-ending warnings only, fixture SHA256 matches `814F21AACFE2E2B25514990188F9801D81F0ED4A464F7A03B05DD235E4A02B47`, and Claude Code focused review returned `PASS with non-blocking P3 notes`. Claude Web is not required for E0-02 because no authority semantics, validator strictness, product semantics, Storybook, Playwright, backend/runtime/API/schema, route handoff, cross-surface propagation, real-data, secrets, deploy, or external pilot behavior changed. Jira cloud update is authorized but not executed because `JIRA_BASE_URL`, `JIRA_EMAIL`, and `JIRA_API_TOKEN` are not safely available as runtime environment variables; the token provided in chat must not be hard-coded into commands, logs, repo files, or prompts.
- 48h bounded automation is active under Jarvis authorization. Queue order is `E0-03-LAUNCH`, `E0-03-IMPLEMENT`, `E0-04-LAUNCH`, `E0-04-IMPLEMENT`, then bounded docs/Jira hygiene only if no code ticket is safe. E0-03 launch now records `docs\S6_E0_03_STORYBOOK_FIRST_STORY_SET_LAUNCH_CHECKLIST_2026_04_25.md` as `READY_FOR_AUTONOMOUS_IMPLEMENTATION_GO` with bounded interpretation `GO_FOR_STATIC_FIRST_STORY_SET_ONLY`. E0-03 may create static Storybook stories for existing mock-only workbench phases through the E0-02 validated adapter path and may add a minimal `initialPhaseNumber` story hook to `frontend\src\App.tsx`. It must not implement P2 strong-confirm composer, P2 delay/observe/reject workflows, CS-P2-05 concurrency, Playwright, new P1/P2/P3 pages, P3 ratification claims, backend/runtime/API/schema, real data, secrets, deploy, or external pilot behavior.
- Sprint 0 E0-03 implementation is complete and gated as `IMPLEMENTED_GATE_PASS_CLAUDE_CODE_PASS_WITH_NON_BLOCKING_NOTES`. Implemented files are limited to `frontend\.storybook\preview.ts`, `frontend\src\App.tsx`, `frontend\src\App.test.tsx`, `frontend\src\secupilot\surface\storybook\CoreSurfaceStories.stories.tsx`, plus E0-03 checklist/route/handoff records. The stories render the existing mock-only workbench from validated fixture phases through the E0-02 adapter path, add only a minimal `initialPhaseNumber` story hook, and include Phase 0-6 plus a cross-surface overview. Gates passed: frontend tests 36 OK, frontend build PASS, Storybook build PASS, backend guard 42 OK, `git diff --check` PASS with line-ending warnings only, and Claude Code focused review returned `PASS with non-blocking notes`. Claude Web is not required for E0-03 because no authority semantics, validator strictness, P2 workflow, P3 ratification scope, Playwright, backend/runtime/API/schema, route handoff, cross-surface propagation, real-data, secrets, deploy, or external pilot behavior changed. Next authorized route in the 48h queue is E0-04 launch checklist.
- Sprint 0 E0-04 launch now records `docs\S6_E0_04_PLAYWRIGHT_LCP_LCB_LCN_SEED_LAUNCH_CHECKLIST_2026_04_25.md` as `READY_FOR_AUTONOMOUS_IMPLEMENTATION_GO` with bounded interpretation `GO_FOR_PLAYWRIGHT_SEED_ONLY`. Exact dependency scope is limited to `npm install --save-dev @playwright/test` and `npx playwright install chromium`. Exact implementation scope is limited to `test:e2e` script, Playwright config for local Vite app, seed specs for current fixture-driven LC-P/LC-B/LC-N red lines, and minimal `data-testid` attributes only when needed for stable assertions. E0-04 must not implement P2 strong-confirm composer, P2 decision workflows, CS-P2-05 concurrency, route handoff, cross-surface propagation, backend/runtime/API/schema, real data, secrets, launch, deploy, public endpoint, external pilot, or broad abstraction.
- Sprint 0 E0-04 implementation is complete and gated as `IMPLEMENTED_GATE_PASS_CLAUDE_CODE_PASS_WITH_NON_BLOCKING_NOTES`. Implemented files are limited to Playwright dependency/config/test script, LC-P/LC-B/LC-N seed specs under `frontend\tests\e2e\`, minimal `data-testid` additions in `frontend\src\App.tsx`, and E0-04 checklist/route/handoff records. Gates passed: frontend tests 36 OK, frontend build PASS, Storybook build PASS, Playwright seed 5 OK, backend guard 42 OK, and `git diff --check` PASS with line-ending warnings only. Claude Code focused review returned `PASS with non-blocking notes`; notes were accepted as forward-guard and fixture-dependency reminders, not blockers. Claude Web is not required for E0-04 because no authority semantics, validator strictness, product workflow, route handoff, cross-surface propagation, backend/runtime/API/schema, real-data, secrets, deploy, or external pilot behavior changed.
- E0-02B fixture QA expansion reconciliation now records `docs\S6_E0_02B_FIXTURE_QA_EXPANSION_LAUNCH_CHECKLIST_2026_04_27.md` as the governed docs-only launch checklist for the 2026-04-27 E0-02 brief. Reconciliation decision: `2026-04-27 E0-02 brief v0.1 is accepted as post-closeout expansion input; it does not invalidate E0-02/E0-03/E0-04, but gates future fixture QA / cross-surface / boundary expansion.` E0-02 remains closed in `786b579`, E0-03 remains closed in `94ebd21`, and E0-04 remains closed in `2a71408`. E0-02B is listed as `AUTOMATION_CANDIDATE_PENDING_JARVIS_IMPLEMENTATION_GO`, but implementation is not authorized yet; future fixture QA, Phase 07 `CROSS_SURFACE`, boundary-case registry, poison-pill registry, and resolver-degradation expansion require Jarvis GO and a bounded exact implementation pass. This reconciliation does not authorize code, dependency, Storybook, Playwright, backend/runtime/API/schema, real-data, secrets, deploy, public endpoint, external pilot, stage, commit, or push.
- E0-02B fixture QA expansion implementation is complete and pushed as `633cc73 Implement Sprint 0 E0-02B fixture QA expansion`, with closeout state `IMPLEMENTED_GATE_PASS_CLAUDE_CODE_PASS_COMMITTED_PUSHED`. Implemented files are limited to the fixture layer and records: `frontend\src\secupilot\surface\fixtures\fixtureTypes.ts`, `fixtureRegistry.ts`, `mockFixtureAdapter.ts`, `README.md`, `__tests__\mockFixtureAdapter.test.ts`, `__tests__\coreSurfaceFixtureAdapter.test.ts`, plus the E0-02B checklist/route/handoff records. The adapter defaults to validated fixture loading, permits `validate=false` only for poison-pill rejection tests, registers Phase 07 `CROSS_SURFACE`, boundary cases, poison pills, and resolver-degradation fixtures, and preserves `ResolvedSurfaceContext` plus `ContextValidator` as authority. Gates passed: frontend tests 47 OK, frontend build PASS, backend guard 42 OK, and `git diff --check` PASS with line-ending warnings only. Claude Code focused review returned `PASS_WITH_NOTES`; both non-blocking notes were addressed, and follow-up review returned `PASS`. Claude Web is not required for E0-02B because no authority semantics, validator strictness, product workflow, Storybook, Playwright, backend/runtime/API/schema, route handoff, runtime cross-surface propagation, real-data, secrets, deploy, public endpoint, or external pilot behavior changed.
- Jira cloud is synchronized for E0-02B as `SCRUM-19 [E0-02B] Fixture QA Expansion`, parent `SCRUM-14`, status `已完成`, labels `closed,codex,e0,e0-02b,fixture-qa,review-pass,secupilot,sprint0`. Existing E0 cloud issues `SCRUM-14` through `SCRUM-18` were read-only checked and not bulk-mutated.
- Continuation GO after E0-02B push was evaluated as `NO_NEW_CODE_TICKET_STARTED_FROM_GENERAL_GO`: E0-01/E0-02/E0-02B/E0-03/E0-04 are complete, older P1-CD-A/P1-CD-B/S6-MF-A/S6-SB-B pending-stage notes have been synchronized to their pushed commits, and no new code work may start until a next exact bounded ticket is selected with allowed files, tests, review path, HOLD conditions, and stage/commit/push authorization. Bounded docs/Jira hygiene may continue if safe.
- Visual Negative handoff correction review now records `D:\产品设计\secupilot0421\visual negative` as reviewed, including the outer Markdown files and the zip manifest contents. Gap-01 is corrected by explicitly referencing `AI_COLLAB Amendment v0.2` as the Sprint 0 execution-governance authority for execution surface, reviewer floor, single-writer lock, and SWE HOLD-on-expansion behavior. Gap-02 is corrected by adding the E01-N01 review check that missing or unsupported `surface` triggers SH-08, and by confirming E0-01 already implements missing `surface` -> `SH-08_INVALID_CONTEXT_SHAPE`, unsupported `surface` -> `SH-08_UNSUPPORTED_ENUM`, and surface/role mismatch -> `SH-08_SURFACE_ROLE_MISMATCH`. The correction also carries forward three execution details: `validate=false` is allowed only in unit tests that deliberately load poison-pill fixtures and must not be accepted or forwarded by production-like, Storybook, Playwright, or `ResolvedSurfaceContextProvider` fixture paths; Playwright observation-window tests may use `await page.clock.fastForward()` only for read-only timer display checks and must use explicit `emitStateSync` / resolved context input for material state migration; P3 summary tests should reject over-certain copy when `unsupported_claims` exist, including `expect(summaryText).not.toMatch(/完全受控|已彻底消除/i)`. Because E0-03 and E0-04 are already implemented and pushed, this older handoff does not reopen them; any new Storybook/Playwright expansion requires a new exact bounded ticket.
- E0-03B and E0-04B launch checklists now split the visual-negative follow-up path. `docs\S6_E0_03B_STORYBOOK_NEGATIVE_BOUNDARY_STORIES_LAUNCH_CHECKLIST_2026_04_27.md` returns `READY_FOR_AUTONOMOUS_IMPLEMENTATION_GO_WITH_BOUNDS` for Storybook-only registry negative/boundary stories: it may change only `frontend\src\secupilot\surface\storybook\CoreSurfaceStories.stories.tsx` plus closeout records, may display existing validated phase/boundary/resolver-degradation fixture entries, may list poison-pill IDs only as non-renderable fail-closed documentation, and must not accept or forward `validate=false`. `docs\S6_E0_04B_PLAYWRIGHT_LCB_LCN_REDLINE_EXPANSION_LAUNCH_CHECKLIST_2026_04_27.md` returns `HOLD_FOR_IMPLEMENTATION_PENDING_E0_03B_CLOSEOUT_AND_EXACT_RENDERABLE_REDLINE_SCOPE` because deeper LC-B/LC-N redlines require E0-03B closeout plus exact renderable DOM/state-sync scope; Playwright must not invent P2 concurrency, backend `STATE_SYNC`, route handoff, or missing product behavior.
- E0-03B implementation is complete and pushed as `f6d0fed Implement E0-03B Storybook boundary registry stories`, with closeout state `IMPLEMENTED_GATE_PASS_CLAUDE_CODE_PASS_COMMITTED_PUSHED`. Jira cloud is synchronized as `SCRUM-20 [E0-03B] Storybook negative/boundary registry stories`, parent `SCRUM-14`, status `已完成`. The only implementation file changed is `frontend\src\secupilot\surface\storybook\CoreSurfaceStories.stories.tsx`, adding registry Storybook views for validated phases including Phase 07 `CROSS_SURFACE`, boundary cases, resolver-degradation fixtures, and a non-renderable poison-pill inventory. Renderable stories use `mockFixtureAdapter.getFixture(id)` default validation; poison-pill fixtures are listed by ID only and are not loaded or rendered; no `validate=false` path was added. Gates passed: frontend tests 47 OK, frontend build PASS, Storybook build PASS with chunk-size warning only, backend guard 42 OK, and `git diff --check` PASS with line-ending warning only. Claude Code focused review returned `PASS`. E0-03B does not change App/page/components, fixture registry, fixture adapter, validator, Playwright, dependencies, backend/runtime/API/schema, route handoff, cross-surface propagation, real-data, secrets, deploy, public endpoint, or external pilot behavior. E0-04B relaunch/readiness check after E0-03B closeout returns `IMPLEMENTATION_HOLD_CONFIRMED`: the current Playwright config serves the Vite app, not Storybook, and the app still lacks exact renderable DOM/test IDs plus `emitStateSync` / resolved context input for the deeper LC-B/LC-N redlines. E0-04B remains HOLD pending exact renderable redline scope.
- E0-04C app redline renderability launch checklist now records `docs\S6_E0_04C_APP_REDLINE_RENDERABILITY_LAUNCH_CHECKLIST_2026_04_27.md` as the next exact prerequisite ticket before any E0-04B Playwright expansion. Decision is `READY_FOR_AUTONOMOUS_IMPLEMENTATION_GO_WITH_BOUNDS`: E0-04C may add static, read-only, mock-only app renderability for selected existing E0-02B boundary and resolver-degradation fixture IDs, limited to `frontend\src\App.tsx`, `frontend\src\App.test.tsx`, and closeout records. It may expose DOM markers for later Playwright assertions (`missing-signal-notice`, `concurrency-inline-warning`, `resolver-degradation-notice`, and `manager-summary`) using only existing validated fixtures through default `mockFixtureAdapter.getFixture(id)` behavior. It must not add Playwright tests, Storybook changes, new fixtures, fixture registry changes, adapter changes, validator changes, dependencies, `validate=false`, poison-pill rendering, P2 concurrency workflow, observation-window timer/state migration, backend `STATE_SYNC`, route handoff, cross-surface propagation, backend/runtime/API/schema, real data, secrets, deploy, public endpoint, or external pilot behavior. E0-04B remains HOLD until E0-04C closes and a separate relaunch proves exact Playwright assertions.
- E0-04C implementation is complete and pushed as `37362e0 Implement E0-04C app redline renderability hooks`, with closeout state `IMPLEMENTED_GATE_PASS_CLAUDE_CODE_PASS_COMMITTED_PUSHED`. Jira cloud is synchronized as `SCRUM-21 [E0-04C] App redline renderability hooks`, parent `SCRUM-14`, status `已完成`. Implemented files are limited to `frontend\src\App.tsx`, `frontend\src\App.test.tsx`, plus E0-04C closeout route/handoff records. The app now has a mock-only `Mock redline fixture` selector with a closed allowlist of existing E0-02B boundary/resolver-degradation fixtures, loads renderable states through `mockFixtureAdapter.getFixture(id)` default validation, and exposes static read-only DOM markers for `missing-signal-notice`, `concurrency-inline-warning`, `resolver-degradation-notice`, `blast-radius-redline`, and `manager-summary`. Tests prove missing-signal source, stale approve inline-warning non-workflow behavior, L1 blast radius OFF, cautious P3 manager summary without raw host evidence, and poison-pill option exclusion. Gates passed: frontend tests 52 OK, frontend build PASS, Storybook build PASS with chunk-size warning only, backend guard 42 OK, and `git diff --check` PASS with line-ending warning only. Claude Code focused review returned `PASS`. E0-04C does not add Playwright tests, Storybook changes, fixture registry/adapter/validator changes, dependencies, `validate=false`, poison-pill rendering, P2 concurrency workflow, observation-window timer/state migration, backend `STATE_SYNC`, route handoff, cross-surface propagation, backend/runtime/API/schema, real data, secrets, deploy, public endpoint, or external pilot behavior. After E0-04C closeout, the next route may relaunch E0-04B only for static Playwright assertions over these app DOM markers; material state migration and real concurrency behavior remain out of scope.
- E0-04B static redline Playwright relaunch now records `docs\S6_E0_04B_PLAYWRIGHT_LCB_LCN_REDLINE_EXPANSION_LAUNCH_CHECKLIST_2026_04_27.md` as `READY_FOR_AUTONOMOUS_IMPLEMENTATION_GO_WITH_BOUNDS` after E0-04C closeout. The old HOLD is lifted only for static Playwright assertions over E0-04C app DOM markers. Allowed implementation is one focused spec at `frontend\tests\e2e\core-surface.redline-expansion.spec.ts` using the existing Vite app and existing `Mock redline fixture` selector to assert `missing-signal-notice`, `concurrency-inline-warning`, `resolver-degradation-notice`, `blast-radius-redline`, `manager-summary`, and poison-pill option exclusion. App/component/route/fixture/adapter/validator/Storybook/dependency/Playwright config changes remain unauthorized. P2 concurrency workflow, approve/reject/delay/observe composer, material observation-window migration, `emitStateSync`, backend `STATE_SYNC`, backend/runtime/API/schema, real data, secrets, deploy, public endpoint, external pilot, and launch behavior remain out of scope.
- E0-04B implementation is complete and pushed as `f026905 Implement E0-04B static redline Playwright assertions`, with closeout state `IMPLEMENTED_GATE_PASS_CLAUDE_CODE_PASS_COMMITTED_PUSHED`. Jira cloud is synchronized as `SCRUM-22 [E0-04B] Static redline Playwright assertions`, parent `SCRUM-14`, status `已完成`. The only implementation file is `frontend\tests\e2e\core-surface.redline-expansion.spec.ts`, covering static app redline assertions for `missing-signal-notice`, `concurrency-inline-warning`, `resolver-degradation-notice`, `blast-radius-redline`, `manager-summary`, and poison-pill selector exclusion. Gates passed: targeted Playwright 5 OK, frontend tests 52 OK, frontend build PASS, Storybook build PASS with chunk-size warning only, full Playwright 10 OK, backend guard 42 OK, and `git diff --check` PASS. Claude Code first review returned `PASS_WITH_FINDINGS`; the MEDIUM selector-vacuous-pass issue and LOW/INFO hardening notes were fixed in the spec, and focused re-review returned `PASS`. E0-04B does not change App/component/route, fixture registry/adapter/validator, Storybook, dependencies, Playwright config, backend/runtime/API/schema, P2 concurrency workflow, material observation-window migration, `emitStateSync`, backend `STATE_SYNC`, real data, secrets, deploy, public endpoint, external pilot, or launch behavior.
- E0-04D observation-window/state-sync Playwright readiness now records `docs\S6_E0_04D_OBSERVATION_WINDOW_STATE_SYNC_PLAYWRIGHT_READINESS_CHECKLIST_2026_04_27.md` as a launch/readiness-only checklist. Decision is `IMPLEMENTATION_HOLD_PENDING_EXACT_STATE_SYNC_HARNESS`: remaining observation-window/state-sync assertions need an exact `emitStateSync` / resolved-context harness and exact allowed files before implementation. The current queue authorization does not allow implementation for E0-04D. No code, test, config, fixture, dependency, backend/runtime/API/schema, route handoff, Storybook, real-data, secrets, deploy, public endpoint, or external pilot change is authorized by E0-04D. The queued P1/P2/P3 route-readiness step is not started because no exact next ticket and allowed file scope have been selected; P2/P3 ratification gates still apply.
- Use `py -3 scripts/git_preflight.py --mode pilot` as the canonical deterministic gate entry for pilot validation evidence refresh.
- Make code changes only in `D:\产品设计\New folder`.
- Package from this root only.
- Keep Git as the only code-truth layer and use release artifacts only for delivery.
